---
phase: 07-schedule-time-upload-posting
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - packages/cli/src/schedule-resolver.ts
  - packages/cli/src/server.ts
  - packages/cli/src/__tests__/schedule-route.test.ts
  - packages/workspace/src/types.ts
  - packages/workspace/src/api.ts
  - packages/workspace/src/hooks/useSchedule.ts
  - packages/workspace/src/hooks/__tests__/useSchedule.test.ts
autonomous: true
requirements:
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
must_haves:
  truths:
    - "Workspace calls local CLI schedule route, not the hosted backend directly, for file upload orchestration"
    - "CLI reads brag-output/<draftId>/<format>.jpg and PUTs bytes to R2 using presigned URLs"
    - "CLI calls backend schedule endpoint only after all local PUTs finish"
    - "useSchedule exposes loading, uploading, scheduling, done, failed, confirmation, and error states"
  artifacts:
    - path: "packages/cli/src/schedule-resolver.ts"
      provides: "local file upload + backend schedule orchestration"
      contains: "uploadUrl"
    - path: "packages/workspace/src/hooks/useSchedule.ts"
      provides: "Workspace schedule state machine"
      contains: "schedulePost"
---

<objective>
Add the local scheduling orchestration layer: the Workspace triggers a local CLI route; the CLI uploads rendered JPEG bytes from disk to R2 via presigned URLs and then calls the backend schedule endpoint; the Workspace hook manages submit/progress/result state.
</objective>

<tasks>
<task type="tdd">
  <name>Task 1: Add CLI schedule route/resolver tests</name>
  <files>packages/cli/src/__tests__/schedule-route.test.ts</files>
  <read_first>
    - packages/cli/src/__tests__/video-render-route.test.ts
    - packages/cli/src/server.ts
    - packages/cli/src/render-resolver.ts
  </read_first>
  <action>
    Add tests with mocked `resolveAndSchedule`. Cover:
    - `POST /api/local/schedule` returns success confirmation for valid body
    - invalid/traversal draftId is rejected
    - only image formats are accepted
    - resolver receives outputDir, credentials.api_key, backend base, draftId, selections, caption, scheduling
  </action>
  <acceptance_criteria>
    - `npx vitest run packages/cli/src/__tests__/schedule-route.test.ts` fails before implementation and passes after
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement CLI schedule resolver and local route</name>
  <files>packages/cli/src/schedule-resolver.ts, packages/cli/src/server.ts</files>
  <read_first>
    - packages/cli/src/server.ts
    - packages/cli/src/render-resolver.ts
    - packages/cli/src/video-render-resolver.ts
    - packages/cli/src/proxy.ts
  </read_first>
  <action>
    Create `packages/cli/src/schedule-resolver.ts` exporting `resolveAndSchedule(args)`. It should:
    - determine selected image formats from selections
    - read files from `path.join(outputDir, draftId, `${format}.jpg`)`
    - call backend `POST /api/v1/schedule/upload-url` with Bearer API key and `{draftId, formats}`
    - PUT each local JPEG buffer to the returned `uploadUrl` with `Content-Type: image/jpeg`
    - after all PUTs succeed, call backend `POST /api/v1/schedule` with `{draftId, urls, keys, selections, caption, scheduling}`
    - write stdout progress lines: `[brag] Uploading <format> to R2`, `[brag] Scheduling via Buffer`, and success/failure
    In `server.ts`, register `POST /api/local/schedule` before `createBackendProxy`. Validate body and unsafe draftId using existing `isUnsafeOutputId`. Do not proxy file bytes through the hosted backend.
  </action>
  <acceptance_criteria>
    - `grep "api/v1/schedule/upload-url" packages/cli/src/schedule-resolver.ts` finds the presign call
    - `grep "api/v1/schedule" packages/cli/src/schedule-resolver.ts` finds the schedule call
    - `grep "api/local/schedule" packages/cli/src/server.ts` finds the local route registration before `createBackendProxy`
    - `npx vitest run packages/cli/src/__tests__/schedule-route.test.ts` passes
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 3: Add Workspace useSchedule tests</name>
  <files>packages/workspace/src/hooks/__tests__/useSchedule.test.ts</files>
  <read_first>
    - packages/workspace/src/hooks/__tests__/useVideoRender.test.ts
    - packages/workspace/src/hooks/useRender.ts
    - packages/workspace/src/api.ts
    - packages/workspace/src/types.ts
  </read_first>
  <action>
    Create hook tests with mocked API helpers. Cover:
    - initial phase is `idle`
    - trigger calls `flush()` first and fails with `Save failed before schedule` if it returns null
    - trigger calls `schedulePost()` with draftId, selections, caption, and scheduling
    - in-flight trigger is a no-op during `uploading` or `scheduling`
    - success stores confirmation rows
    - API rejection sets `phase:"failed"` and error message
  </action>
  <acceptance_criteria>
    - `npx vitest run packages/workspace/src/hooks/__tests__/useSchedule.test.ts` fails before implementation and passes after
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add Workspace schedule API/types/hook</name>
  <files>packages/workspace/src/types.ts, packages/workspace/src/api.ts, packages/workspace/src/hooks/useSchedule.ts</files>
  <read_first>
    - packages/workspace/src/types.ts
    - packages/workspace/src/api.ts
    - packages/workspace/src/hooks/useVideoRender.ts
    - packages/workspace/src/hooks/useRender.ts
  </read_first>
  <action>
    Add shared types: `SchedulePhase`, `ScheduleSelection`, `ScheduleMode`, `ScheduleConfirmation`, `ScheduleRequest`, and channel/integration types needed by the panel. Add API helpers:
    - `fetchIntegrations()` → `GET /api/v1/sous-chef/integrations`
    - `fetchRoutingDefaults()` → `GET /api/v1/routing-defaults`
    - `saveRoutingDefault(format, channels)` → `PUT /api/v1/routing-defaults`
    - `schedulePost(request)` → `POST /api/local/schedule`
    Implement `useSchedule({ flush, caption })` with phases `idle|uploading|scheduling|done|failed`, an in-flight guard, confirmation/error state, and a trigger that saves routing defaults before scheduling.
  </action>
  <acceptance_criteria>
    - `packages/workspace/src/api.ts` exports `schedulePost`
    - `packages/workspace/src/hooks/useSchedule.ts` exports `useSchedule`
    - `npx vitest run packages/workspace/src/hooks/__tests__/useSchedule.test.ts` passes
    - `npx tsc --noEmit --project packages/workspace/tsconfig.json` passes
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-PATH | Tampering | local file read | mitigate | Validate draftId/format and resolve paths under outputDir before reading |
| T-07-04-BYTES | Information Disclosure | rendered JPEG bytes | mitigate | Bytes go only to presigned R2 URL; hosted backend receives public URL/key only |
| T-07-04-REPLAY | Tampering | duplicate submit | mitigate | useSchedule in-flight guard blocks duplicate local schedule requests |
</threat_model>

<verification>
1. `npx vitest run packages/cli/src/__tests__/schedule-route.test.ts`
2. `npx vitest run packages/workspace/src/hooks/__tests__/useSchedule.test.ts`
3. `npx tsc --noEmit --project packages/workspace/tsconfig.json`
4. `npx tsc --noEmit --project packages/cli/tsconfig.json`
</verification>

<success_criteria>
The Workspace can initiate scheduling through the local CLI, and the CLI uploads local rendered JPEGs to R2 before calling the backend schedule endpoint.
</success_criteria>
