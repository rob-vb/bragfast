---
phase: 07-schedule-time-upload-posting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/integrations/buffer/push.ts
  - src/lib/integrations/__tests__/push.test.ts
autonomous: true
requirements:
  - SCHED-03
  - SCHED-04
must_haves:
  truths:
    - "pushToBuffer() supports queue mode with mode:'addToQueue' and schedulingType:'automatic'"
    - "pushToBuffer() supports exact-time mode with mode:'customScheduled' and dueAt set to a UTC ISO string"
    - "Buffer image-only guard remains: video-* formats still throw PushError('media')"
    - "Implements D-05 and uses Buffer's current documented createPost fields: schedulingType, mode, dueAt"
  artifacts:
    - path: "src/lib/integrations/buffer/push.ts"
      provides: "BufferPushParams.scheduling and createPost input mapping"
      contains: "customScheduled"
    - path: "src/lib/integrations/__tests__/push.test.ts"
      provides: "Unit tests for queue vs exact Buffer scheduling input"
      contains: "dueAt"
---

<objective>
Extend the Buffer push primitive so Phase 7 can schedule either the next queue slot or an exact future time without touching the higher-level schedule flow yet.
</objective>

<context>
Official Buffer docs verified 2026-05-21: queue mode uses `schedulingType: automatic` and `mode: addToQueue`; exact-time mode uses `schedulingType: automatic`, `mode: customScheduled`, and `dueAt` as a UTC ISO timestamp.
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Add Buffer scheduling tests</name>
  <files>src/lib/integrations/__tests__/push.test.ts</files>
  <read_first>
    - src/lib/integrations/__tests__/push.test.ts
    - src/lib/integrations/buffer/push.ts
  </read_first>
  <action>
    Add tests under `describe("pushToBuffer")` that mock `bufferGraphQL` and assert:
    - default/queue call sends `input.schedulingType === "automatic"` and `input.mode === "addToQueue"` with no `dueAt`
    - exact-time call with `scheduling: { type: "custom", scheduledAt: "2026-06-01T10:30:00.000Z" }` sends `input.mode === "customScheduled"` and `input.dueAt === "2026-06-01T10:30:00.000Z"`
    - existing video-format rejection still does not call `bufferGraphQL`
  </action>
  <acceptance_criteria>
    - `src/lib/integrations/__tests__/push.test.ts` contains `customScheduled`
    - `npx vitest run src/lib/integrations/__tests__/push.test.ts` fails before implementation and passes after
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend pushToBuffer scheduling input</name>
  <files>src/lib/integrations/buffer/push.ts</files>
  <read_first>
    - src/lib/integrations/buffer/push.ts
    - src/lib/integrations/buffer/graphql.ts
    - src/lib/integrations/error-classes.ts
  </read_first>
  <action>
    Extend `BufferPushParams` with optional `scheduling?: { type: "queue" } | { type: "custom"; scheduledAt: string }`. Keep existing callers working by defaulting to queue. Build `input` as:
    - always `schedulingType: "automatic"`
    - queue or absent: `mode: "addToQueue"`
    - custom: `mode: "customScheduled"` and `dueAt: params.scheduling.scheduledAt`
    Do not change media asset shape, error classification, or the video format guard.
  </action>
  <acceptance_criteria>
    - `src/lib/integrations/buffer/push.ts` exports `BufferPushParams` with `scheduling`
    - `grep "customScheduled" src/lib/integrations/buffer/push.ts` returns the exact mode mapping
    - `npx vitest run src/lib/integrations/__tests__/push.test.ts` passes
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-TIME | Tampering | `scheduledAt` input | mitigate | This primitive only maps values; later API routes validate ISO format and future time before calling it |
| T-07-01-MEDIA | Information Disclosure | Buffer media URL | accept | URL is the public R2 URL intentionally sent to Buffer after backend HEAD-check |
</threat_model>

<verification>
1. `npx vitest run src/lib/integrations/__tests__/push.test.ts`
2. `npx tsc --noEmit`
</verification>

<success_criteria>
Buffer push supports both queue and exact-time scheduling while preserving existing queue callers and video rejection.
</success_criteria>
