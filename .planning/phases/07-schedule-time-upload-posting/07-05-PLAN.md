---
phase: 07-schedule-time-upload-posting
plan: 05
type: execute
wave: 5
depends_on:
  - 07-04
files_modified:
  - packages/workspace/src/components/SchedulePanel.tsx
  - packages/workspace/src/pages/Editor.tsx
  - packages/workspace/src/__tests__/schedule-panel.test.tsx
  - packages/workspace/src/__tests__/editor-flow.test.tsx
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - SCHED-06
must_haves:
  truths:
    - "Workspace schedule panel shows Buffer channels grouped by Landscape, Square, Portrait"
    - "Panel preselects saved routingDefaults and uses BUILT_IN_FORMAT_DEFAULTS only when no saved defaults exist"
    - "Panel supports Next queue slot and Exact time with native datetime-local input"
    - "Panel disables submit when Buffer is not connected or no channels are selected"
    - "Success confirmation names scheduled channels and queue/exact post time"
    - "Admin gallery scheduled badge is visible via PixelBadge status='scheduled'"
  artifacts:
    - path: "packages/workspace/src/components/SchedulePanel.tsx"
      provides: "Schedule panel UI"
      contains: "Schedule post"
    - path: "packages/workspace/src/pages/Editor.tsx"
      provides: "SchedulePanel wiring"
      contains: "useSchedule"
---

<objective>
Build the user-facing scheduling UI in the Workspace and finish Admin scheduled-badge visibility, then stop at the human verification checkpoint.
</objective>

<tasks>
<task type="tdd">
  <name>Task 1: Add schedule panel UI tests</name>
  <files>packages/workspace/src/__tests__/schedule-panel.test.tsx, packages/workspace/src/__tests__/editor-flow.test.tsx</files>
  <read_first>
    - packages/workspace/src/__tests__/editor-flow.test.tsx
    - packages/workspace/src/components/RenderPanel.tsx
    - packages/workspace/src/components/FormatSwitcher.tsx
    - packages/workspace/src/hooks/useSchedule.ts
    - packages/workspace/src/types.ts
  </read_first>
  <action>
    Add component tests covering:
    - Buffer not connected notice: `Buffer not connected. Connect Buffer in Settings to schedule posts.`
    - channel groups render labels `Landscape`, `Square`, `Portrait`
    - saved routing defaults pre-check channels
    - clicking `Exact time` reveals label `Post at` and a `datetime-local` input
    - submit calls `trigger()` with selected channels and scheduling type
    - success shows `Scheduled` and channel confirmation lines
    Extend editor-flow coverage to assert `SchedulePanel` appears after the render panel wiring once image output is active.
  </action>
  <acceptance_criteria>
    - `npx vitest run packages/workspace/src/__tests__/schedule-panel.test.tsx packages/workspace/src/__tests__/editor-flow.test.tsx` fails before implementation and passes after
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement SchedulePanel</name>
  <files>packages/workspace/src/components/SchedulePanel.tsx</files>
  <read_first>
    - .planning/phases/07-schedule-time-upload-posting/07-UI-SPEC.md
    - packages/workspace/src/components/RenderPanel.tsx
    - packages/workspace/src/components/FormatSwitcher.tsx
    - packages/workspace/src/hooks/useSchedule.ts
    - packages/workspace/src/types.ts
    - src/lib/integrations/channel-classes.ts
  </read_first>
  <action>
    Create `SchedulePanel` following the UI-SPEC exactly:
    - card class matches RenderPanel: `rounded-[8px] border border-[var(--workspace-border)] bg-[var(--workspace-bg)] p-4`
    - heading `Schedule post`
    - show only Buffer channels from integrations `extra.channels`
    - group by image formats `landscape`, `square`, `portrait`
    - pre-check saved routing defaults; if a format has no saved default, pre-check channels whose Buffer service maps through `channelClassFromBufferService()` into `BUILT_IN_FORMAT_DEFAULTS[format]`
    - queue/exact toggle copy: `Next queue slot`, `Exact time`
    - exact mode reveals `Post at` + `<input type="datetime-local">`
    - progress copy: `Uploading to R2…`, `Scheduling via Buffer…`
    - success heading `Scheduled`
    - all buttons/rows min-h 44px and focus-visible lime outline
    Do not add shadcn components to the Workspace SPA.
  </action>
  <acceptance_criteria>
    - `grep "Schedule post" packages/workspace/src/components/SchedulePanel.tsx` finds heading and CTA copy
    - `grep "datetime-local" packages/workspace/src/components/SchedulePanel.tsx` finds exact-time input
    - `grep "BUILT_IN_FORMAT_DEFAULTS" packages/workspace/src/components/SchedulePanel.tsx` finds default seeding
    - `npx vitest run packages/workspace/src/__tests__/schedule-panel.test.tsx` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire SchedulePanel into Editor</name>
  <files>packages/workspace/src/pages/Editor.tsx</files>
  <read_first>
    - packages/workspace/src/pages/Editor.tsx
    - packages/workspace/src/components/SchedulePanel.tsx
    - packages/workspace/src/hooks/useSchedule.ts
    - packages/workspace/src/components/RenderPanel.tsx
  </read_first>
  <action>
    Import `SchedulePanel` and `useSchedule`. Instantiate `const schedule = useSchedule({ flush: save.flush, caption: config.caption ?? "" })`. Render `SchedulePanel` in the right column below `RenderPanel`, but only for `output === "image"` because video posting is deferred. Pass the active render job id or draft id needed by `useSchedule`, active image format state, and channel selection props expected by the panel. The schedule UI should be usable after images are rendered; it may show a hint if render output is not ready.
  </action>
  <acceptance_criteria>
    - `grep "useSchedule" packages/workspace/src/pages/Editor.tsx` finds import and hook use
    - `grep "SchedulePanel" packages/workspace/src/pages/Editor.tsx` finds component mount
    - `npx vitest run packages/workspace/src/__tests__/editor-flow.test.tsx packages/workspace/src/__tests__/schedule-panel.test.tsx` passes
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Human verification: schedule-time upload and Buffer queue/exact scheduling</name>
  <what-built>
    Full Phase 7 scheduling path: Workspace channel picker, exact/queue timing, CLI direct R2 upload, backend HEAD-check + Buffer schedule, scheduled release row, and Admin scheduled badge.
  </what-built>
  <how-to-verify>
    1. Connect Buffer in Admin Settings and confirm channels are listed.
    2. Open Workspace, render images for a draft, and confirm output files exist.
    3. Open the schedule panel, confirm Buffer channels appear grouped by Landscape/Square/Portrait.
    4. Select `Next queue slot`, schedule, and confirm Workspace shows `Scheduled` with channel names.
    5. Schedule another draft with `Exact time`, using a near-future time, and confirm Buffer shows the scheduled post.
    6. Open Admin history and confirm a `scheduled` badge appears for the release.
    7. Temporarily delete one local rendered file and retry scheduling; confirm the flow fails with `Upload incomplete — try rendering again, then reschedule.` and no partial Buffer post is created.
  </how-to-verify>
  <resume-signal>
    Type `approved` if all checks pass, or describe observed issues.
  </resume-signal>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-CHANNEL | Tampering | channel selection UI | mitigate | UI only submits hardcoded provider `buffer` and backend validates channel shape |
| T-07-05-TIME | Tampering | datetime-local input | mitigate | UI converts local value to UTC ISO; backend validates ISO and rejects invalid custom timing |
| T-07-05-STATUS | Spoofing | scheduled badge | mitigate | Badge is driven from Convex `releases.status`, not client-provided UI state |
</threat_model>

<verification>
1. `npx vitest run packages/workspace/src/__tests__/schedule-panel.test.tsx packages/workspace/src/__tests__/editor-flow.test.tsx`
2. `npx tsc --noEmit --project packages/workspace/tsconfig.json`
3. `npx vitest run`
4. Manual UAT from checkpoint
</verification>

<success_criteria>
Users can pick Buffer channels, choose queue or exact timing, schedule rendered image creations, see confirmation, and see scheduled creations in Admin history with a Scheduled badge.
</success_criteria>
