---
phase: 01-render-core-extraction
plan: 06
type: execute
wave: 4
depends_on: [01-04, 01-05]
files_modified:
  - package.json
  - next.config.ts
  - src/lib/pipeline/render.ts
  - src/lib/pipeline/render-video.ts
autonomous: true
requirements:
  - SC#1
  - SC#2
  - SC#3
  - SC#5
must_haves:
  truths:
    - "The Next.js app still builds (npm run build) with the app-side render.ts and render-video.ts now delegating to @bragfast/render-core"
    - "render.ts outer shell calls renderImage() from @bragfast/render-core instead of its own Satori/Sharp loop"
    - "render-video.ts outer shell calls renderVideo() from @bragfast/render-core instead of the old renderVideoLocal() private function"
    - "@bragfast/render-core appears in next.config.ts serverExternalPackages so Next.js does not try to webpack-bundle the native package"
    - "The existing POST /api/v1/cook/image and convex/videoRender.ts continue to work unchanged"
  artifacts:
    - path: "next.config.ts"
      provides: "serverExternalPackages includes @bragfast/render-core"
      contains: "@bragfast/render-core"
    - path: "package.json"
      provides: "root package.json has @bragfast/render-core workspace dep and workspaces field"
      contains: "workspace:*"
  key_links:
    - from: "src/lib/pipeline/render.ts"
      to: "@bragfast/render-core"
      via: "import { renderImage } from '@bragfast/render-core'"
      pattern: "from.*@bragfast/render-core"
    - from: "src/lib/pipeline/render-video.ts"
      to: "@bragfast/render-core"
      via: "import { renderVideo } from '@bragfast/render-core'"
      pattern: "from.*@bragfast/render-core"
    - from: "next.config.ts"
      to: "packages/render-core"
      via: "serverExternalPackages"
      pattern: "@bragfast/render-core"
user_setup: []
---

<objective>
Wire the app to consume `@bragfast/render-core`: add workspace dep to root package.json, add to next.config.ts serverExternalPackages, and adapt the inner loops of `render.ts` and `render-video.ts` to delegate to `renderImage()` / `renderVideo()` respectively. The outer shells (Convex mutations, R2 upload, credit accounting, webhook) remain unchanged.

Purpose: This is the final validation that the package boundary is clean. The existing cook API routes continue to work because the outer shells are unchanged — only the inner render calls are replaced. After this plan, all 5 success criteria are green.

Output: A working app that delegates rendering to the package, confirmed by `npm run build` succeeding and the existing API routes remaining functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-render-core-extraction/01-CONTEXT.md
@.planning/phases/01-render-core-extraction/01-PATTERNS.md
@.planning/phases/01-render-core-extraction/01-VALIDATION.md

<interfaces>
<!-- From next.config.ts (current state, read before editing) -->
serverExternalPackages: [
  "@remotion/lambda", "@remotion/bundler", "@remotion/renderer",
  "@remotion/lambda-client", "@remotion/serverless-client", "sharp",
]
outputFileTracingIncludes: { "/api/**": ["./src/assets/fonts/**/*"] }

<!-- From src/lib/pipeline/render.ts (app side — outer shell stays, inner loop changes) -->
The inner Satori/Sharp loop (lines 159–195 in renderReleaseAsync) is REPLACED by:
  const coreResult = await renderImage(coreReq);
  where coreReq: LocalRenderRequest is assembled from the already-resolved inputs that
  renderReleaseAsync has at that point (after resolveAllTemplates, resolveBrand, buildSlideDataMaps,
  prefetchStaticImages calls complete — all of which STAY in render.ts).

<!-- From src/lib/pipeline/render-video.ts (app side — outer shell stays) -->
renderVideoLocal() private function at lines 240–275 is REPLACED by:
  const coreResult = await renderVideo(coreReq);
  where coreReq: LocalVideoRenderRequest includes:
    compositionId, inputProps (unchanged from current render-video.ts),
    remotionEntryPoint: path.join(process.cwd(), "src/remotion/index.ts")
    (process.cwd() is fine in the app route — the app knows its own directory structure)

<!-- @bragfast/render-core public API available after Plans 03–05 -->
import { renderImage, renderVideo, LocalRenderRequest, LocalVideoRenderRequest,
  ImageRenderResult, VideoRenderResult } from "@bragfast/render-core"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: next.config.ts + root package.json workspace dep + npm install</name>
  <read_first>
    - next.config.ts — read full file before editing (current serverExternalPackages and outputFileTracingIncludes)
    - package.json (root) — read the "dependencies" section and confirm "workspaces" field presence (added in Plan 01; check it's there)
    - .planning/phases/01-render-core-extraction/01-PATTERNS.md §"next.config.ts (modified)" — exact change
    - .planning/phases/01-render-core-extraction/01-PATTERNS.md §"package.json (root, modified)" — workspace dep entry
    - .planning/phases/01-render-core-extraction/01-RESEARCH.md §"ESM vs CJS Decision" — why serverExternalPackages (not transpilePackages)
  </read_first>
  <files>
    next.config.ts,
    package.json
  </files>
  <action>
    next.config.ts:
      Add "@bragfast/render-core" as the FIRST entry in serverExternalPackages array.
      Result: serverExternalPackages: ["@bragfast/render-core", "@remotion/lambda", ...existing...].
      Add "./packages/render-core/fonts/**/*" to outputFileTracingIncludes["/api/**"] array.
      Result: ["./src/assets/fonts/**/*", "./packages/render-core/fonts/**/*"].
      These two changes ensure Next.js: (1) does not try to webpack-bundle the native package,
      and (2) includes the package's font assets when deploying serverless API routes.
      No other changes to next.config.ts.

    package.json (root):
      Add "@bragfast/render-core": "workspace:*" to the "dependencies" section.
      The "workspaces": ["packages/*"] field was added in Plan 01 — verify it is present before editing.
      No other changes.

    THEN run: npm install (from repo root) to link the new workspace dep.
      Verify: node -e "require('@bragfast/render-core')" exits 0 (package resolves from root).
  </action>
  <verify>
    <automated>
      cd /Users/rob/Development/bragfast &&
      grep -q "@bragfast/render-core" next.config.ts &&
      grep -q "workspace:\*" package.json &&
      node -e "require('@bragfast/render-core')" &&
      echo "workspace link OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts serverExternalPackages contains "@bragfast/render-core"
    - package.json dependencies contains "@bragfast/render-core": "workspace:*"
    - node -e "require('@bragfast/render-core')" exits 0 (workspace symlink resolves)
    - outputFileTracingIncludes includes ./packages/render-core/fonts/**/*
    Maps to: SC#5 prerequisite (correct peerDep wiring for cross-platform Sharp), SC#1/SC#2 (app can now import renderImage/renderVideo)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adapt render.ts + render-video.ts to delegate to render-core + build verification</name>
  <read_first>
    - src/lib/pipeline/render.ts — read full file (understand full outer shell: Convex mutations, R2 upload path, OUTPUT_LOCAL branch, credit handling, webhook — all STAY; only inner Satori/Sharp loop is replaced)
    - src/lib/pipeline/render-video.ts — read full file (understand full outer shell + the renderVideoLocal private fn at lines 240–275 that is now in render-core; how renderVideoAsync calls renderVideoLocal)
    - .planning/phases/01-render-core-extraction/01-PATTERNS.md §"App side — files that STAY" — which functions stay in each file
    - .planning/phases/01-render-core-extraction/01-RESEARCH.md §"Architecture Patterns" §system diagram — clarifies caller/core boundary
    - .planning/phases/01-render-core-extraction/01-CONTEXT.md §"D-01, D-02, D-03" — confirm outer shell responsibilities
  </read_first>
  <files>
    src/lib/pipeline/render.ts,
    src/lib/pipeline/render-video.ts
  </files>
  <action>
    src/lib/pipeline/render.ts:
      Add import: import { renderImage } from "@bragfast/render-core";
      Add import types: import type { LocalRenderRequest, ImageRenderResult } from "@bragfast/render-core".

      Inside renderReleaseAsync (or the function that contains the Satori/Sharp loop):
        After the existing calls to resolveAllTemplates(), resolveBrand(), buildSlideDataMaps(),
        prefetchStaticImages() — which all STAY unchanged — add a runtime purity guard (D-01)
        before constructing the core request:
          // D-01 purity guard: render-core requires logoBase64 (not logo_url) — enforce at the seam
          if (resolvedBrand.logo_url && !resolvedBrand.logoBase64) {
            throw new Error(
              `render-core purity violation: brand "${resolvedBrand.name}" has logo_url but logoBase64 is missing. ` +
              "resolveBrand() must fetch and embed the logo as base64 before calling renderImage()."
            );
          }
        Then assemble the LocalRenderRequest:
          const coreReq: LocalRenderRequest = {
            formats: [RESOLVED format entries assembled from the resolve* outputs],
            brand: resolvedBrand,  // already resolved (logoBase64, not logo_url)
          };
        Replace the inner for-format/for-slide Satori/Sharp loop (render.ts lines 159–195) with:
          const coreResult: ImageRenderResult = await renderImage(coreReq);
        After renderImage returns:
          Use coreResult.formats[format].slides[slideIndex] Buffers where the old code
          used jpgBuffer — feed them to the existing uploadImage() / writeFile() calls
          (the OUTPUT_LOCAL branch and R2 upload branch stay as-is; they just receive Buffers now).
        Remove the now-inlined satori/sharp/CanvasRenderer imports from render.ts if they are
        no longer used by anything else in the file. Do NOT remove resolveTemplate, resolveBrand,
        buildSlideDataMaps, prefetchStaticImages, cleanup imports — those stay.
        Keep: all Convex mutations (markCompleted, markFailed, refund), uploadImage, webhook delivery.

    src/lib/pipeline/render-video.ts:
      Add import: import { renderVideo } from "@bragfast/render-core";
      Add import types: import type { LocalVideoRenderRequest, VideoRenderResult } from "@bragfast/render-core".

      Inside the function that currently calls renderVideoLocal() (the app-side local render branch):
        Replace the call to renderVideoLocal(compositionId, inputProps, outputPath) with:
          const coreReq: LocalVideoRenderRequest = {
            compositionId,
            inputProps,
            remotionEntryPoint: path.join(process.cwd(), "src/remotion/index.ts"),
            // process.cwd() is correct here: app route runs from repo root, knows its structure
          };
          const coreResult: VideoRenderResult = await renderVideo(coreReq);
        Use coreResult.buffer as the video Buffer for the existing R2 upload / OUTPUT_LOCAL branch.
        Remove the private renderVideoLocal() function body (lines 240–275) — it now lives in render-core.
        Keep: all Convex mutations, uploadImage, R2 upload, Lambda path (leave Lambda path unchanged;
        it is not the active path but removing it would be risky scope creep).

    THEN run: npm run build
      This compiles Next.js with the updated files.
      Expected: build exits 0. If TypeScript errors appear in the wiring:
        - "Property X does not exist on LocalRenderRequest" → check LocalRenderRequest type in packages/render-core/src/types.ts
        - "renderImage is not a function" → check tsup built dist/index.js has the export
        - "Cannot find module @bragfast/render-core" → run npm install first (Task 1)
      Fix any TypeScript errors in render.ts / render-video.ts; do NOT change the package types.
  </action>
  <verify>
    <automated>
      cd /Users/rob/Development/bragfast &&
      grep -q "@bragfast/render-core" src/lib/pipeline/render.ts &&
      grep -q "@bragfast/render-core" src/lib/pipeline/render-video.ts &&
      node packages/render-core/scripts/audit-deps.mjs &&
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (Next.js app builds successfully with the adapted files)
    - src/lib/pipeline/render.ts contains import from "@bragfast/render-core"
    - src/lib/pipeline/render-video.ts contains import from "@bragfast/render-core"
    - The private renderVideoLocal() function is removed from render-video.ts (now in render-core)
    - audit-deps.mjs still exits 0 (render.ts and render-video.ts are app-side — they ARE allowed to import convex; audit only checks packages/render-core/src/)
    - npm run lint exits 0 (no new linting errors from the wiring changes)
    Maps to: SC#1 (app route uses renderImage from package), SC#2 (app route uses renderVideo from package), SC#5 (app builds with native deps wired correctly)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js app route → @bragfast/render-core | App imports render functions from the workspace package |
| src/lib/pipeline/render.ts → render-core renderImage | Assembled LocalRenderRequest (resolved brand, base64 media) crosses the boundary |
| src/lib/pipeline/render-video.ts → render-core renderVideo | process.cwd() path passed as remotionEntryPoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | remotionEntryPoint from process.cwd() in render-video.ts | accept | process.cwd() in the app route is the repo root (controlled environment); no user input reaches this value; the app route authenticates callers before this code runs |
| T-06-02 | Information Disclosure | LocalRenderRequest brand.logoBase64 in app-core boundary | accept | Brand data is already resolved from Convex before being passed to render-core; render-core does not log or store it; the package returns Buffers only |
| T-06-03 | Elevation of Privilege | Next.js serverExternalPackages bypass | accept | serverExternalPackages is correct configuration for native-dep packages; it prevents webpack from trying to bundle Sharp/Remotion, which would fail; not a security boundary |
| T-06-SC | Tampering | npm/pip installs | accept | npm install only links the workspace package (already in monorepo); no remote packages fetched in this plan |
</threat_model>

<verification>
After both tasks complete — the full phase validation suite:
1. `node packages/render-core/scripts/audit-deps.mjs` exits 0 (SC#3 final check)
2. `npx vitest run packages/render-core` exits 0 (SC#4 green)
3. `node packages/render-core/scripts/prove-image.mjs` exits 0 (SC#1 green)
4. `node packages/render-core/scripts/prove-video.mjs` exits 0 (SC#2 green)
5. `npm run build` exits 0 (app still compiles with package wired in)
6. SC#5: push to main and verify GitHub Actions matrix (macos-14 + ubuntu-22.04) runs green
</verification>

<success_criteria>
- npm run build exits 0 (app compiles with adapted render.ts and render-video.ts)
- All 5 SC proof commands pass simultaneously
- render.ts and render-video.ts no longer contain the Satori/Sharp/Remotion inner loops — those are in render-core
- The package boundary is clean: packages/render-core/src/ has zero convex/@aws-sdk/next imports
</success_criteria>

<output>
Create `.planning/phases/01-render-core-extraction/01-06-SUMMARY.md` when done
</output>
