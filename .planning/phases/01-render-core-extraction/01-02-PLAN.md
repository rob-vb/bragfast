---
phase: 01-render-core-extraction
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - packages/render-core/vitest.config.ts
  - packages/render-core/src/__tests__/fonts.test.ts
  - packages/render-core/scripts/prove-image.mjs
  - packages/render-core/scripts/prove-video.mjs
  - packages/render-core/scripts/audit-deps.mjs
  - .github/workflows/render-core-ci.yml
autonomous: true
requirements:
  - SC#1
  - SC#2
  - SC#3
  - SC#4
  - SC#5
must_haves:
  truths:
    - "All 5 success criteria have an automated proof script or CI check before any source extraction runs"
    - "audit-deps.mjs catches forbidden imports (convex, @aws-sdk, next) in packages/render-core/src/"
    - "fonts.test.ts fails RED until fonts.ts __dirname fix is applied (Plan 03)"
    - "prove-image.mjs runs standalone and fails only because renderImage() is not yet exported"
    - "CI matrix runs on macos-14 (arm64) and ubuntu-22.04 (x64)"
  artifacts:
    - path: "packages/render-core/vitest.config.ts"
      provides: "package-scoped vitest config (node environment, no jsdom)"
      min_lines: 10
    - path: "packages/render-core/src/__tests__/fonts.test.ts"
      provides: "SC#4 unit test for __dirname font resolution"
      contains: "process.chdir"
    - path: "packages/render-core/scripts/prove-image.mjs"
      provides: "SC#1 standalone image render proof"
      contains: "renderImage"
    - path: "packages/render-core/scripts/prove-video.mjs"
      provides: "SC#2 standalone video render proof"
      contains: "renderVideo"
    - path: "packages/render-core/scripts/audit-deps.mjs"
      provides: "SC#3 forbidden-import static scan"
      contains: "convex"
    - path: ".github/workflows/render-core-ci.yml"
      provides: "SC#5 cross-platform CI matrix"
      contains: "macos-14"
  key_links:
    - from: "packages/render-core/vitest.config.ts"
      to: "packages/render-core/src/__tests__/"
      via: "include glob"
      pattern: "packages/render-core/src"
    - from: ".github/workflows/render-core-ci.yml"
      to: "packages/render-core/scripts/"
      via: "npm run audit:purity + npm test steps"
      pattern: "audit-deps"
user_setup: []
---

<objective>
Create all Wave 0 test scaffolding — proof scripts, audit script, vitest config, and CI matrix — before any source extraction begins. These are the automated guards that tell the executor when extraction is correct.

Purpose: VALIDATION.md §"Wave 0 Requirements" mandates these scripts exist before plans run. The Nyquist rule requires every task have automated verification; proof scripts ARE that verification for SC#1–SC#5. Scripts start in a failing state (renderImage/renderVideo not yet exported) and turn green as extraction plans complete.

Output: Five test files + CI workflow covering all five success criteria, each runnable immediately even though most will fail until extraction is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-render-core-extraction/01-VALIDATION.md
@.planning/phases/01-render-core-extraction/01-PATTERNS.md
@.planning/phases/01-render-core-extraction/01-RESEARCH.md

<interfaces>
<!-- From VALIDATION.md — exact script names and commands -->
SC#1 proof: node packages/render-core/scripts/prove-image.mjs
SC#2 proof: node packages/render-core/scripts/prove-video.mjs
SC#3 audit: node packages/render-core/scripts/audit-deps.mjs
SC#4 unit:  npx vitest run packages/render-core -t "font __dirname"
SC#5 CI:    GitHub Actions .github/workflows/render-core-ci.yml — macos-14 + ubuntu-22.04

<!-- From PATTERNS.md §"packages/render-core/scripts/audit-purity.js" -->
Forbidden strings: "convex", "@aws-sdk", "next"
Grep pattern: grep -r --include="*.ts" --include="*.tsx" in packages/render-core/src/

<!-- From PATTERNS.md §"packages/render-core/src/__tests__/fonts.test.ts" -->
Test must chdir to /tmp before calling loadFontsForFamily("Plus Jakarta Sans")
afterEach restores original cwd

<!-- From RESEARCH.md §"Sharp Native Binaries (SC#5)" -->
CI matrix: macos-14 (arm64), ubuntu-22.04 (x64)
Node version: 22, cache: npm
Steps: checkout → setup-node → npm ci → audit:purity → npm test

<!-- From vitest.config.ts (root) — root uses jsdom; package needs node environment -->
Root vitest config environment: jsdom — package config MUST override to node
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest package config + SC#4 fonts unit test</name>
  <read_first>
    - .planning/phases/01-render-core-extraction/01-PATTERNS.md §"packages/render-core/src/__tests__/fonts.test.ts" — exact test body with chdir
    - .planning/phases/01-render-core-extraction/01-VALIDATION.md — SC#4 vitest run command
    - vitest.config.ts (root) — read existing config to understand what the package must override (environment: jsdom → must become node)
  </read_first>
  <files>
    packages/render-core/vitest.config.ts,
    packages/render-core/src/__tests__/fonts.test.ts
  </files>
  <action>
    packages/render-core/vitest.config.ts:
      Import defineConfig from "vitest/config". No plugins (no React needed — server-side only).
      test: environment "node" (NOT jsdom — this is a Node package; root config uses jsdom
      which would break fs/path operations), globals true.
      include: ["src/__tests__/**/*.test.ts"].
      resolve alias: { "@bragfast/render-core": path.resolve(__dirname, "src/index.ts") }
      so tests can import from the package root without a dist/ build.

    packages/render-core/src/__tests__/fonts.test.ts:
      Import { describe, it, expect, afterEach } from "vitest".
      Import { loadFontsForFamily } from "../fonts".
      describe("font __dirname resolution") block:
        Store originalCwd = process.cwd() before tests.
        afterEach: process.chdir(originalCwd) — restore cwd after each test.
        it("loads Plus Jakarta Sans regardless of cwd"):
          process.chdir("/tmp");
          const fonts = await loadFontsForFamily("Plus Jakarta Sans");
          expect(fonts.length).toBeGreaterThan(0);
          expect(fonts[0].data.byteLength).toBeGreaterThan(0);
      This test starts RED (fonts.ts not yet moved; src/fonts.ts uses process.cwd() which
      will fail from /tmp). It turns GREEN in Plan 03 when fonts.ts is extracted with __dirname.
      Do NOT import from "../../../src/lib/fonts" — import from "../fonts" (the new package path).
  </action>
  <verify>
    <automated>
      cd /Users/rob/Development/bragfast &&
      node -e "require('./packages/render-core/vitest.config.ts')" 2>/dev/null || true &&
      ls packages/render-core/src/__tests__/fonts.test.ts &&
      grep -q "process.chdir" packages/render-core/src/__tests__/fonts.test.ts &&
      grep -q "loadFontsForFamily" packages/render-core/src/__tests__/fonts.test.ts &&
      echo "scaffold OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/render-core/vitest.config.ts exists with environment "node" (not jsdom)
    - packages/render-core/src/__tests__/fonts.test.ts exists with process.chdir("/tmp") guard
    - Test imports loadFontsForFamily from "../fonts" (package-relative path, not app path)
    - Running `npx vitest run packages/render-core` reports the test (even if it fails — fail is expected before Plan 03)
    Maps to: SC#4 — font __dirname resolution unit test
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Proof scripts (SC#1, SC#2, SC#3) and CI matrix (SC#5)</name>
  <read_first>
    - .planning/phases/01-render-core-extraction/01-PATTERNS.md §"packages/render-core/scripts/test-image.ts" — SC#1 script shape
    - .planning/phases/01-render-core-extraction/01-PATTERNS.md §"packages/render-core/scripts/test-video.ts" — SC#2 script shape
    - .planning/phases/01-render-core-extraction/01-PATTERNS.md §"packages/render-core/scripts/audit-purity.js" — SC#3 grep pattern
    - .planning/phases/01-render-core-extraction/01-RESEARCH.md §"Sharp Native Binaries (SC#5)" — CI YAML shape
    - .planning/phases/01-render-core-extraction/01-RESEARCH.md §"Dependency Audit Enforcement (SC#3)" — Tier 1 static grep
    - .planning/phases/01-render-core-extraction/01-VALIDATION.md — exact proof script filenames (prove-image.mjs, prove-video.mjs, audit-deps.mjs)
  </read_first>
  <files>
    packages/render-core/scripts/prove-image.mjs,
    packages/render-core/scripts/prove-video.mjs,
    packages/render-core/scripts/audit-deps.mjs,
    .github/workflows/render-core-ci.yml
  </files>
  <action>
    NOTE: VALIDATION.md uses .mjs extension (prove-image.mjs, prove-video.mjs, audit-deps.mjs).
    PATTERNS.md uses .ts/.js. Use .mjs as authoritative (matches VALIDATION.md §Per-Task Verification Map).

    packages/render-core/scripts/audit-deps.mjs:
      ESM script (#!/usr/bin/env node, type: module implied by .mjs).
      Use import { execSync } from "child_process" and { createRequire } for path.
      Scan packages/render-core/src/ for files containing "convex", "@aws-sdk", "next" as import strings.
      Use grep -r --include="*.ts" --include="*.tsx" for each forbidden string, checking
      for import-style occurrences: from "convex", from "@aws-sdk", from "next".
      On any match: log "FAIL" + matching file, set exitCode = 1.
      On no matches: log "PASS: no forbidden imports (convex, @aws-sdk, next)".
      process.exit(exitCode).
      Guard: grep exits 1 when no matches — catch the non-zero exit and treat as PASS.
      Derive srcDir from import.meta.url using fileURLToPath + path.dirname, then ../src.
      This is the SC#3 check per VALIDATION.md.

    packages/render-core/scripts/prove-image.mjs:
      ESM script. Import { renderImage } from "../src/index.js" — will throw at Plan 01
      (stub barrel has no renderImage). Script must gracefully handle this:
      wrap in try/catch; if error message includes "is not a function" or "not exported",
      print "SC#1 PENDING — renderImage not yet exported (expected until Plan 03)" and exit 0.
      Once renderImage is exported (Plans 03–04): construct a minimal LocalRenderRequest with:
        brand: { name: "Test", logoBase64: "", website: "",
          colors: { background: "#ffffff", text: "#000000", primary: "#ff0000" } }
        formats: landscape + square + portrait, each with one slide.
        For slide: use STANDARD_BROWSER template (import getDefaultConfig from
          "../../src/lib/templates/default-configs.js") — test-only app-side import, acceptable.
        objectData: { title: { text: "Hello SC#1" } }
      Call renderImage(req), assert each format returns Buffer with length > 1000 bytes.
      Write /tmp/test-render-core-{fmt}.jpg per format.
      Print "SC#1 PASS: {fmt} → {N} bytes ({dimensions})".

    packages/render-core/scripts/prove-video.mjs:
      ESM script. Import { renderVideo } from "../src/index.js" — same graceful pending logic.
      If renderVideo not exported: print "SC#2 PENDING" and exit 0.
      Once exported (Plan 05): construct LocalVideoRenderRequest:
        compositionId: "landscape"
        remotionEntryPoint: path.join(process.cwd(), "src/remotion/index.ts")
          — this path uses process.cwd() intentionally: the proof script is test-only code
          run from the repo root, and the Remotion composition lives in the app repo (RESEARCH §Open Question 2).
        inputProps: minimal object matching VideoCanvasComposition expected props:
          { config: getDefaultConfig("standard-browser"), format: "landscape",
            slides: [{ title: { text: "Hello SC#2" } }],
            brand: { name: "Test", logoBase64: "", website: "",
              colors: { background: "#fff", text: "#000", primary: "#f00" },
              font_family: "Plus Jakarta Sans" },
            slideDuration: 4, slideDurations: [4] }
      Assert result.buffer.length > 10000. Write /tmp/test-render-core-video.mp4.
      Print "SC#2 PASS: landscape video → {N} bytes".

    .github/workflows/render-core-ci.yml:
      name: "render-core CI"
      on: push (branches: main, feat/**) + pull_request.
      jobs.test:
        strategy.matrix.os: [macos-14, ubuntu-22.04]
        runs-on: ${{ matrix.os }}
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4 (node-version: '22', cache: 'npm')
          - run: npm ci
          - run: npm run build --workspace=packages/render-core
          - run: node packages/render-core/scripts/audit-deps.mjs
          - run: npx vitest run packages/render-core
        env: (none — render-core is network-pure for unit tests; prove-video not run in CI
          because it requires Chromium download which is slow; SC#5 validates Sharp binary only)
      Note: prove-image.mjs and prove-video.mjs are NOT included in CI steps — they are
      developer-run integration proofs. CI runs audit-deps + vitest unit tests only.
      SC#5 is validated by the CI matrix successfully completing npm ci (Sharp downloads
      the correct platform binary) and the vitest run passing.
      SC#1 CI coverage: Plan 03 Task 1 (fonts.test.ts) already gates the __dirname fix in CI.
      For a true CI-runnable SC#1 smoke test, add a minimal vitest integration test in
      packages/render-core/src/__tests__/render-image.test.ts (Plan 04 scope) that calls
      renderImage() with a tiny in-memory CanvasTemplateConfig + resolved Brand using bundled
      fonts only (no Google Fonts fetch, no Chromium). This gives CI an automated SC#1 gate
      without the Chromium download overhead. prove-image.mjs remains the full developer proof.
      prove-video.mjs is developer-only by design — Chromium download size (~200MB) makes it
      unsuitable for standard CI; this decision is intentional and documented here.
  </action>
  <verify>
    <automated>
      cd /Users/rob/Development/bragfast &&
      ls packages/render-core/scripts/audit-deps.mjs &&
      ls packages/render-core/scripts/prove-image.mjs &&
      ls packages/render-core/scripts/prove-video.mjs &&
      ls .github/workflows/render-core-ci.yml &&
      grep -q "macos-14" .github/workflows/render-core-ci.yml &&
      grep -q "ubuntu-22.04" .github/workflows/render-core-ci.yml &&
      node packages/render-core/scripts/audit-deps.mjs &&
      echo "scaffold OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/render-core/scripts/audit-deps.mjs exits 0 on the current (empty src/) state
    - packages/render-core/scripts/prove-image.mjs exits 0 with "SC#1 PENDING" message (renderImage not yet exported)
    - packages/render-core/scripts/prove-video.mjs exits 0 with "SC#2 PENDING" message
    - .github/workflows/render-core-ci.yml has matrix: [macos-14, ubuntu-22.04]
    - audit-deps.mjs correctly exits 1 when a forbidden string is present (verify by testing: echo "import 'convex'" | grep -c "convex" should match)
    Maps to: SC#1 proof (pending), SC#2 proof (pending), SC#3 audit script, SC#5 CI matrix
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → npm registry | CI runs npm ci which fetches packages over the network |
| prove-video.mjs → Remotion/Chromium | When run locally, prove-video calls ensureBrowser() which downloads Chromium |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | audit-deps.mjs grep scan | mitigate | Script scans actual source files for string patterns; cannot be bypassed without modifying source; exits 1 on any match |
| T-02-02 | Information Disclosure | CI workflow | accept | Workflow only runs public npm commands; no secrets accessed in this plan's steps |
| T-02-03 | Denial of Service | prove-video.mjs Chromium download | accept | Chromium download only triggered on local prove-video run; CI does not include this step; ensureBrowser() uses Remotion's own verified binary source |
| T-02-SC | Tampering | npm/pip installs | mitigate | No new packages installed in this plan — files are authored locally; no install risk |
</threat_model>

<verification>
After both tasks complete:
1. `node packages/render-core/scripts/audit-deps.mjs` exits 0 (src/ is empty stubs — no forbidden imports)
2. `node packages/render-core/scripts/prove-image.mjs` exits 0 with "PENDING" message
3. `node packages/render-core/scripts/prove-video.mjs` exits 0 with "PENDING" message
4. `ls .github/workflows/render-core-ci.yml` succeeds
5. `grep -c "macos-14\|ubuntu-22.04" .github/workflows/render-core-ci.yml` returns 2
6. `npx vitest run packages/render-core` reports test suite (fonts.test.ts may fail — expected until Plan 03)
</verification>

<success_criteria>
- All 5 SC proof commands exist and are runnable
- audit-deps.mjs is functional and exits 0 on clean src/
- CI matrix covers both target platforms
- Proof scripts exit 0 in "pending" state (not error) — executor can run full suite at any point
</success_criteria>

<output>
Create `.planning/phases/01-render-core-extraction/01-02-SUMMARY.md` when done
</output>
