---
phase: 06-local-video-render
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - packages/workspace/src/types.ts
  - packages/workspace/src/api.ts
  - packages/workspace/src/hooks/useVideoRender.ts
  - packages/workspace/src/hooks/__tests__/useVideoRender.test.ts
autonomous: true
requirements:
  - RND-02
  - RND-04

must_haves:
  truths:
    - "Workspace types include VideoRenderPhase and VideoRenderStatusResponse"
    - "api.ts exports triggerVideoRender(draftId, format) and pollVideoRenderStatus(id)"
    - "useVideoRender hook transitions through flushing → chrome-download → rendering → done"
    - "useVideoRender hook exposes framesRendered, totalFrames, downloadPct, url, error"
    - "Poll stops when phase is 'done' or 'failed'"
    - "trigger() is a no-op if phase is already flushing/chrome-download/rendering"
  artifacts:
    - path: "packages/workspace/src/types.ts"
      provides: "VideoRenderPhase union type + VideoRenderStatusResponse interface"
      contains: "VideoRenderPhase"
    - path: "packages/workspace/src/api.ts"
      provides: "triggerVideoRender + pollVideoRenderStatus exported functions"
      exports: ["triggerVideoRender", "pollVideoRenderStatus"]
    - path: "packages/workspace/src/hooks/useVideoRender.ts"
      provides: "useVideoRender hook with full state machine"
      exports: ["useVideoRender", "UseVideoRenderResult"]
    - path: "packages/workspace/src/hooks/__tests__/useVideoRender.test.ts"
      provides: "Unit tests for all VideoRenderPhase transitions"
  key_links:
    - from: "packages/workspace/src/hooks/useVideoRender.ts"
      to: "packages/workspace/src/api.ts"
      via: "import { triggerVideoRender, pollVideoRenderStatus }"
      pattern: "triggerVideoRender|pollVideoRenderStatus"
    - from: "packages/workspace/src/hooks/useVideoRender.ts"
      to: "packages/workspace/src/types.ts"
      via: "import type { VideoRenderPhase, VideoRenderStatusResponse, FormatKey }"
      pattern: "VideoRenderPhase"
---

<objective>
Build the Workspace SPA contracts and hook for video render: the VideoRenderPhase type + VideoRenderStatusResponse interface in types.ts, the two API functions in api.ts, and the useVideoRender hook that manages the full state machine (flushing → chrome-download → rendering → done/failed) with 1-second polling.

Purpose: Plan 04 (Editor + RenderPanel wiring) depends on useVideoRender being importable and tested before it wires it into the UI.
Output: types.ts extended, api.ts extended, useVideoRender.ts (new hook), unit tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-local-video-render/06-CONTEXT.md
@.planning/phases/06-local-video-render/06-RESEARCH.md
@.planning/phases/06-local-video-render/06-PATTERNS.md

<interfaces>
<!-- Existing types.ts (lines 1-97) — extend after RenderStatusResponse -->
From packages/workspace/src/types.ts:
```typescript
// Already present:
export type DraftOutput = "image" | "video";
export type FormatKey = "landscape" | "square" | "portrait";
export interface DraftConfig {
  output: DraftOutput;
  format?: FormatKey;
  ...
}
export interface RenderStatusResponse {
  id: string;
  formats: Record<"landscape" | "square" | "portrait", FormatRenderState>;
}
// Add after RenderStatusResponse (new for Phase 6):
export type VideoRenderPhase =
  | "idle" | "flushing" | "chrome-download" | "rendering" | "done" | "failed";
export interface VideoRenderStatusResponse {
  id: string;
  phase: "pending" | "chrome-download" | "rendering" | "done" | "failed";
  framesRendered: number;
  totalFrames: number;
  downloadPct: number;
  url?: string;
  error?: string;
}
```

<!-- Existing api.ts — mirror triggerRender/pollRenderStatus pattern (lines 55-67) -->
From packages/workspace/src/api.ts:
```typescript
// Existing requestJson helper (private, lines 10-14):
async function requestJson<T>(url: `/api/${string}`, init?: RequestInit): Promise<T>

// Existing pattern to mirror:
export async function triggerRender(draftId: string): Promise<{ id: string; status: "pending" }> {
  return requestJson<{ id: string; status: "pending" }>("/api/local/render", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ draftId }),
  });
}
export async function pollRenderStatus(id: string): Promise<RenderStatusResponse> {
  return requestJson<RenderStatusResponse>(
    `/api/local/render/${encodeURIComponent(id)}/status` as `/api/${string}`,
  );
}
```

<!-- useRender hook to mirror exactly (packages/workspace/src/hooks/useRender.ts) -->
Key patterns:
- const POLL_INTERVAL_MS = 1000;
- renderPhaseRef kept in sync via useEffect to avoid stale closure in trigger callback
- trigger() guard: checks renderPhaseRef.current for in-progress states before proceeding
- Poll useEffect depends only on [jobId]; starts a setInterval + fires immediately
- clearInterval on terminal phase OR on fetch error
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types.ts and api.ts with video render contracts</name>
  <files>packages/workspace/src/types.ts, packages/workspace/src/api.ts</files>
  <read_first>
    - packages/workspace/src/types.ts — read the entire file; add new types after RenderStatusResponse (line 92); do not modify any existing type
    - packages/workspace/src/api.ts — read the entire file; add new functions after revealOutputFolder; use the existing requestJson helper (do not duplicate it)
  </read_first>
  <action>
    TYPES.TS — add after the RenderStatusResponse interface (after line 92), before the EditorView type:

    VideoRenderPhase union:
      export type VideoRenderPhase = "idle" | "flushing" | "chrome-download" | "rendering" | "done" | "failed";

    VideoRenderStatusResponse interface:
      export interface VideoRenderStatusResponse {
        id: string;
        phase: "pending" | "chrome-download" | "rendering" | "done" | "failed";
        framesRendered: number;
        totalFrames: number;
        downloadPct: number;
        url?: string;
        error?: string;
      }

    No other changes to types.ts.

    API.TS — add two exported functions after revealOutputFolder (at the end of the file):
    Add VideoRenderStatusResponse to the import list from "./types" at the top.

    triggerVideoRender: accepts draftId:string and format:FormatKey. Posts to "/api/local/render/video" with JSON body { draftId, format }. Returns Promise<{ id: string; status: "pending" }>.

    pollVideoRenderStatus: accepts id:string. Gets from "/api/local/render/video/${encodeURIComponent(id)}/status" cast as `/api/${string}`. Returns Promise<VideoRenderStatusResponse>.

    Import FormatKey from "./types" (it is already declared there — add it to the existing import list).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project packages/workspace/tsconfig.json 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <done>
    packages/workspace/src/types.ts exports VideoRenderPhase and VideoRenderStatusResponse.
    packages/workspace/src/api.ts exports triggerVideoRender and pollVideoRenderStatus.
    grep "VideoRenderPhase" packages/workspace/src/types.ts returns the union type.
    grep "triggerVideoRender" packages/workspace/src/api.ts returns the exported function.
    TypeScript compilation passes with zero errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement useVideoRender hook and unit tests</name>
  <files>packages/workspace/src/hooks/useVideoRender.ts, packages/workspace/src/hooks/__tests__/useVideoRender.test.ts</files>
  <read_first>
    - packages/workspace/src/hooks/useRender.ts — read entire file; useVideoRender mirrors this exactly; copy the renderPhaseRef pattern, trigger guard, poll useEffect, and POLL_INTERVAL_MS
    - packages/workspace/src/types.ts — after Task 1; VideoRenderPhase, VideoRenderStatusResponse, FormatKey
    - packages/workspace/src/api.ts — after Task 1; triggerVideoRender, pollVideoRenderStatus signatures
    - packages/workspace/src/hooks/__tests__/ — check for existing hook test patterns (e.g. useRender.test.ts); mirror the testing approach (vi.mock api, @testing-library/react renderHook, fake timers)
  </read_first>
  <behavior>
    - Test 1: initial state — renderPhase "idle", framesRendered 0, totalFrames 0, downloadPct 0, url null, error null, jobId null
    - Test 2: trigger() → sets phase to "flushing" → flush resolves with draftId → triggerVideoRender called → phase becomes "rendering" (from pending poll response), jobId set
    - Test 3: trigger() no-op when phase is "rendering" (guard check via renderPhaseRef)
    - Test 4: poll response with phase:"chrome-download", downloadPct:42 → renderPhase becomes "chrome-download", downloadPct becomes 42
    - Test 5: poll response with phase:"rendering", framesRendered:100, totalFrames:240 → renderPhase "rendering", framesRendered 100, totalFrames 240
    - Test 6: poll response with phase:"done", url:"/output/d1/landscape.mp4" → renderPhase "done", url set, polling stops
    - Test 7: poll response with phase:"failed", error:"encode error" → renderPhase "failed", error set, polling stops
    - Test 8: flush() returning null → renderPhase "failed", error "Save failed before render"
  </behavior>
  <action>
    CREATE packages/workspace/src/hooks/useVideoRender.ts:

    Imports: useCallback, useEffect, useRef, useState from "react";
    Import triggerVideoRender, pollVideoRenderStatus from "../api";
    Import type FormatKey, VideoRenderPhase, VideoRenderStatusResponse from "../types";

    Constant: const POLL_INTERVAL_MS = 1000;

    Export interfaces:
      UseVideoRenderArgs: { flush: () => Promise<string | null>; activeFormat: FormatKey; }
      UseVideoRenderResult: { renderPhase: VideoRenderPhase; framesRendered: number; totalFrames: number; downloadPct: number; url: string | null; jobId: string | null; error: string | null; trigger: () => Promise<void>; }

    Hook body — mirrors useRender.ts structure exactly:
    - useState for renderPhase ("idle"), framesRendered (0), totalFrames (0), downloadPct (0), url (null), jobId (null), error (null)
    - renderPhaseRef = useRef<VideoRenderPhase>("idle"); kept in sync via useEffect
    - trigger callback: guard against flushing/chrome-download/rendering via renderPhaseRef.current; set "flushing"; await flush(); on null → set "failed" + error; on draftId → try triggerVideoRender(draftId, activeFormat) → setJobId(job.id), setRenderPhase("rendering"); catch → set "failed" + error
    - poll useEffect: depends on [jobId]; if null return; setInterval 1000ms + immediate pollTick; pollTick calls pollVideoRenderStatus(jobId), maps all response fields to state: setFramesRendered, setTotalFrames, setDownloadPct; maps response.phase to renderPhase (response.phase "pending" → keep "rendering" since we set it in trigger; otherwise map directly); if response.url → setUrl; if response.phase === "done" or "failed" → clearInterval, if response.error → setError; catch → clearInterval, set "failed" + error message
    - Return all state + trigger

    Note on phase mapping in poll: the CLI response.phase can be "pending" briefly after 202 returns. Map "pending" → do not change renderPhase from "rendering" (keep it). Map all other phases directly to setRenderPhase(response.phase). The "chrome-download" → "rendering" transition is natural as Remotion progresses.

    CREATE packages/workspace/src/hooks/__tests__/useVideoRender.test.ts:
    Use @testing-library/react renderHook and act. Use vi.mock("../api") to mock triggerVideoRender and pollVideoRenderStatus. Use vi.useFakeTimers() to control the 1000ms poll interval. Test all 8 behaviors above. After each timer advance, use await act(async () => { vi.advanceTimersByTime(1000); }) to flush the poll interval and state updates.
  </action>
  <verify>
    <automated>npx vitest run packages/workspace/src/hooks/__tests__/useVideoRender.test.ts</automated>
  </verify>
  <done>
    packages/workspace/src/hooks/useVideoRender.ts exports useVideoRender and UseVideoRenderResult.
    All 8 unit tests pass.
    renderPhaseRef pattern matches useRender.ts (no stale closure bugs).
    Poll clears interval on terminal phases (done/failed) and on fetch error.
    TypeScript compilation passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → GET /api/local/render/video/:id/status | Response data from CLI poll is trusted (same localhost); no XSS vector in types as-is |
| SPA → POST /api/local/render/video | draftId from Workspace state, format from DraftConfig — both locally managed, no user-controllable injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-WS | Information Disclosure | VideoRenderStatusResponse.error field rendered in UI | accept | Error messages come from the CLI process (trusted); no PII; error strings should not be shown verbatim to end users in production but this is a local dev tool |
| T-06-02-WS | Tampering | pollVideoRenderStatus response parsed without schema validation | accept | Poll endpoint is localhost-only (originLockMiddleware); spoofing requires local code execution already |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. npx vitest run packages/workspace/src/hooks/__tests__/useVideoRender.test.ts — all 8 tests green
2. npx tsc --noEmit (workspace package) — zero errors
3. grep "VideoRenderPhase" packages/workspace/src/types.ts — returns union type export
4. grep "triggerVideoRender\|pollVideoRenderStatus" packages/workspace/src/api.ts — both functions present
5. grep "useVideoRender" packages/workspace/src/hooks/useVideoRender.ts — confirms hook export
6. grep "POLL_INTERVAL_MS" packages/workspace/src/hooks/useVideoRender.ts — confirms 1000 constant
</verification>

<success_criteria>
VideoRenderPhase and VideoRenderStatusResponse exported from types.ts.
triggerVideoRender and pollVideoRenderStatus exported from api.ts.
useVideoRender hook implements the full phase state machine with renderPhaseRef guard, 1000ms polling, and terminal phase cleanup.
All unit tests pass.
TypeScript compilation clean.
</success_criteria>

<output>
Create .planning/phases/06-local-video-render/06-03-SUMMARY.md when done.
</output>
