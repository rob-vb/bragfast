---
phase: 06-local-video-render
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - packages/workspace/src/pages/Editor.tsx
  - packages/workspace/src/components/RenderPanel.tsx
autonomous: false
requirements:
  - RND-02
  - RND-04

must_haves:
  truths:
    - "User sees an image|video output toggle in the editor that persists to DraftConfig.output"
    - "Clicking Render with output='video' triggers useVideoRender.trigger() not useRender.trigger()"
    - "Clicking Render with output='image' triggers the existing all-3 image render path unchanged"
    - "Chrome-download gate (progress bar, heading, auto-proceed note) appears only during 'chrome-download' phase"
    - "Frame counter (NNN / 240 frames in Geist Mono) appears only during 'rendering' video phase"
    - "Rendered video is previewable via <video muted controls> in the Workspace panel"
    - "Download video anchor is present when video render is done"
    - "No hasVideo/videoUrl branch forces fade-in (D-09 honored)"
    - "All new interactive elements have min-h-[44px] and focus-visible lime outline"
  artifacts:
    - path: "packages/workspace/src/pages/Editor.tsx"
      provides: "OutputToggle + useVideoRender + video render branching in RenderPanel invocation"
      contains: "useVideoRender"
    - path: "packages/workspace/src/components/RenderPanel.tsx"
      provides: "output prop, chrome-download gate, frame progress row, <video> preview, video download"
      contains: "chrome-download"
  key_links:
    - from: "packages/workspace/src/pages/Editor.tsx"
      to: "packages/workspace/src/hooks/useVideoRender.ts"
      via: "import { useVideoRender } from '../hooks/useVideoRender'"
      pattern: "useVideoRender"
    - from: "packages/workspace/src/pages/Editor.tsx"
      to: "packages/workspace/src/components/RenderPanel.tsx"
      via: "output={output} videoRenderPhase={videoRender.renderPhase} ..."
      pattern: "videoRenderPhase"
    - from: "packages/workspace/src/components/RenderPanel.tsx"
      to: "CLI /output static route"
      via: "<video src={videoUrl}> served from /output/<draftId>/<format>.mp4"
      pattern: "video.*src.*url"
---

<objective>
Wire the output toggle and video render path into the Editor, and extend RenderPanel to display the Chrome-download gate, frame progress, video preview, and Download video action. This completes the full user-facing Phase 6 feature and ends with a human verification checkpoint.

Purpose: All prior plans build plumbing; this plan makes the feature visible and usable in the Workspace.
Output: Editor.tsx with OutputToggle + video path; RenderPanel.tsx with all video states; human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-local-video-render/06-CONTEXT.md
@.planning/phases/06-local-video-render/06-UI-SPEC.md
@.planning/phases/06-local-video-render/06-PATTERNS.md

<interfaces>
<!-- Current RenderPanelProps (packages/workspace/src/components/RenderPanel.tsx) -->
From existing RenderPanel.tsx (approx lines 7-15):
```typescript
interface RenderPanelProps {
  renderPhase: RenderPhase;
  formats: Record<"landscape" | "square" | "portrait", FormatRenderState>;
  jobId: string | null;
  caption: string;
  activeFormat: FormatKey;
  onTrigger: () => Promise<void>;
  onReveal: () => void;
}
```
New props to add:
```typescript
  output: DraftOutput;
  videoRenderPhase?: VideoRenderPhase;
  framesRendered?: number;
  totalFrames?: number;
  downloadPct?: number;
  videoUrl?: string | null;
  onVideoTrigger?: () => Promise<void>;
```

<!-- Current Editor.tsx structure (packages/workspace/src/pages/Editor.tsx) -->
Key existing elements:
- useRender hook at approx line 48: const render = useRender({ flush: save.flush });
- activeFormat derived from config.format ?? "landscape"
- RenderPanel invocation in right column (approx lines 109-119)
- FormatSwitcher pattern at approx lines 14-47 of FormatSwitcher.tsx — OutputToggle uses identical DOM structure with grid-cols-2 and DraftOutput values

<!-- OutputToggle DOM contract (from 06-PATTERNS.md + 06-UI-SPEC.md) -->
Container: role="tablist" aria-label="Output type"
  className="grid w-full max-w-[420px] grid-cols-2 rounded-[8px] border border-[var(--workspace-border)] bg-[var(--workspace-surface)] p-1"
Segment: role="tab" aria-selected={active} type="button"
  min-h-[40px] (touch-safe; 40px is within the panel — the 44px rule applies to primary action buttons)
  focus-visible lime outline
  Active: bg-[rgba(111,143,136,0.16)] text-[var(--workspace-forest)] + lime underline indicator
  Inactive: text-[var(--workspace-muted)] hover:text-[var(--workspace-forest)]
  onClick: calls updateConfig({ ...config, output: opt }) where opt is "image" or "video"
Labels: "Image" and "Video" (from UI-SPEC copywriting contract)

<!-- Chrome-download gate DOM contract (from 06-PATTERNS.md + 06-UI-SPEC.md) -->
Shown when output==="video" && videoRenderPhase==="chrome-download".
  Heading 12px semibold: "One-time setup"
  Body 12px regular: "Downloading Chrome renderer (~170 MB). This only happens once."
  Progress bar: role="progressbar" aria-valuenow={downloadPct??0} aria-valuemin={0} aria-valuemax={100}
    aria-label="Chrome download progress"
    Track: h-2 w-full rounded-full bg-[var(--workspace-border)]
    Fill: h-2 rounded-full bg-[var(--workspace-lime)] style={{width:`${downloadPct??0}%`}}
  Note: "{downloadPct??0}% — Render will start automatically."

<!-- Frame progress row DOM contract (from 06-PATTERNS.md + 06-UI-SPEC.md) -->
Shown when output==="video" && videoRenderPhase==="rendering".
  Spinner: role="status" aria-label="Rendering video"
    className="h-4 w-4 rounded-full border-2 border-[var(--workspace-border)] border-t-[var(--workspace-forest)] animate-spin"
  Label: 12px semibold "Rendering video…"
  Counter: aria-live="polite" font-[Geist_Mono,monospace] text-[12px]
    "{framesRendered??0} / {totalFrames??0} frames"

<!-- Video preview and actions DOM contract (from 06-PATTERNS.md + 06-UI-SPEC.md) -->
Shown when output==="video" && videoRenderPhase==="done" && videoUrl truthy.
  <video src={videoUrl} muted controls aria-label="Rendered video preview"
    className="mt-4 w-full rounded-[8px] border border-[var(--workspace-border)]" />
  Download: <a href={videoUrl} download={`${activeFormat}.mp4`}
    aria-label="Download rendered video" className={actionClassName()}>Download video</a>
  "Open folder" and "Copy caption" actions reuse existing Phase 5 implementations.

<!-- D-09 constraint: DO NOT add any branch on hasVideo or videoUrl to change animation/entrance -->
The composition resolvePreset() does not branch on videoUrl. Do not add such a branch in
Editor.tsx, RenderPanel.tsx, or any component. The showcase entrance applies to all visuals.

<!-- Empty state for video output without video in slot -->
When output==="video" and no video has been rendered yet (phase==="idle"):
Show "Drop a video into the visual slot first." in place of or below the Render button.
The Render button should still be present and clickable (the CLI will error gracefully if
no video is in the slot — the empty state is a UX hint, not a hard block).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RenderPanel with output prop and all video states</name>
  <files>packages/workspace/src/components/RenderPanel.tsx</files>
  <read_first>
    - packages/workspace/src/components/RenderPanel.tsx — read the ENTIRE file before editing; understand all existing props, state, and render logic; note the actionClassName() helper, the spinner CSS class from FormatStatusRow, the aria-live wrapper, and where to add new sections
    - packages/workspace/src/types.ts — after Plan 03; confirm DraftOutput, VideoRenderPhase imports
    - packages/workspace/src/components/FormatSwitcher.tsx — note the lime active underline indicator pattern; RenderPanel does not use FormatSwitcher but borrows its visual pattern for the video output indicator
    - 06-UI-SPEC.md — the copywriting contract (exact strings) and color tokens; no deviation from spec
  </read_first>
  <action>
    Read RenderPanel.tsx in full first. Then apply these additions:

    1. IMPORTS: Add DraftOutput and VideoRenderPhase to the import from "../types".

    2. PROPS: Extend RenderPanelProps interface with 7 new optional fields:
       output: DraftOutput (required, not optional — every caller passes it)
       videoRenderPhase?: VideoRenderPhase
       framesRendered?: number
       totalFrames?: number
       downloadPct?: number
       videoUrl?: string | null
       onVideoTrigger?: () => Promise<void>

    3. RENDER BUTTON BRANCHING: The existing Render button logic stays for output==="image". Add a conditional: when output==="video", render a separate button labeled "Render video" (idle) or "Saving…" (flushing) or "Retry render" (failed). The video button calls onVideoTrigger(). Both buttons share the same min-h-[44px] className and lime background. The video button is hidden (display:none or conditional null) when videoRenderPhase is "chrome-download" or "rendering" (download gate or frame progress takes its place).

    4. CHROME-DOWNLOAD GATE: Add after the button section — shown when output==="video" && videoRenderPhase==="chrome-download". Full DOM per the interfaces block above including role="progressbar", aria-valuenow, lime fill progress bar, and "Render will start automatically." note.

    5. FRAME PROGRESS ROW: Add after chrome-download gate — shown when output==="video" && videoRenderPhase==="rendering". Spinner + "Rendering video…" label + Geist Mono frame counter per interfaces block.

    6. VIDEO PREVIEW + ACTIONS: Add after the existing image preview section — shown when output==="video" && videoRenderPhase==="done" && videoUrl truthy:
       - <video> element per the interfaces block (muted, controls, aria-label, rounded)
       - Download video anchor with actionClassName() styling
       - "Open folder" calls onReveal() — same as image path
       - "Copy caption" — same as image path (reuse existing Phase 5 implementation)

    7. EMPTY STATE: When output==="video" && videoRenderPhase==="idle", show a 12px muted hint below the Render button: "Drop a video into the visual slot first."

    8. Do NOT modify any existing image-path logic. All output==="image" code paths remain exactly as they were. Add output==="video" branches as new conditional blocks.

    Design constraints (from 06-UI-SPEC.md):
    - All interactive elements: min-h-[44px] (buttons and download anchors)
    - All focus-visible: outline-2 outline-offset-2 outline-[var(--workspace-lime)]
    - Frame counter: font-[Geist_Mono,monospace] text-[12px] aria-live="polite"
    - Progress bar fill: bg-[var(--workspace-lime)]
    - No new CSS variables — only existing --workspace-* tokens
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    RenderPanel.tsx has output: DraftOutput prop (required).
    When output==="video": chrome-download gate renders with progressbar ARIA; frame counter uses Geist Mono with aria-live="polite"; video preview is a <video muted controls> element; Download video is an anchor with download attribute; all strings match the 06-UI-SPEC copywriting contract exactly.
    When output==="image": all existing behavior unchanged.
    TypeScript compilation passes with zero errors.
    grep "chrome-download" packages/workspace/src/components/RenderPanel.tsx returns the gate condition.
    grep "Geist_Mono" packages/workspace/src/components/RenderPanel.tsx returns the frame counter.
    No "hasVideo" or "videoUrl" branch in the animation/entrance logic (D-09).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add OutputToggle and useVideoRender to Editor.tsx</name>
  <files>packages/workspace/src/pages/Editor.tsx</files>
  <read_first>
    - packages/workspace/src/pages/Editor.tsx — read the ENTIRE file; understand existing imports, hook instantiation order, canvas preview area, RenderPanel invocation, and updateConfig pattern
    - packages/workspace/src/hooks/useVideoRender.ts — after Plan 03; UseVideoRenderResult shape and trigger signature
    - packages/workspace/src/components/FormatSwitcher.tsx — note the tablist/tab/aria-selected pattern; OutputToggle duplicates it with grid-cols-2 and DraftOutput values
    - packages/workspace/src/components/RenderPanel.tsx — after Task 1; all new props that must be passed
    - 06-UI-SPEC.md layout contract — OutputToggle sits between SlotPanel and RenderPanel in the right column
  </read_first>
  <action>
    Read Editor.tsx in full first. Then apply these additions:

    1. IMPORTS: Add useVideoRender from "../hooks/useVideoRender". Add DraftOutput type from "../types" if not already imported. Add VideoRenderPhase from "../types".

    2. DERIVED VALUE: const output = (config.output ?? "image") as DraftOutput; — read from DraftConfig. No new useState. DraftConfig.output is the source of truth.

    3. ACTIVE FORMAT: activeFormat is already derived from config.format. Confirm it is in scope as FormatKey before instantiating useVideoRender.

    4. HOOK INSTANTIATION: After the existing useRender line, add:
       const videoRender = useVideoRender({ flush: save.flush, activeFormat });
       (save.flush is the same flush function passed to useRender.)

    5. OUTPUT TOGGLE: In the right column, between SlotPanel and RenderPanel, insert the OutputToggle. It is an inline element — do NOT extract it to a separate component file. DOM per the interfaces block:
       - role="tablist" aria-label="Output type"
       - className matching FormatSwitcher container (grid-cols-2, rounded-[8px], border, surface bg, p-1)
       - Two segments mapping ["image","video"] as DraftOutput[]
       - Each segment: role="tab" aria-selected={output===opt} type="button"
       - onClick: updateConfig({ ...config, output: opt }) — persists to DraftConfig.output via the existing auto-save debounce
       - Labels: "Image" and "Video"
       - Active segment: bg-[rgba(111,143,136,0.16)] text-[var(--workspace-forest)] + lime underline indicator (same pattern as FormatSwitcher)
       - Inactive: text-[var(--workspace-muted)] hover:text-[var(--workspace-forest)]
       - All segments: min-h-[40px] (40px is acceptable within a toggle; 44px applies to primary action buttons), focus-visible lime outline

    6. CANVAS PREVIEW AREA: Extend the canvas preview section to show <video> when video render is done:
       When output==="video" && videoRender.renderPhase==="done" && videoRender.url:
         show <video src={videoRender.url} muted controls aria-label={`${activeFormat} render`} className="w-full" />
       Otherwise: existing TemplatePreview / <img> logic unchanged.
       Important: add the video branch BEFORE the image done branch. Existing image logic remains untouched.

    7. RENDER PANEL INVOCATION: Pass the new video props to RenderPanel:
       output={output}
       videoRenderPhase={videoRender.renderPhase}
       framesRendered={videoRender.framesRendered}
       totalFrames={videoRender.totalFrames}
       downloadPct={videoRender.downloadPct}
       videoUrl={videoRender.url}
       onVideoTrigger={videoRender.trigger}
       Keep all existing props unchanged. The onTrigger prop: pass render.trigger for both image and video — RenderPanel now branches internally on output, so the button that fires onTrigger is the image button. The video button fires onVideoTrigger.

    8. REVEAL: The onReveal prop: when output==="video", pass () => { if (videoRender.jobId) void revealOutputFolder(videoRender.jobId); }; when output==="image", pass the existing image jobId reveal. Simplest approach: pass a unified onReveal that checks output:
       onReveal={() => {
         const id = output === "video" ? videoRender.jobId : render.jobId;
         if (id) void revealOutputFolder(id);
       }}

    CONSTRAINT — D-09: The canvas preview and RenderPanel must NOT contain any branch of the form "if (hasVideo || videoUrl) { entrance = 'fade-in' }". The Remotion composition handles all visual entrances via resolvePreset() without branching on videoUrl. Do not touch animation/entrance logic anywhere in this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Editor.tsx imports useVideoRender and instantiates it with { flush: save.flush, activeFormat }.
    OutputToggle renders between SlotPanel and RenderPanel with role="tablist" and two role="tab" segments.
    OutputToggle onClick calls updateConfig({ ...config, output: opt }).
    RenderPanel receives output, videoRenderPhase, framesRendered, totalFrames, downloadPct, videoUrl, onVideoTrigger props.
    Canvas preview area shows <video> element when output==="video" && videoRender.renderPhase==="done".
    No hasVideo/videoUrl animation branch anywhere (D-09).
    TypeScript compilation passes with zero errors.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Human verification: video render end-to-end flow</name>
  <what-built>
    Full Phase 6 video render flow: output toggle in Editor, async CLI render with Chrome-download gate, frame progress in terminal and Workspace panel, .mp4 written to brag-output/, and video preview served from /output/ route.
  </what-built>
  <how-to-verify>
    Run the full test suite first:
      npx vitest run

    Then start the Workspace:
      brag  (or: npm run dev from packages/cli)

    In the Workspace:
    1. Open or create a Draft. Drag a video file (any .mp4 or .mov) into a visual Slot. Confirm the slot shows a video preview (VisualField existing behavior).
    2. Click the "Video" segment in the new Output toggle. Confirm the toggle indicator moves to "Video" and the Render button label changes to "Render video".
    3. Click "Render video".
    4. First-run scenario (if Chromium not cached): observe the Workspace panel showing "One-time setup" heading, "Downloading Chrome renderer (~170 MB)…" body, and a lime progress bar incrementing from 0% to 100%. Confirm the terminal also shows "[brag] Chrome download: NN%" lines. After 100%, the download gate disappears and render begins automatically.
    5. During render: observe the panel showing "Rendering video…" spinner + "NNN / 240 frames" counter in Geist Mono updating every second. Confirm the terminal shows "[brag] Video: NNN/240 frames" lines overwriting in place.
    6. After render completes: observe a <video> element appearing in the panel with playback controls. Confirm the video plays correctly (no audio expected — muted per D-08).
    7. Confirm ./brag-output/<draftId>/landscape.mp4 (or square/portrait, whichever format was active) exists and has non-zero size.
    8. Click "Download video" — file saves to Downloads.
    9. Click "Open folder" — file manager opens to the brag-output folder.
    10. Click "Image" in the output toggle. Confirm the Render button reverts to "Render images" and the existing Phase 5 image render path is accessible (do not re-render unless you want to — just confirm the UI state is correct).

    Expected terminal output:
      [brag] Chrome download: 0%
      [brag] Chrome download: ... 100%
      [brag] Video: 1/240 frames
      [brag] Video: 240/240 frames
      [brag] Video rendered: landscape.mp4
  </how-to-verify>
  <resume-signal>
    Type "approved" if all behaviors above work correctly.
    Or describe any issues observed (e.g. "Chrome gate didn't appear", "frame counter stuck at 0/0", "video preview blank").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → CLI /output static route | <video src> fetches from /output/<id>/<format>.mp4; browser is same-origin (localhost) |
| Editor.tsx updateConfig → DraftConfig.output | User-controlled toggle writes to local DraftConfig state; persisted via auto-save PATCH to authenticated backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-UI | Information Disclosure | <video src={videoUrl}> served from /output | accept | localhost-only; originLockMiddleware on all CLI routes; video is user's own content |
| T-06-02-UI | Tampering | DraftConfig.output persisted to backend via PATCH | accept | Same PATCH endpoint as all other DraftConfig fields; no new surface; backend validates via session auth |
| T-06-03-UI | Spoofing | OutputToggle segments call updateConfig with literal "image"/"video" values | accept | Values are hardcoded literals from the DraftOutput type union; no user text input involved |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. npx vitest run — full suite green before checkpoint
2. npx tsc --noEmit — zero errors
3. grep "useVideoRender" packages/workspace/src/pages/Editor.tsx — confirms hook import and instantiation
4. grep "tablist" packages/workspace/src/pages/Editor.tsx — confirms OutputToggle aria role
5. grep "chrome-download" packages/workspace/src/components/RenderPanel.tsx — confirms gate condition
6. grep "Geist_Mono" packages/workspace/src/components/RenderPanel.tsx — confirms frame counter font
7. grep -v "^//" packages/workspace/src/pages/Editor.tsx | grep -c "hasVideo\|videoUrl.*fade\|videoUrl.*entrance" — must return 0 (D-09 constraint)
8. MANUAL: drag video into slot → set output=video → click Render → observe chrome gate → observe frame counter → observe <video> preview → confirm MP4 on disk
</verification>

<success_criteria>
Output toggle is visible in Editor between SlotPanel and RenderPanel; clicking "Video" persists to DraftConfig.output.
Video render path uses useVideoRender.trigger(); image render path uses useRender.trigger() — both unchanged from their respective implementations.
Chrome-download gate shows progress bar with aria-valuenow during first-run; auto-proceeds when download completes.
Frame counter updates per second during render using Geist Mono font.
<video> element with controls appears in RenderPanel after render completes; src points to /output/<draftId>/<format>.mp4.
MP4 file exists on disk at brag-output/<draftId>/<format>.mp4.
Human verification checkpoint approved.
</success_criteria>

<output>
Create .planning/phases/06-local-video-render/06-04-SUMMARY.md when done.
</output>
