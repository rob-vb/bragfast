---
phase: 03-cli-local-server-workspace-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/workspace/package.json
  - packages/workspace/index.html
  - packages/workspace/vite.config.ts
  - packages/workspace/tsconfig.json
  - packages/workspace/src/main.tsx
  - packages/workspace/src/App.tsx
  - packages/workspace/src/api.ts
  - packages/workspace/src/types.ts
autonomous: true
requirements:
  - CLI-05
must_haves:
  truths:
    - "packages/workspace exists as a self-contained Vite react-ts SPA package"
    - "The SPA shell renders a minimal page and fetches /api/repo-context using relative URLs"
    - "No Next.js server imports exist anywhere in packages/workspace"
  artifacts:
    - path: "packages/workspace/package.json"
      provides: "Workspace SPA package config (name @bragfast/workspace, no private field — workspace member)"
      contains: "@vitejs/plugin-react"
    - path: "packages/workspace/vite.config.ts"
      provides: "Vite config with base '/', build.outDir 'dist'"
      contains: "outDir"
    - path: "packages/workspace/src/App.tsx"
      provides: "Top-level component: fetches /api/repo-context with relative URL"
      contains: "repo-context"
    - path: "packages/workspace/src/api.ts"
      provides: "Typed fetch helper for /api/repo-context"
      exports: ["fetchRepoContext", "RepoContext"]
  key_links:
    - from: "packages/workspace/src/App.tsx"
      to: "/api/repo-context"
      via: "fetch with relative URL in useEffect"
      pattern: "fetch.*api/repo-context"
---

<objective>
Scaffold the `packages/workspace` Vite React SPA package — the browser-side shell that the CLI Express server will serve in Phase 3.

Purpose: Establish the SPA package before the CLI server is built; separating scaffold from install and wiring keeps Wave 1 autonomous and avoids the blocked-on-install problem. The SPA must prove it compiles and uses relative URLs for all API calls.

Output: A complete `packages/workspace` directory with Vite config, React entry, minimal App shell that fetches `/api/repo-context`, and typed API helper. No `npm install` in this plan — that happens in Plan 03-05 after the package legitimacy checkpoint.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cli-local-server-workspace-shell/03-CONTEXT.md
@.planning/phases/03-cli-local-server-workspace-shell/03-RESEARCH.md
@.planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. No codebase exploration required. -->

From RESEARCH.md Pattern 7 — Vite config:
  defineConfig({ plugins: [react()], base: "/", build: { outDir: "dist", assetsDir: "assets" } })

From PATTERNS.md — SPA api.ts uses relative URLs matching http.ts typed-fetch style:
  export async function fetchRepoContext(): Promise<RepoContext> {
    const response = await fetch("/api/repo-context");
    if (!response.ok) throw new Error(`repo-context failed (${response.status})`);
    return (await response.json()) as RepoContext;
  }

From PATTERNS.md — App.tsx uses useState + useEffect, never hardcodes port:
  const [context, setContext] = useState<RepoContext | null>(null);
  useEffect(() => {
    fetch("/api/repo-context").then(r => r.json()).then(setContext).catch(() => undefined);
  }, []);

From DESIGN.md constraints:
  - Light mode only, zero border-radius, Press Start 2P + Geist fonts, hard-offset box shadows
  - The shell is minimal HTML for Phase 3; full editor comes in Phase 4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/workspace package scaffold</name>
  <files>
    packages/workspace/package.json,
    packages/workspace/index.html,
    packages/workspace/tsconfig.json,
    packages/workspace/vite.config.ts
  </files>
  <read_first>
    - packages/cli/package.json — copy "type": "module", engines, devDependencies style
    - packages/render-core/tsup.config.ts — understand peer package structure in the monorepo
    - .planning/phases/03-cli-local-server-workspace-shell/03-RESEARCH.md — Pattern 7 (vite.config), Standard Stack table (exact versions)
    - .planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md — No Analog Found section (workspace/package.json)
  </read_first>
  <action>
    Create packages/workspace/ as a new npm workspace member.

    packages/workspace/package.json:
    - name: "@bragfast/workspace"
    - version: "0.0.1"
    - private: true
    - type: "module"
    - scripts: { "build": "vite build", "dev": "vite", "preview": "vite preview" }
    - dependencies: { "react": "19.2.3", "react-dom": "19.2.3" } — match root react version
    - devDependencies: { "vite": "8.0.13", "@vitejs/plugin-react": "6.0.2", "typescript": "5.9.3", "@types/react": "^19", "@types/react-dom": "^19" }
    - engines: { "node": ">=20" }

    packages/workspace/index.html:
    Standard Vite react-ts index.html: `<div id="root"></div>` + `<script type="module" src="/src/main.tsx"></script>`. Title: "brag.fast Workspace".

    packages/workspace/tsconfig.json:
    - target: "ES2020", useDefineForClassFields: true
    - lib: ["ES2020", "DOM", "DOM.Iterable"]
    - module: "ESNext", moduleResolution: "bundler"
    - strict: true, skipLibCheck: true, noEmit: true
    - jsx: "react-jsx"

    packages/workspace/vite.config.ts (per RESEARCH.md Pattern 7):
    - plugins: [react()]
    - base: "/"
    - build: { outDir: "dist", assetsDir: "assets" }
    Import react from "@vitejs/plugin-react".
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('packages/workspace/package.json','utf8')); console.assert(p.name==='@bragfast/workspace'); console.assert(p.scripts.build==='vite build'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/workspace/package.json has name "@bragfast/workspace", scripts.build "vite build", devDependencies includes "vite": "8.0.13"
    - packages/workspace/vite.config.ts imports from "@vitejs/plugin-react" and sets base: "/" and build.outDir: "dist"
    - packages/workspace/index.html contains `id="root"` and `src="/src/main.tsx"`
    - packages/workspace/tsconfig.json has "jsx": "react-jsx" and "moduleResolution": "bundler"
  </acceptance_criteria>
  <done>All four scaffold files exist with correct content; package is ready to be added to root workspace</done>
</task>

<task type="auto">
  <name>Task 2: Create SPA source files (types, api, main, App)</name>
  <files>
    packages/workspace/src/types.ts,
    packages/workspace/src/api.ts,
    packages/workspace/src/main.tsx,
    packages/workspace/src/App.tsx
  </files>
  <read_first>
    - packages/cli/src/http.ts — typed-fetch pattern to mirror in api.ts (relative URL variant)
    - packages/cli/src/auth.ts lines 1-5 — .catch(() => undefined) browser-open pattern; apply same to fetch error swallowing
    - .planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md — workspace/src/api.ts section, workspace/src/App.tsx section
    - DESIGN.md — light mode, Press Start 2P + Geist, zero border-radius, hard-offset shadows
  </read_first>
  <action>
    packages/workspace/src/types.ts:
    Export the shared RepoContext interface (mirrors the one in repo-context.ts that Plan 03-04 will create):
      export interface RepoContext { tag: string | null; sha: string | null; name: string | null; version: string | null; }

    packages/workspace/src/api.ts:
    Import RepoContext from "./types". Export fetchRepoContext() — typed fetch helper using relative URL "/api/repo-context". On non-ok response throw Error with status code. Return response.json() cast to RepoContext. Match the http.ts pattern (named export async function, typed return, no default export).

    packages/workspace/src/main.tsx:
    Standard Vite react-ts entry: import StrictMode from "react", createRoot from "react-dom/client", App from "./App". Call createRoot(document.getElementById("root")!).render(<StrictMode><App /></StrictMode>).

    packages/workspace/src/App.tsx:
    - Import useState, useEffect from "react"
    - Import RepoContext from "./types", fetchRepoContext from "./api"
    - State: const [context, setContext] = useState<RepoContext | null>(null)
    - useEffect: call fetchRepoContext().then(setContext).catch(() => undefined) on mount
    - Render: minimal shell — a `<main>` with heading "brag.fast Workspace" (styled inline or with a minimal className per DESIGN.md: font-family Geist/monospace, light background, no border-radius anywhere). Show repo context values (name, version, sha, tag) when non-null, or "Loading..." when null.
    - All fetch calls use relative URL "/api/repo-context" via api.ts — never hardcode "http://127.0.0.1:PORT" in any SPA source file (per CONTEXT.md locked decision + RESEARCH.md anti-pattern).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const api=fs.readFileSync('packages/workspace/src/api.ts','utf8'); console.assert(!api.includes('127.0.0.1'),'Bearer URL found'); console.assert(api.includes('/api/repo-context'),'relative URL missing'); const app=fs.readFileSync('packages/workspace/src/App.tsx','utf8'); console.assert(!app.includes('127.0.0.1'),'hardcoded URL in App'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/workspace/src/types.ts exports RepoContext interface with tag, sha, name, version fields (all string | null)
    - packages/workspace/src/api.ts: fetchRepoContext() uses fetch("/api/repo-context") — no hardcoded host or port
    - packages/workspace/src/App.tsx: no "127.0.0.1" literal anywhere; fetches via fetchRepoContext() from api.ts
    - packages/workspace/src/main.tsx: calls createRoot(document.getElementById("root")!)
    - grep -r "127.0.0.1" packages/workspace/src/ returns no matches
  </acceptance_criteria>
  <done>All four SPA source files exist; no hardcoded localhost URLs; App fetches /api/repo-context via the api.ts typed helper</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA source → CLI Express server | All API calls use relative URLs; the Express server (not the SPA) controls the host/port |
| packages/workspace/dist → CLI process | Built assets are copied server-side; no runtime exposure to Bearer tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | packages/workspace/src/*.ts | mitigate | No hardcoded host/port or Bearer token in any SPA source; enforced by `grep -r "127.0.0.1" packages/workspace/src/` gate in verify |
| T-03-SC | Tampering | npm package installs (vite, @vitejs/plugin-react) | mitigate | slopcheck unavailable — all [ASSUMED]; blocking human checkpoint required in Plan 03-05 before npm install |
</threat_model>

<verification>
After both tasks complete:
- node -e "require('./packages/workspace/src/types.ts')" — TypeScript only; verify via grep: grep -c "RepoContext" packages/workspace/src/types.ts packages/workspace/src/api.ts packages/workspace/src/App.tsx
- grep -r "127.0.0.1" packages/workspace/src/ returns zero lines
- All four scaffold files exist: ls packages/workspace/{package.json,index.html,tsconfig.json,vite.config.ts,src/main.tsx,src/App.tsx,src/api.ts,src/types.ts}
</verification>

<success_criteria>
- packages/workspace/ directory exists with all 8 files
- No hardcoded 127.0.0.1 or port numbers in any SPA source file
- RepoContext type is defined and exported from types.ts, consumed by api.ts and App.tsx
- vite.config.ts has base "/" and build.outDir "dist" (required for CLI's tsup cpSync in Plan 03-05)
</success_criteria>

<output>
Create `.planning/phases/03-cli-local-server-workspace-shell/03-01-SUMMARY.md` when done
</output>
