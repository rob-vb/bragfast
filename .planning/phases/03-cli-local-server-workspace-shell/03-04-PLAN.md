---
phase: 03-cli-local-server-workspace-shell
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - packages/cli/src/repo-context.ts
autonomous: true
requirements:
  - CLI-07
must_haves:
  truths:
    - "getRepoContext(cwd) returns { tag, sha, name, version } from a real git repo without throwing"
    - "getRepoContext() returns all-null fields for a non-git directory — never throws"
    - "No shell interpolation of user-controlled input — git commands use fixed args only"
  artifacts:
    - path: "packages/cli/src/repo-context.ts"
      provides: "getRepoContext(cwd: string): RepoContext"
      exports: ["getRepoContext", "RepoContext"]
  key_links:
    - from: "packages/cli/src/server.ts"
      to: "packages/cli/src/repo-context.ts"
      via: "import { getRepoContext } from './repo-context'"
      pattern: "getRepoContext"
---

<objective>
Implement `packages/cli/src/repo-context.ts` — the utility that reads git context (latest tag, short commit SHA) and package.json (name, version) from the user's current working directory, for prefilling Workspace copy slots. No AI. Handles non-git directories and missing package.json gracefully.

Purpose: Turns the RED repo-context test harness from Plan 03-02 GREEN. Isolated as its own plan because it is a pure utility with no dependency on server.ts or proxy.ts, allowing Wave 2 parallel execution alongside Plan 03-03.

Output: packages/cli/src/repo-context.ts. The module is already imported by server.ts (Plan 03-03 references it) and tested by repo-context.test.ts.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-local-server-workspace-shell/03-CONTEXT.md
@.planning/phases/03-cli-local-server-workspace-shell/03-RESEARCH.md
@.planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md
@.planning/phases/03-cli-local-server-workspace-shell/03-02-SUMMARY.md

<interfaces>
<!-- Exact contracts needed. No codebase exploration required. -->

From PATTERNS.md — repo-context.ts interface (mirrors credentials.ts export style):
  export interface RepoContext {
    tag: string | null;
    sha: string | null;
    name: string | null;
    version: string | null;
  }
  export function getRepoContext(cwd: string): RepoContext

From RESEARCH.md Pattern 5 — implementation (use verbatim):
  const exec = (cmd: string): string | null => {
    try { return execSync(cmd, { cwd, stdio: "pipe" }).toString().trim() || null; }
    catch { return null; }
  };
  const tag = exec("git describe --tags --abbrev=0");
  const sha = exec("git rev-parse --short HEAD");
  // package.json read in separate try/catch

From PATTERNS.md — credentials.ts try/catch pattern:
  Each fallible call gets its own try/catch so a missing tag doesn't suppress a valid SHA.

Security constraint from CONTEXT.md + threat model:
  No shell interpolation of untrusted input — execSync commands use fixed string args only.
  cwd parameter is process.cwd() (controlled by the CLI runtime, not user HTTP input).
  All commands are fully static strings: "git describe --tags --abbrev=0", "git rev-parse --short HEAD".
  Do NOT use template literals or string concatenation inside the git command strings.

From packages/cli/src/credentials.ts — FS import style:
  import { readFileSync } from "fs"  (not "node:fs" — match existing project convention)
  import { execSync } from "child_process"  (not "node:child_process" — match convention)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement packages/cli/src/repo-context.ts</name>
  <files>packages/cli/src/repo-context.ts</files>
  <read_first>
    - packages/cli/src/credentials.ts — full file; copy the try/catch + named import style; copy the interface export pattern exactly
    - .planning/phases/03-cli-local-server-workspace-shell/03-RESEARCH.md — Pattern 5 (full implementation), Pitfall 6 (non-git CWD throws)
    - .planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md — repo-context.ts section (verbatim implementation including the per-command try/catch rationale)
    - packages/cli/src/__tests__/repo-context.test.ts — what the tests assert (4 behaviors: all-null for non-git, sha from real repo, name/version from synthetic package.json, no-throw on execSync error)
  </read_first>
  <action>
    Create packages/cli/src/repo-context.ts as an ESM module.

    Imports:
    - import { execSync } from "child_process"
    - import { readFileSync } from "fs"
    - import path from "path"

    Export interface RepoContext { tag: string | null; sha: string | null; name: string | null; version: string | null; }

    Export function getRepoContext(cwd: string): RepoContext:

    Internal exec helper (not exported): (cmd: string) => string | null
    - try { return execSync(cmd, { cwd, stdio: "pipe" }).toString().trim() || null; }
    - catch { return null; }
    - The empty-string-to-null coercion (|| null) handles "no tags" case where git exits 0 but prints nothing.

    Read tag: exec("git describe --tags --abbrev=0")
    Read sha: exec("git rev-parse --short HEAD")

    Read name/version in a SEPARATE try/catch block (not the same try as tag/sha):
    - readFileSync(path.join(cwd, "package.json"), "utf8")
    - JSON.parse() cast to { name?: string; version?: string }
    - name = pkg.name ?? null, version = pkg.version ?? null
    - On any error: name = null, version = null

    Return { tag, sha, name, version }.

    SECURITY: All exec() command strings are static string literals — no template literals containing cwd or any user input. The cwd value is passed as the options.cwd argument to execSync, which is safe OS-level directory context, not shell interpolation.

    No async/await — function is synchronous. This matches the usage in server.ts route handler which calls getRepoContext(process.cwd()) synchronously.
  </action>
  <verify>
    <automated>npx vitest run packages/cli 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/cli/src/repo-context.ts exists and exports getRepoContext and RepoContext
    - Function is synchronous (no async, no Promise in return type)
    - git command strings are static literals — no template literals: grep "exec(\`" packages/cli/src/repo-context.ts returns 0 lines
    - Each git command wrapped in its own try/catch (tag and sha have separate try/catch from each other and from package.json read)
    - npx vitest run packages/cli — all tests PASS (GREEN)
    - The "non-git directory" test returns { tag: null, sha: null, name: null, version: null } without throwing
  </acceptance_criteria>
  <done>repo-context.ts implemented and all repo-context.test.ts tests green; CLI-07 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.cwd() → execSync cwd option | cwd is passed as options.cwd, not interpolated into the shell command string — no injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering (command injection) | repo-context.ts execSync | mitigate | Git command strings are static literals; cwd is passed as options.cwd (not shell-interpolated). Verified by "grep 'exec(\`'" gate returning 0 lines. |
| T-03-09 | Denial of Service (hang on large repo) | repo-context.ts execSync | accept | stdio: "pipe" prevents output from blocking; git commands are fast (O(1) regardless of repo size); no timeout needed for this use case. |
</threat_model>

<verification>
After task completes:
- npx vitest run packages/cli — all PASS
- grep "exec(\`" packages/cli/src/repo-context.ts — 0 matches (no shell interpolation)
- node -e "const src=require('fs').readFileSync('packages/cli/src/repo-context.ts','utf8'); console.assert(src.includes('stdio: \"pipe\"'),'missing pipe'); console.log('OK')"
</verification>

<success_criteria>
- packages/cli/src/repo-context.ts exists with synchronous getRepoContext(cwd: string): RepoContext
- All 4 repo-context.test.ts tests pass
- No shell interpolation in git commands (static string literals only)
- Returns all-null for non-git directories without throwing
- stdio: "pipe" prevents execSync from inheriting terminal and blocking
</success_criteria>

<output>
Create `.planning/phases/03-cli-local-server-workspace-shell/03-04-SUMMARY.md` when done
</output>
