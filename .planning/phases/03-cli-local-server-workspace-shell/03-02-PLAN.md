---
phase: 03-cli-local-server-workspace-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/__tests__/server.test.ts
  - packages/cli/src/__tests__/origin-lock.test.ts
  - packages/cli/src/__tests__/proxy.test.ts
  - packages/cli/src/__tests__/repo-context.test.ts
autonomous: true
requirements:
  - CLI-05
  - CLI-06
  - CLI-07
  - AUTH-02
must_haves:
  truths:
    - "Test files exist for all four behaviors (server lifecycle, origin-lock, proxy Bearer injection, repo-context)"
    - "Tests are written to fail (RED state) until Plan 03-03/04 implementation exists — this is Wave 0 gap closure"
    - "Test command is: npx vitest run packages/cli"
  artifacts:
    - path: "packages/cli/src/__tests__/server.test.ts"
      provides: "Tests for CLI-05 (URL print), CLI-06 (port conflict), SPA static fallback"
      contains: "startServer"
    - path: "packages/cli/src/__tests__/origin-lock.test.ts"
      provides: "Tests for AUTH-02 (wrong-origin rejection)"
      contains: "originLockMiddleware"
    - path: "packages/cli/src/__tests__/proxy.test.ts"
      provides: "Tests for AUTH-02 (Bearer injected server-side)"
      contains: "createBackendProxy"
    - path: "packages/cli/src/__tests__/repo-context.test.ts"
      provides: "Tests for CLI-07 (git tag/SHA/name/version, non-git graceful)"
      contains: "getRepoContext"
  key_links:
    - from: "packages/cli/src/__tests__/server.test.ts"
      to: "packages/cli/src/server.ts"
      via: "import { startServer } from '../server'"
      pattern: "from.*server"
    - from: "packages/cli/src/__tests__/origin-lock.test.ts"
      to: "packages/cli/src/server.ts"
      via: "import { originLockMiddleware } from '../server'"
      pattern: "originLockMiddleware"
---

<objective>
Write the Wave 0 test harnesses for all four Phase 3 CLI behaviors (CLI-05, CLI-06, CLI-07, AUTH-02) as failing tests in RED state. The implementation modules (server.ts, proxy.ts, repo-context.ts) do not exist yet — Plan 03-03 and 03-04 will implement them; these tests define the contracts first.

Purpose: Validates that Plan 03-03/04 implementations satisfy the right contracts. The vitest config already covers `src/__tests__/**/*.test.ts` so no config changes are needed.

Output: Four test files in packages/cli/src/__tests__/. All tests import from modules that don't exist yet — they will fail with "Cannot find module" until Plans 03-03/04 ship.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-cli-local-server-workspace-shell/03-CONTEXT.md
@.planning/phases/03-cli-local-server-workspace-shell/03-RESEARCH.md
@.planning/phases/03-cli-local-server-workspace-shell/03-VALIDATION.md
@.planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md

<interfaces>
<!-- Key test patterns. Executor does not need to explore the codebase. -->

From packages/cli/src/__tests__/auth.test.ts (exact structure to mirror):
  import { mkdtempSync } from "fs";
  import { rm } from "fs/promises";
  import os from "os";
  import path from "path";
  import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

  let tmp: string;
  beforeEach(() => { tmp = mkdtempSync(path.join(os.tmpdir(), "brag-cli-")); process.env.BRAG_HOME = tmp; });
  afterEach(async () => { delete process.env.BRAG_HOME; await rm(tmp, { recursive: true, force: true }); });

  vi.fn mock pattern:
  const openBrowser = vi.fn(async (_url: string) => undefined);

From RESEARCH.md Validation Scenarios:
  Wrong-origin test:
    const res = await request(app).get("/api/repo-context")
      .set("Origin", "http://evil.com").set("Host", "127.0.0.1:3421");
    expect(res.status).toBe(401);

  Port conflict test:
    const blocker = net.createServer();
    await new Promise(r => blocker.listen(3421, "127.0.0.1", r));
    const { port } = await startServer(credentials);
    expect(port).not.toBe(3421);
    blocker.close();

  Non-git repo context:
    const ctx = getRepoContext(os.tmpdir());
    expect(ctx).toEqual({ tag: null, sha: null, name: null, version: null });

Credentials shape for test fixtures (from packages/cli/src/credentials.ts):
  { api_key: "bf_test_key_1234", created_at: new Date().toISOString() }

From RESEARCH.md — supertest usage:
  import request from "supertest";
  // pass the express app (not the http.Server) to request()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write server lifecycle and port tests (CLI-05, CLI-06)</name>
  <files>packages/cli/src/__tests__/server.test.ts</files>
  <read_first>
    - packages/cli/src/__tests__/auth.test.ts — full file; copy beforeEach/afterEach/vi.fn structure exactly
    - packages/cli/src/credentials.ts — Credentials interface shape for fixture
    - .planning/phases/03-cli-local-server-workspace-shell/03-RESEARCH.md — Validation Scenarios (port conflict test), Pattern 1 (ServerHandle interface)
    - .planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md — server.ts ServerOptions interface
  </read_first>
  <behavior>
    - Test: startServer binds to 127.0.0.1 and returns a port number (number, > 0)
    - Test: startServer writes "Workspace: http://127.0.0.1:<PORT>" to the injected stdout (CLI-05 URL-print)
    - Test: startServer calls openBrowser with "http://127.0.0.1:<PORT>" (CLI-05 browser-open)
    - Test: when default port 3421 is occupied by a net.createServer() blocker, startServer returns a different port (CLI-06)
    - Test: GET / on the started server returns 200 (static SPA fallback — index.html)
    - Test: GET /nonexistent-path returns 200 (SPA router fallback, not 404)
    - Test: { port, close } — calling close() resolves without error
  </behavior>
  <action>
    Import "net" from "node:net", "supertest" from "supertest", startServer from "../server", Credentials from "../credentials".
    Use a stub workspace-dist directory (mkdtempSync with a minimal index.html file) to avoid the real workspace-dist not existing. Pass the stub dir path via a ServerOptions field or environment override — check what the server.ts pattern from PATTERNS.md exposes (spaDir?: string in ServerOptions is the right hook).
    Use injectable openBrowser (vi.fn) and stdout (a mock with .write() vi.fn) from ServerOptions so no real browser opens.
    After each test call handle.close() in afterEach to prevent port leaks.
    Use `import { createServer } from "node:net"` for the port blocker (not "net") to avoid name collision with express createServer.
    Mark tests with describe("server", ...) and nested describe blocks per behavior group.
  </action>
  <verify>
    <automated>AUTOMATED=MISSING — packages/cli/src/server.ts does not exist yet; test file exists but will fail with "Cannot find module '../server'"; confirm with: ls packages/cli/src/__tests__/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/cli/src/__tests__/server.test.ts exists
    - File imports startServer from "../server" (module will exist after Plan 03-03)
    - File contains at least 5 it() blocks covering: port bind, stdout URL print, openBrowser call, port conflict fallback, SPA fallback GET /
    - No import from "../proxy" or "../repo-context" in this file (separation of concerns)
    - grep -c "127.0.0.1" packages/cli/src/__tests__/server.test.ts returns >= 2
  </acceptance_criteria>
  <done>server.test.ts exists with tests for CLI-05 + CLI-06; all tests currently fail with "Cannot find module" (RED state is correct)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write origin-lock and proxy Bearer tests (AUTH-02)</name>
  <files>
    packages/cli/src/__tests__/origin-lock.test.ts,
    packages/cli/src/__tests__/proxy.test.ts
  </files>
  <read_first>
    - packages/cli/src/__tests__/auth.test.ts — vi.fn and describe structure
    - .planning/phases/03-cli-local-server-workspace-shell/03-RESEARCH.md — Validation Scenarios (wrong-origin rejection), Pattern 2 (originLockMiddleware), Pattern 3 (createBackendProxy)
    - .planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md — proxy.ts section (originLockMiddleware exported from server.ts or separate)
  </read_first>
  <behavior>
    origin-lock.test.ts:
    - Test: GET /api/repo-context with Origin "http://evil.com" and correct Host returns 401
    - Test: GET /api/repo-context with Origin "http://evil.com" and wrong Host returns 401
    - Test: GET /api/repo-context with no Origin header and correct Host passes through (not rejected at CORS level)
    - Test: GET /api/repo-context with correct Origin "http://127.0.0.1:<PORT>" and correct Host passes through

    proxy.test.ts:
    - Test: createBackendProxy injects "Authorization: Bearer bf_test_key_1234" into the proxyReq (use a spy/mock on proxyReq.setHeader)
    - Test: the Bearer token does NOT appear in the response headers sent back to the caller (Information Disclosure mitigation)
  </behavior>
  <action>
    origin-lock.test.ts:
    Import supertest, express, and originLockMiddleware from "../server" (or "../origin-lock" if extracted separately — follow what Plan 03-03 exports). Build a minimal express app with originLockMiddleware(PORT) applied, a test route GET /api/repo-context that returns 200 JSON. Use supertest to send requests with varying Origin/Host headers. Check status 401 for rejected requests.

    proxy.test.ts:
    Import createBackendProxy from "../proxy". The proxy middleware wraps http-proxy-middleware, so test the header injection logic in isolation: create a mock proxyReq object with a setHeader spy (vi.fn()), call the internal proxyReq handler with it, and assert setHeader was called with ("Authorization", "Bearer bf_test_key_1234"). Do not make real network calls to the backend — mock the target.
    For the "token not in response" test, create a minimal Express app + supertest; mock http-proxy-middleware to capture the proxyReq headers, assert no Authorization header is in the final response object.
  </action>
  <verify>
    <automated>AUTOMATED=MISSING — modules don't exist yet; confirm files exist: ls packages/cli/src/__tests__/origin-lock.test.ts packages/cli/src/__tests__/proxy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/cli/src/__tests__/origin-lock.test.ts exists and imports originLockMiddleware from "../server"
    - packages/cli/src/__tests__/proxy.test.ts exists and imports createBackendProxy from "../proxy"
    - origin-lock tests include at least one test asserting status 401 for wrong-origin request with `.set("Origin", "http://evil.com")`
    - proxy tests include at least one vi.fn() spy on setHeader asserting ("Authorization", `Bearer bf_test_key_1234`)
    - grep "401" packages/cli/src/__tests__/origin-lock.test.ts returns at least 1 match
  </acceptance_criteria>
  <done>origin-lock.test.ts and proxy.test.ts exist in RED state; both import from modules that Plan 03-03 will create</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write repo-context tests (CLI-07)</name>
  <files>packages/cli/src/__tests__/repo-context.test.ts</files>
  <read_first>
    - packages/cli/src/__tests__/credentials.test.ts — tmpdir + process.env override pattern for FS isolation
    - .planning/phases/03-cli-local-server-workspace-shell/03-RESEARCH.md — Validation Scenarios (non-git CWD), Pattern 5 (getRepoContext return shape)
    - .planning/phases/03-cli-local-server-workspace-shell/03-PATTERNS.md — repo-context.ts section (RepoContext interface, exec try/catch pattern)
  </read_first>
  <behavior>
    - Test: getRepoContext(os.tmpdir()) returns { tag: null, sha: null, name: null, version: null } (non-git directory — CLI-07 graceful fallback)
    - Test: getRepoContext() called on the bragfast repo directory (process.cwd()) returns an object where sha is a non-null string matching /^[0-9a-f]{7}$/ (short SHA) and name is "bragfast"
    - Test: getRepoContext() called on a tmpdir with a package.json containing { name: "test-pkg", version: "1.2.3" } (but no .git) returns { tag: null, sha: null, name: "test-pkg", version: "1.2.3" }
    - Test: getRepoContext() never throws even when git is not in PATH (mock execSync to throw); returns all-null object
  </behavior>
  <action>
    Import getRepoContext from "../repo-context", os from "os", path from "path", writeFileSync + mkdtempSync + rmSync from "fs".
    Use beforeEach/afterEach to create and clean up tmp directories (mirror credentials.test.ts pattern).
    For the "real repo" test, pass process.cwd() which is the bragfast monorepo root — it has a git repo and a package.json with name "bragfast". This is a live filesystem test (not mocked), so it will work in any environment where the code is being developed. Assert sha matches /^[0-9a-f]{7,}$/.
    For the "package.json without git" test, create a tmp dir, write a package.json file into it, call getRepoContext(tmpDir), and assert name/version are populated while tag/sha are null.
    For the "execSync throws" test, vi.mock("child_process", ...) to make execSync throw; assert return value is { tag: null, sha: null, name: null, version: null }.
  </action>
  <verify>
    <automated>AUTOMATED=MISSING — module doesn't exist yet; confirm file exists: ls packages/cli/src/__tests__/repo-context.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/cli/src/__tests__/repo-context.test.ts exists and imports getRepoContext from "../repo-context"
    - File contains at least 4 it() blocks matching the four behaviors above
    - One test passes process.cwd() (real repo with git) and asserts sha is a non-null string
    - One test creates a tmp dir with a synthetic package.json and asserts name/version populated, tag/sha null
    - grep "os.tmpdir()" packages/cli/src/__tests__/repo-context.test.ts returns at least 1 match
  </acceptance_criteria>
  <done>repo-context.test.ts exists in RED state; imports from module that Plan 03-04 will create</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → CLI modules | Test files import from not-yet-created modules; RED state is intentional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Spoofing | origin-lock.test.ts | mitigate | Test explicitly asserts 401 for wrong-origin requests; validates HOST header guard and CORS block both active |
| T-03-03 | Information Disclosure | proxy.test.ts | mitigate | Test asserts Authorization header is set in proxyReq (injected server-side) and does NOT appear in response headers returned to caller |
| T-03-SC | Tampering | supertest install (devDep) | mitigate | supertest is a long-lived DefinitelyTyped package; Plan 03-05 legitimacy checkpoint gates the actual npm install |
</threat_model>

<verification>
After all three tasks complete:
- ls packages/cli/src/__tests__/server.test.ts packages/cli/src/__tests__/origin-lock.test.ts packages/cli/src/__tests__/proxy.test.ts packages/cli/src/__tests__/repo-context.test.ts — all four files exist
- grep -l "Cannot find module" is NOT the verification — vitest will report import errors; that is expected (RED state)
- wc -l packages/cli/src/__tests__/*.test.ts — all files are non-empty
</verification>

<success_criteria>
- All four test files exist
- Imports reference the correct module paths (../server, ../proxy, ../repo-context) that Plans 03-03/04 will create
- origin-lock tests assert 401 for wrong-origin requests
- proxy tests assert Bearer token injected via setHeader spy, not leaked in response
- repo-context tests cover non-git, real-git, package-json-only, and execSync-throws scenarios
</success_criteria>

<output>
Create `.planning/phases/03-cli-local-server-workspace-shell/03-02-SUMMARY.md` when done
</output>
