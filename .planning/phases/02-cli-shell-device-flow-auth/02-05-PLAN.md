---
phase: 02-cli-shell-device-flow-auth
plan: 05
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified:
  - packages/cli/scripts/prove-device-flow.mjs
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/02-cli-shell-device-flow-auth/02-VALIDATION.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - AUTH-01
must_haves:
  truths:
    - full automated phase suite passes
    - planning state records exact npm-name caveat
    - manual e2e instructions are current
---

# Plan 02-05 — Phase Integration + Signoff

## Objective

Run the end-to-end proof suite, fix integration issues, and mark Phase 2 complete or explicitly blocked on npm package ownership.

## Tasks

1. Add `packages/cli/scripts/prove-device-flow.mjs` using mocked/local endpoints where possible.
2. Run:

```bash
npm run build
npm run lint
npx vitest run convex/__tests__/deviceCodes.test.ts
npx vitest run packages/cli
node packages/cli/scripts/prove-device-flow.mjs
```

3. Manually verify `/device?code=...` with a signed-in browser session when a local server is available.
4. Update `02-VALIDATION.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`.

## Acceptance

- All automated checks pass.
- Literal `npx brag` is documented as blocked unless npm package name ownership changes; package is published as `bragfast`, so `npx bragfast` is the public first-run path.
