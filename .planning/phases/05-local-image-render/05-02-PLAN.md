---
phase: 05-local-image-render
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/workspace/src/types.ts
  - packages/workspace/src/api.ts
autonomous: true
requirements:
  - RND-01
  - RND-03
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04

must_haves:
  decisions: [D-01, D-07]
  truths:
    - "SPA types.ts exports FormatRenderState, RenderStatusResponse, UseRenderResult contracts"
    - "api.ts exports triggerRender, pollRenderStatus, revealOutputFolder functions using relative URLs"
    - "All new api.ts functions follow the requestJson helper pattern — no absolute URLs"
  artifacts:
    - path: "packages/workspace/src/types.ts"
      provides: "FormatRenderState union, RenderStatusResponse interface, FormatKey already present"
      exports: ["FormatRenderState", "RenderStatusResponse"]
    - path: "packages/workspace/src/api.ts"
      provides: "triggerRender, pollRenderStatus, revealOutputFolder"
      exports: ["triggerRender", "pollRenderStatus", "revealOutputFolder"]
  key_links:
    - from: "packages/workspace/src/api.ts"
      to: "/api/local/render"
      via: "triggerRender POST"
    - from: "packages/workspace/src/api.ts"
      to: "/api/local/render/:id/status"
      via: "pollRenderStatus GET"
    - from: "packages/workspace/src/api.ts"
      to: "/api/local/reveal"
      via: "revealOutputFolder POST"
---

<objective>
Add the SPA-side type contracts and API helper functions that the render hooks and RenderPanel will consume. This plan establishes the interface layer — no hooks, no components, just types and API calls.

Purpose: Plans 03 and 04 import from these files; they must exist with the correct shapes before hooks and components can be written without guessing.

Output: Updated types.ts with render status types; updated api.ts with three new render/poll/reveal functions.

Decisions covered: D-01 (async job + poll — triggerRender + pollRenderStatus contract), D-07 (reveal helper revealOutputFolder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-local-image-render/05-CONTEXT.md
@.planning/phases/05-local-image-render/05-PATTERNS.md

# Files to modify
@packages/workspace/src/types.ts
@packages/workspace/src/api.ts

<interfaces>
<!-- Existing types.ts and api.ts contracts the executor must preserve and extend -->

From packages/workspace/src/types.ts (existing, must not be broken):
```typescript
export type FormatKey = "landscape" | "square" | "portrait";
// ... (DraftConfig, BrandRecord, DraftPreview, DraftDetail, etc. already present)
```

From packages/workspace/src/api.ts (existing, must not be broken):
```typescript
async function requestJson<T>(url: `/api/${string}`, init?: RequestInit): Promise<T>
export async function fetchRepoContext(): Promise<RepoContext>
export async function fetchDrafts(): Promise<DraftPreview[]>
export async function fetchDraft(id: string): Promise<DraftDetail>
export async function createDraft(config: DraftConfig): Promise<{ draft_id: string; created_at: string }>
export async function patchDraft(id: string, config: DraftConfig): Promise<{ draft_id: string; created_at: string }>
export async function fetchBrands(): Promise<BrandRecord[]>
```

CLI endpoint contracts (from Plan 01, matching server.ts implementation):
- POST /api/local/render body: { draftId: string } → 202 { id: string; status: "pending" }
- GET /api/local/render/:id/status → 200 { id: string; formats: Record<"landscape"|"square"|"portrait", FormatJobState> }
  where FormatJobState: { phase: "pending" } | { phase: "done"; url: string } | { phase: "failed"; error: string }
- POST /api/local/reveal body: { id: string } → 200 { ok: true }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts — add FormatRenderState and RenderStatusResponse</name>
  <files>packages/workspace/src/types.ts</files>
  <read_first>
    - packages/workspace/src/types.ts (full file — understand existing interfaces to place additions correctly)
    - .planning/phases/05-local-image-render/05-PATTERNS.md (useRender.ts section: FormatRenderState union, UseRenderResult shape)
    - .planning/phases/05-local-image-render/05-CONTEXT.md (D-01, D-04, D-05 — poll response shape, per-format state)
  </read_first>
  <action>
    Append to packages/workspace/src/types.ts (do not modify any existing type — add only):

    FormatRenderState: discriminated union type with four variants:
    - { phase: "idle" }
    - { phase: "pending" }
    - { phase: "done"; url: string }      // url = "/output/<id>/<format>.jpg"
    - { phase: "failed"; error: string }

    RenderStatusResponse: interface with:
    - id: string
    - formats: Record&lt;"landscape" | "square" | "portrait", FormatRenderState&gt;

    Place these after the existing BrandRecord / Brand block. Add a brief comment above: "// Phase 5: render job types".
  </action>
  <verify>
    <automated>cd /Users/rob/Development/bragfast && npx tsc -p packages/workspace/tsconfig.json --noEmit 2>&1 | grep -i "types.ts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/workspace/src/types.ts exports FormatRenderState and RenderStatusResponse
    - FormatRenderState is a discriminated union on the "phase" key with variants: "idle" | "pending" | "done" | "failed"
    - RenderStatusResponse.formats is typed as Record&lt;"landscape"|"square"|"portrait", FormatRenderState&gt;
    - TypeScript compilation of packages/workspace passes (0 errors in types.ts)
    - No existing types modified or removed
  </acceptance_criteria>
  <done>FormatRenderState and RenderStatusResponse exported from types.ts; workspace TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: api.ts — triggerRender, pollRenderStatus, revealOutputFolder</name>
  <files>packages/workspace/src/api.ts</files>
  <read_first>
    - packages/workspace/src/api.ts (full file — requestJson helper pattern, existing POST with JSON body pattern)
    - packages/workspace/src/types.ts (just updated — RenderStatusResponse, FormatRenderState)
    - .planning/phases/05-local-image-render/05-PATTERNS.md (api.ts section: requestJson usage, new render calls template)
    - .planning/phases/05-local-image-render/05-CONTEXT.md (D-01, D-07 — endpoint contracts)
  </read_first>
  <action>
    Add three new exported functions to packages/workspace/src/api.ts. Do not modify any existing function.

    First, add import for RenderStatusResponse from "./types" (add to the existing import statement from "./types" at the top).

    triggerRender(draftId: string): Promise&lt;{ id: string; status: "pending" }&gt;
    — POST "/api/local/render" with JSON body { draftId }
    — Use requestJson with method POST, Content-Type: application/json, body JSON.stringify({ draftId })

    pollRenderStatus(id: string): Promise&lt;RenderStatusResponse&gt;
    — GET "/api/local/render/${encodeURIComponent(id)}/status"
    — Use requestJson with no additional init (GET is the default)
    — The URL must be typed as: `/api/local/render/${string}/status` to satisfy the `` `/api/${string}` `` constraint

    revealOutputFolder(id: string): Promise&lt;void&gt;
    — POST "/api/local/reveal" with JSON body { id }
    — Use requestJson (return is discarded); wrap as Promise&lt;void&gt; by returning void
    — Any non-ok response will throw via requestJson (error propagated to caller)

    All three functions must be added after the existing fetchBrands function, at the end of the file. The URL template literal must satisfy the `` `/api/${string}` `` type constraint — use the cast pattern: `` `/api/local/render/${encodeURIComponent(id)}/status` as `/api/${string}` `` if TypeScript demands it.
  </action>
  <verify>
    <automated>cd /Users/rob/Development/bragfast && npx tsc -p packages/workspace/tsconfig.json --noEmit 2>&1 | grep -i "api.ts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - api.ts exports triggerRender, pollRenderStatus, revealOutputFolder
    - triggerRender posts to "/api/local/render" with { draftId } body
    - pollRenderStatus gets "/api/local/render/:id/status" using encodeURIComponent(id)
    - revealOutputFolder posts to "/api/local/reveal" with { id } body
    - Return type of pollRenderStatus is Promise&lt;RenderStatusResponse&gt;
    - No absolute URLs used — all paths are relative /api/... strings
    - TypeScript compilation passes with 0 errors in api.ts
  </acceptance_criteria>
  <done>Three render API functions exported from api.ts; workspace TypeScript compiles clean; all URLs are relative</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → CLI server | These api.ts functions call CLI-local endpoints; the CLI server enforces origin-lock; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | pollRenderStatus exposes render job state | accept | Endpoint is origin-locked to 127.0.0.1:<port>; only the local Workspace SPA can call it |
| T-05-07 | Tampering | triggerRender — draftId param from SPA could be manipulated | accept | Validation happens on the CLI server (Plan 01); SPA is trusted UI within the origin-locked context |
| T-05-SC | Tampering | No new npm packages — no package-manager installs | accept | No install step in this plan |
</threat_model>

<verification>
1. `npx tsc -p packages/workspace/tsconfig.json --noEmit` exits 0
2. `grep -c 'FormatRenderState' packages/workspace/src/types.ts` returns >= 1
3. `grep -c 'RenderStatusResponse' packages/workspace/src/types.ts` returns >= 1
4. `grep -c 'triggerRender' packages/workspace/src/api.ts` returns >= 1
5. `grep -c 'pollRenderStatus' packages/workspace/src/api.ts` returns >= 1
6. `grep -c 'revealOutputFolder' packages/workspace/src/api.ts` returns >= 1
7. `grep -v '^#' packages/workspace/src/api.ts | grep -c 'http://'` returns 0 (no absolute URLs)
</verification>

<success_criteria>
- types.ts and api.ts compile clean with new render types and functions
- FormatRenderState discriminated union matches the CLI server's FormatJobState shape exactly
- All API calls use relative URLs (enforced by requestJson's type signature)
</success_criteria>

<output>
Create `.planning/phases/05-local-image-render/05-02-SUMMARY.md` when done
</output>
