---
phase: 05-local-image-render
plan: 04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-03
files_modified:
  - packages/workspace/src/components/RenderPanel.tsx
  - packages/workspace/src/pages/Editor.tsx
autonomous: false
requirements:
  - RND-01
  - RND-03
  - RND-05
  - RND-06
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04

must_haves:
  truths:
    - "Clicking 'Render images' in the Workspace flushes the save, submits the render job, and shows per-format progress rows"
    - "After render completes, the canvas preview is replaced by actual rendered JPEGs from /output/<id>/<format>.jpg"
    - "Format switcher still works after render — switching formats swaps which rendered JPEG is shown"
    - "Copy caption button copies DraftConfig.caption to clipboard and flashes 'Copied!' for 2s"
    - "Download link saves the rendered file to the browser's download location"
    - "Open folder button calls POST /api/local/reveal and reveals the output folder in the OS file manager"
    - "Failed formats show inline error message — no modal, no toast"
    - "All-failed state re-enables the Render button with label 'Retry render'"
    - "RenderPanel is never a modal; it sits below the canvas in the editor layout"
  artifacts:
    - path: "packages/workspace/src/components/RenderPanel.tsx"
      provides: "RenderPanel component with Render button, FormatStatusRow list, RenderedPreview, ActionRow"
      exports: ["RenderPanel"]
    - path: "packages/workspace/src/pages/Editor.tsx"
      provides: "Editor wired with useRender hook; RenderPanel rendered below canvas; TemplatePreview swapped for rendered image when done"
      contains: "useRender"
  key_links:
    - from: "packages/workspace/src/pages/Editor.tsx"
      to: "packages/workspace/src/hooks/useRender.ts"
      via: "useRender({ flush: save.flush })"
    - from: "packages/workspace/src/components/RenderPanel.tsx"
      to: "/output/<id>/<format>.jpg"
      via: "<img src={url}> and <a href={url} download>"
    - from: "packages/workspace/src/components/RenderPanel.tsx"
      to: "packages/workspace/src/api.ts revealOutputFolder"
      via: "handleOpenFolder calls revealOutputFolder(jobId)"
---

<objective>
Build the RenderPanel component (Render button, FormatStatusRow list, RenderedPreview, ActionRow) and wire it into Editor.tsx using the useRender hook from Plan 03. After this plan, a developer can click "Render images", see per-format progress, view actual rendered JPEGs, copy caption, download files, and open the output folder.

Purpose: This is the user-facing surface that satisfies all OUT-01..04 and closes the render loop visible to the developer.

Output: RenderPanel.tsx (new component), Editor.tsx (wired with useRender + RenderPanel).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-local-image-render/05-CONTEXT.md
@.planning/phases/05-local-image-render/05-PATTERNS.md
@.planning/phases/05-local-image-render/05-UI-SPEC.md

# Files to modify
@packages/workspace/src/pages/Editor.tsx
@packages/workspace/src/components/SavedIndicator.tsx
@packages/workspace/src/hooks/useRender.ts
@packages/workspace/src/api.ts

<interfaces>
<!-- All types and hooks the executor needs. Extracted from prior plans. -->

From packages/workspace/src/hooks/useRender.ts (Plan 03):
```typescript
export type RenderPhase = "idle" | "flushing" | "rendering" | "done" | "failed-all" | "partial";

export interface UseRenderArgs {
  flush: () => Promise<string | null>;
}

export interface UseRenderResult {
  renderPhase: RenderPhase;
  formats: Record<"landscape" | "square" | "portrait", FormatRenderState>;
  jobId: string | null;
  trigger: () => Promise<void>;
}

export function useRender(args: UseRenderArgs): UseRenderResult
```

From packages/workspace/src/types.ts (Plan 02):
```typescript
export type FormatRenderState =
  | { phase: "idle" }
  | { phase: "pending" }
  | { phase: "done"; url: string }
  | { phase: "failed"; error: string };
```

From packages/workspace/src/api.ts (Plan 02):
```typescript
export async function revealOutputFolder(id: string): Promise<void>
```

From packages/workspace/src/hooks/useAutoSave.ts (Plan 03):
```typescript
export interface UseAutoSaveResult {
  draftId: string | null;
  status: SaveStatus;
  statusLabel: string;
  flush: () => Promise<string | null>;
}
```

From packages/workspace/src/pages/Editor.tsx (current):
```typescript
const save = useAutoSave({ draftId, config: dirty ? config : null });
// save.draftId, save.status, save.statusLabel, save.flush (after Plan 03)
```

CSS tokens in use (from packages/workspace/src/index.css):
  --workspace-bg, --workspace-surface, --workspace-forest, --workspace-lime,
  --workspace-sage, --workspace-ink, --workspace-border, --workspace-muted
Destructive: #EF4444 (Tailwind red-500)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RenderPanel.tsx — full render panel component</name>
  <files>packages/workspace/src/components/RenderPanel.tsx</files>
  <read_first>
    - packages/workspace/src/components/SavedIndicator.tsx (status-keyed visual pattern, token usage to mirror)
    - packages/workspace/src/pages/Editor.tsx (existing layout tokens, border/bg patterns, FormatSwitcher positioning)
    - .planning/phases/05-local-image-render/05-UI-SPEC.md (full spec: component states, FormatStatusRow, RenderedPreview, ActionRow, copywriting contract, accessibility requirements, color usage)
    - .planning/phases/05-local-image-render/05-PATTERNS.md (RenderPanel section: status-keyed pattern, token usage, Render button shape, spinner pattern, copy-caption flash pattern)
    - .planning/phases/05-local-image-render/05-CONTEXT.md (D-04, D-05, D-06, D-07, D-08, D-11, D-12)
  </read_first>
  <action>
    Create packages/workspace/src/components/RenderPanel.tsx.

    PROPS interface RenderPanelProps:
    - renderPhase: RenderPhase
    - formats: Record&lt;"landscape" | "square" | "portrait", FormatRenderState&gt;
    - jobId: string | null
    - caption: string
    - activeFormat: "landscape" | "square" | "portrait"
    - onTrigger: () =&gt; Promise&lt;void&gt;
    - onReveal: () =&gt; void     // calls revealOutputFolder; parent handles

    COMPONENT LAYOUT (inline panel, never a modal — D-04):
    Outer div: bg-[var(--workspace-bg)] border border-[var(--workspace-border)] rounded-[8px] p-4 mt-4 aria-live="polite"

    STATE 1 — Idle (renderPhase === "idle") or failed-all retry:
    - Show only the Render button: full-width, min-h-[44px], bg-[var(--workspace-lime)] text-[var(--workspace-forest)] font-semibold rounded-[8px] hover:opacity-90 disabled:opacity-50 disabled:cursor-not-allowed
    - Label: "Render images" when idle; "Retry render" when renderPhase === "failed-all" (and formats all have phase "failed")
    - Disabled (opacity-50 cursor-not-allowed aria-disabled="true"): when renderPhase === "flushing"
    - Flushing label: "Saving…"
    - onClick: calls onTrigger(); guard button with disabled when renderPhase is "flushing" or "rendering"

    STATE 2 — Rendering / partial / done (renderPhase is "flushing" | "rendering" | "done" | "partial"):
    - Hide the Render button; show the section heading "Rendered images" (16px/600 text-[var(--workspace-forest)])
    - Show three FormatStatusRow items (one per format): landscape, square, portrait

    FormatStatusRow (inline — do not extract to a separate file; keep as a function component inside RenderPanel.tsx):
    Props: format ("landscape"|"square"|"portrait"), state (FormatRenderState), jobId (string|null)
    Renders a flex row: items-center gap-2 py-2 border-b border-[var(--workspace-border)] last:border-b-0
    — Idle: gray muted dot (h-2 w-2 rounded-full bg-[var(--workspace-border)]) + format label in text-[var(--workspace-muted)] text-[12px] font-semibold
    — Pending: spinner (span role="status" aria-label="Rendering {format}" className="h-4 w-4 rounded-full border-2 border-[var(--workspace-border)] border-t-[var(--workspace-forest)] animate-spin") + "{Format} rendering…" in text-[var(--workspace-ink)] text-[12px] font-semibold
    — Done: lime filled dot (h-2 w-2 rounded-full bg-[var(--workspace-lime)]) + format label in text-[var(--workspace-forest)] text-[12px] font-semibold + Download anchor right-aligned (ml-auto)
    — Failed: red X indicator (h-4 w-4 text-red-500 "✕") + "{Format} failed" in text-[var(--workspace-forest)] text-[12px] font-semibold + inline error message in a span with bg-red-500/10 text-red-600 text-[12px] rounded px-2 py-0.5 ml-2

    Download anchor (per D-06, OUT-04):
    - href="/output/{jobId}/{format}.jpg" — use encodeURIComponent(jobId) for safety
    - download attribute: "{format}.jpg"
    - aria-label="Download {format} image"
    - min-h-[44px] flex items-center: text-[12px] font-semibold text-[var(--workspace-forest)] underline underline-offset-2
    - Text: "Download {format}" per copywriting contract

    RenderedPreview (per D-05, OUT-03):
    - Displayed when renderPhase is "done" or "partial" AND the activeFormat's FormatRenderState.phase === "done"
    - Shown as an &lt;img&gt; tag inside the canvas wrapper (NOT inside RenderPanel — this is passed as context to Editor.tsx; RenderPanel just provides the showPreview flag and url)
    - Actually: RenderPanel DOES own the preview image for simplicity. Add a preview area below the format rows when renderPhase is "done" or "partial":
      - If formats[activeFormat].phase === "done": render &lt;img src={formats[activeFormat].url} alt="{activeFormat} render preview" className="w-full rounded-[8px] border border-[var(--workspace-border)]" /&gt;
      - This preview is the rendered JPEG, not the CanvasRenderer

    ActionRow (per D-06, D-07, D-08, OUT-01, OUT-02, OUT-04):
    - Shown when renderPhase is "done" or "partial"
    - Horizontal flex gap-2 flex-wrap mt-4
    - Three items, each min-h-[44px]:

    1. Copy caption button (OUT-01):
       - Uses local useState copied: boolean
       - onClick: void navigator.clipboard.writeText(caption).then(() =&gt; { setCopied(true); window.setTimeout(() =&gt; setCopied(false), 2000); })
       - Label: copied ? "Copied!" : "Copy caption"
       - className: min-h-[44px] px-3 text-[12px] font-semibold text-[var(--workspace-forest)] border border-[var(--workspace-border)] rounded-[8px] hover:bg-[var(--workspace-surface)] bg-white

    2. Download active format (OUT-04):
       - &lt;a&gt; styled as button
       - Only render if formats[activeFormat].phase === "done"
       - href and download as described in FormatStatusRow Download anchor
       - Label: "Download {activeFormat}"
       - aria-label: "Download {activeFormat} image"
       - Same className as Copy caption button

    3. Open folder button (OUT-02):
       - onClick: onReveal()
       - Label: "Open folder"
       - aria-label: "Open output folder in file manager"
       - Same className as Copy caption button

    All-failed summary line (per UI-SPEC):
    - If renderPhase === "failed-all": show a paragraph above the format rows:
      "Render failed. Check the terminal for details." — text-[12px] text-red-600 mb-3

    ACCESSIBILITY: wrap the outer div with aria-live="polite". Render button: aria-disabled when flushing.
  </action>
  <verify>
    <automated>cd /Users/rob/Development/bragfast && npx tsc -p packages/workspace/tsconfig.json --noEmit 2>&1 | grep -i "RenderPanel" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/workspace/src/components/RenderPanel.tsx exists and exports RenderPanel
    - Component renders Render button when renderPhase === "idle"
    - Component renders FormatStatusRow list when renderPhase !== "idle"
    - Render button label is "Render images" (idle), "Saving…" (flushing), "Retry render" (failed-all)
    - Render button is disabled (aria-disabled="true" opacity-50) when flushing
    - FormatStatusRow spinner has role="status" and aria-label="Rendering {format}"
    - Download anchor has download attribute and aria-label="Download {format} image"
    - Open folder button has aria-label="Open output folder in file manager"
    - Copy caption flashes "Copied!" for 2s on success
    - All-failed summary "Render failed. Check the terminal for details." appears when renderPhase === "failed-all"
    - No absolute URLs — /output/{jobId}/{format}.jpg relative path only
    - No hex literals except #EF4444 (red-500); all other colors use --workspace-* tokens
    - TypeScript compilation passes: 0 errors in RenderPanel.tsx
  </acceptance_criteria>
  <done>RenderPanel fully implemented with all states; TypeScript clean; accessibility attributes present; no modal; actionable error display</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    RenderPanel.tsx component and Editor.tsx wired with useRender hook and RenderPanel. The complete render flow is now wired end-to-end: Render button → flush → trigger → poll → preview + actions.

    Editor.tsx changes made by this checkpoint:
    1. Import useRender from "../hooks/useRender" and RenderPanel from "../components/RenderPanel"
    2. Import revealOutputFolder from "../api"
    3. Initialize: const render = useRender({ flush: save.flush })
    4. Replace the TemplatePreview div with: if render.renderPhase is "done" or "partial" AND formats[activeFormat].phase === "done", show the rendered image instead of TemplatePreview. Specifically: the canvas wrapper div renders TemplatePreview when render.renderPhase === "idle" || render.renderPhase === "flushing" || render.renderPhase === "rendering", and renders &lt;img src={render.formats[activeFormat].url}&gt; (where phase === "done") otherwise. If format is done but activeFormat is failed, show TemplatePreview as fallback.
    5. Add RenderPanel below the canvas wrapper div (still inside the left &lt;section&gt;):
       &lt;RenderPanel
         renderPhase={render.renderPhase}
         formats={render.formats}
         jobId={render.jobId}
         caption={config.caption ?? ""}
         activeFormat={activeFormat}
         onTrigger={render.trigger}
         onReveal={() =&gt; { if (render.jobId) void revealOutputFolder(render.jobId); }}
       /&gt;

    After writing Editor.tsx, Claude must verify the workspace build compiles.
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/rob/Development/bragfast && npm run build 2>&1 | tail -30` — build must exit 0
    2. Start the CLI: `cd /Users/rob/Development/bragfast && node packages/cli/dist/index.js` (or `npx bragfast`) — the Workspace SPA should open
    3. Open the Workspace, select a template, fill at least one text slot
    4. Click "Render images" — you should see the button label change to "Saving…" briefly, then three format rows appear with spinning indicators
    5. Check the terminal where brag is running — you should see per-format progress lines (e.g. "[brag] landscape rendered")
    6. After render completes, the canvas area should show the rendered JPEG for the active format
    7. Try "Copy caption" — clipboard should contain the caption text; button should flash "Copied!" for 2s
    8. Try "Download landscape" — browser should download a .jpg file
    9. Try "Open folder" — your OS file manager should open the brag-output/ directory
    10. Verify the rendered files exist on disk: `ls ./brag-output/*/` should show landscape.jpg, square.jpg, portrait.jpg
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 verification steps pass, or describe which steps failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → CLI server | RenderPanel calls /api/local/reveal via revealOutputFolder; CLI server validates the id (Plan 01 guards) |
| SPA rendering rendered images | img src="/output/<id>/<format>.jpg" — relative URL, served by CLI's express.static; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | RenderPanel Download anchor href — jobId in path | mitigate | Use encodeURIComponent(jobId) in href; CLI express.static does not traverse above root |
| T-05-11 | Tampering | onReveal calls revealOutputFolder(jobId) — jobId from state, not user input | accept | jobId comes from triggerRender response (server-generated UUID); not user-supplied; CLI server re-validates anyway |
| T-05-12 | Information Disclosure | RenderedPreview img loads from /output — exposes rendered file content | accept | /output is served by origin-locked CLI; only accessible from the local Workspace tab |
| T-05-SC | Tampering | No new npm packages | accept | No install step |
</threat_model>

<verification>
1. `npx tsc -p packages/workspace/tsconfig.json --noEmit` exits 0
2. `grep -c 'RenderPanel' packages/workspace/src/pages/Editor.tsx` returns >= 1
3. `grep -c 'useRender' packages/workspace/src/pages/Editor.tsx` returns >= 1
4. `grep -c 'aria-live' packages/workspace/src/components/RenderPanel.tsx` returns >= 1
5. `grep -c 'Render images' packages/workspace/src/components/RenderPanel.tsx` returns >= 1
6. `grep -c 'Copied!' packages/workspace/src/components/RenderPanel.tsx` returns >= 1
7. `grep -c 'Open folder' packages/workspace/src/components/RenderPanel.tsx` returns >= 1
8. `grep -v '^#' packages/workspace/src/components/RenderPanel.tsx | grep -c '#[0-9a-fA-F]\{6\}'` returns 0 or 1 (only #EF4444 allowed)
9. Human checkpoint approved
</verification>

<success_criteria>
- End-to-end render flow works: click Render → see progress → see rendered images
- Copy caption, Download, and Open folder all work as specified in D-06, D-07, D-08
- Error states (per-format failure) surface inline in FormatStatusRow — no modal
- All-failed state re-enables Render button with "Retry render" label
- Format switcher swaps rendered preview between landscape/square/portrait
- TypeScript clean; build passes; human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/05-local-image-render/05-04-SUMMARY.md` when done
</output>
