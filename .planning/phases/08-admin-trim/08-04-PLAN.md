---
phase: 08-admin-trim
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - src/lib/types.ts
  - src/lib/plans.ts
  - src/lib/pricing-data.tsx
  - src/lib/plan-tiers.ts
  - src/lib/accounting/post-allowance.ts
  - convex/planTiers.ts
  - convex/userProfiles.ts
  - src/app/api/v1/cook/_shared.ts
  - src/app/api/v1/cook/image/route.ts
  - src/app/api/v1/cook/video/route.ts
  - src/app/api/v1/cook/[id]/route.ts
  - src/lib/pipeline/render.ts
  - src/lib/pipeline/render-video.ts
  - convex/videoRender.ts
  - convex/videoRenderHelpers.ts
autonomous: true
requirements:
  - ADM-05

must_haves:
  truths:
    - "calculateCredits function is deleted from src/lib/types.ts"
    - "creditsRemaining has no remaining code references in src/ or convex/ (schema field kept optional for Deploy 2 in Plan 08-10)"
    - "getBalance, reserve, refund mutations are deleted from convex/userProfiles.ts"
    - "cook API routes have no reserveCreditsOrError / refundAndFail calls"
    - "render pipeline has no refund-on-failure block"
    - "npm run build exits 0 after all code reference removal"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "calculateCredits deleted; credits fields removed from ReleaseResult"
    - path: "convex/userProfiles.ts"
      provides: "getBalance/reserve/refund deleted; getStats strips credit fields"
    - path: "src/app/api/v1/cook/_shared.ts"
      provides: "reserveCreditsOrError and refundAndFail deleted"
  key_links:
    - from: "src/app/api/v1/cook/image/route.ts"
      to: "src/app/api/v1/cook/_shared.ts"
      via: "reserveCreditsOrError import — must be removed"
      pattern: "reserveCreditsOrError"
---

<objective>
Full credits teardown — removes all credit references from types, plans config, Convex mutations, cook API routes, and render pipeline. This is the code-reference-clean deploy that sits BETWEEN the two Convex schema deploys (Plan 08-02 made creditsRemaining optional; Plan 08-10 will remove the field entirely).

Purpose: Strip D-12 credits system. After this plan, no code in src/ or convex/ references creditsRemaining, calculateCredits, reserve/refund credit mutations, or the old multi-tier plan config. Cook API routes survive as stubs with credit logic excised.

Output: Modified files with credits stripped; npm run build green; grep gates pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete credit type/config files + strip credits from types.ts and plans.ts</name>
  <files>
    src/lib/types.ts,
    src/lib/plans.ts,
    src/lib/pricing-data.tsx,
    src/lib/plan-tiers.ts,
    src/lib/accounting/post-allowance.ts,
    convex/planTiers.ts
  </files>

  <read_first>
    - src/lib/types.ts — read the full file; find `calculateCredits` function, `CookCreditsInput` type, and `credits_remaining`/`credits_used` fields on `ReleaseResult`; note other exports to ensure they are preserved
    - src/lib/plans.ts — read the full file; current shape has PlanId with starter/pro/scale, PlanConfig with credits field; target shape from PATTERNS.md (PlanId: "trial" | "free" | "plate", no credits field)
    - src/lib/pricing-data.tsx — read; confirm it is exclusively multi-tier UI (Toast/Plate/Buffet); safe to delete entirely
    - src/lib/plan-tiers.ts — read; confirm it only exports TIER_CONFIG used by stripe.ts and upsell-modal.tsx (both being modified this phase)
    - src/lib/accounting/post-allowance.ts — read; confirm it is credits-based allowance logic; safe to delete
    - convex/planTiers.ts — read; confirm it only exports TIER_CONFIG used by convex/stripe.ts (being rewritten in Plan 08-06)
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-3 Layer 4 (types teardown list) + §RQ-3 Layer 1 (schema last)
    - .planning/phases/08-admin-trim/08-PATTERNS.md §plans.ts collapse (target shape)
  </read_first>

  <action>
    Step 1 — Collapse src/lib/plans.ts to the single-plan shape (per PATTERNS.md §plans.ts):
    - Set `PlanId = "trial" | "free" | "plate"`
    - Remove `credits` field from `PlanConfig` interface
    - Replace `PLANS` with three entries: trial (On the House, $0, 14-day free trial), free (Free, $0), plate (Full Plate, $29)
    - Remove `PAID_PLANS` and `ALL_PLANS` exports (no longer needed)

    Step 2 — Strip credits from src/lib/types.ts:
    - Delete the `calculateCredits` function and `CookCreditsInput` type
    - Remove `credits_remaining` and `credits_used` fields from the `ReleaseResult` type/interface

    Step 3 — Delete these files entirely (they are exclusively credits/multi-tier):
    - `src/lib/pricing-data.tsx`
    - `src/lib/plan-tiers.ts`
    - `src/lib/accounting/post-allowance.ts`
    - `convex/planTiers.ts`

    Step 4 — Check for any remaining imports of deleted files in surviving src/ or convex/ files:
    - `grep -r "pricing-data" src/ convex/ --include="*.ts" --include="*.tsx" -l`
    - `grep -r "plan-tiers" src/ convex/ --include="*.ts" --include="*.tsx" -l`
    - `grep -r "post-allowance" src/ convex/ --include="*.ts" --include="*.tsx" -l`
    - `grep -r "planTiers" convex/ --include="*.ts" -l`

    For any surviving files that import deleted modules, remove or stub those imports. Do NOT modify convex/stripe.ts here — it will be updated in Plan 08-06.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -r "calculateCredits" src/ convex/ --include="*.ts" --include="*.tsx" -v "^#"` returns no matches
    - `grep -rn "credits_remaining\|creditsRemaining" src/ --include="*.ts" --include="*.tsx"` returns no matches in src/ (convex/schema.ts has it as optional field — that is fine until Plan 08-10)
    - src/lib/plans.ts: PlanId contains only "trial" | "free" | "plate"; no credits field in PlanConfig
    - src/lib/pricing-data.tsx: file does not exist
    - src/lib/plan-tiers.ts: file does not exist
    - convex/planTiers.ts: file does not exist
    - `npx tsc --noEmit` exits 0 or errors are pre-existing (check against baseline)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Strip credits from Convex userProfiles + cook API routes + render pipeline</name>
  <files>
    convex/userProfiles.ts,
    src/app/api/v1/cook/_shared.ts,
    src/app/api/v1/cook/image/route.ts,
    src/app/api/v1/cook/video/route.ts,
    src/app/api/v1/cook/[id]/route.ts,
    src/lib/pipeline/render.ts,
    src/lib/pipeline/render-video.ts,
    convex/videoRender.ts,
    convex/videoRenderHelpers.ts
  </files>

  <read_first>
    - convex/userProfiles.ts — read the full file; identify `getBalance` (line ~70), `reserve` (line ~83), `refund` (line ~207), and the `creditsRemaining`/`creditsUsedThisMonth` portions of `getStats` (line ~130)
    - src/app/api/v1/cook/_shared.ts — read the full file; find `reserveCreditsOrError` and `refundAndFail` functions (lines ~126-165)
    - src/app/api/v1/cook/image/route.ts — read; find calculateCredits call, reserveCreditsOrError call, refundAndFail call, result.credits_remaining assignment
    - src/app/api/v1/cook/video/route.ts — same as image
    - src/app/api/v1/cook/[id]/route.ts — read; find api.userProfiles.getBalance call and result.credits_remaining
    - src/lib/pipeline/render.ts — read; find calculateCredits import + creditsUsed in ReleaseResult + refund-on-failure block (lines ~173-178)
    - src/lib/pipeline/render-video.ts — read; find creditsUsed in result + credit refund logic
    - convex/videoRender.ts + convex/videoRenderHelpers.ts — skim for creditsUsed/creditsRemaining references
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-3 Layer 2 and Layer 3 (exact functions to remove)
  </read_first>

  <action>
    Step 1 — convex/userProfiles.ts: Delete these three exported mutations/queries entirely:
    - `getBalance` query (reads creditsRemaining — only caller was cook/[id] route)
    - `reserve` mutation (deducts credits)
    - `refund` mutation (refunds credits)
    Also strip `creditsRemaining` and `creditsUsedThisMonth` from the `getStats` query's returned object (keep the rest of getStats intact).

    Step 2 — src/app/api/v1/cook/_shared.ts: Delete `reserveCreditsOrError` and `refundAndFail` functions (lines ~126-165). Remove any imports they require (userProfiles.reserve/refund). Keep all other shared helpers (authentication, response utilities, etc.).

    Step 3 — src/app/api/v1/cook/image/route.ts: Remove:
    - `calculateCredits` import + call
    - `reserveCreditsOrError` import + call
    - `refundAndFail` import + usage (replace try/catch with simple error return if needed)
    - `result.credits_remaining` assignment in the response object
    Keep the route functional as a stub — it must still handle requests, just without credit gating.

    Step 4 — src/app/api/v1/cook/video/route.ts: Same changes as image route.

    Step 5 — src/app/api/v1/cook/[id]/route.ts: Remove:
    - `api.userProfiles.getBalance` call
    - `result.credits_remaining` field from response object

    Step 6 — src/lib/pipeline/render.ts: Remove:
    - `calculateCredits` import
    - `creditsUsed` from `ReleaseResult` shape (if present here — it may be in types.ts only)
    - The refund-on-failure block (lines ~173-178 per RESEARCH.md)

    Step 7 — src/lib/pipeline/render-video.ts: Remove:
    - `creditsUsed` from result
    - Any credit refund logic

    Step 8 — convex/videoRender.ts and convex/videoRenderHelpers.ts: Grep for `creditsUsed` and `creditsRemaining`; remove any references found.

    Run `npm run build` after all changes.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "creditsRemaining\|creditsUsed\|calculateCredits\|reserveCreditsOrError\|refundAndFail\|getBalance\|\.reserve\|\.refund" src/ convex/ --include="*.ts" --include="*.tsx" | grep -v "schema.ts" | grep -v "__tests__"` returns no matches
    - convex/userProfiles.ts: `getBalance`, `reserve`, `refund` exports are absent
    - `npm run build` exits 0
    - Cook API routes still export valid POST handlers (no empty files)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/client → cook API | Cook API routes survive as stubs but without credit gating; they are not called by Workspace/CLI anyway (ADR-0002 confirmed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | Cook API credit removal | accept | Cook routes are no longer the render path (ADR-0002); removing credit checks from dormant routes has no active attack surface. Routes still require authentication via existing `authenticate()` call |
| T-08-06 | Denial of Service | creditsRemaining optional field during transition | accept | Making field optional (Plan 08-02) ensures reads return undefined gracefully; writes no longer set the field; no denial-of-service risk |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
```
npm run build
grep -rn "creditsRemaining\|calculateCredits\|reserveCreditsOrError\|refundAndFail" src/ convex/ --include="*.ts" --include="*.tsx" | grep -v "schema.ts" | grep -v "test"
```

Both must produce clean output (build exits 0, grep returns nothing).
</verification>

<success_criteria>
- All credit references stripped from src/ and convex/ (schema field remains optional — stripped in Plan 08-10)
- plans.ts collapsed to single plan; pricing-data.tsx, plan-tiers.ts, post-allowance.ts, convex/planTiers.ts deleted
- cook API routes and render pipeline are credit-free
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-04-SUMMARY.md` when done
</output>
