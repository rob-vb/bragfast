---
phase: 08-admin-trim
plan: 10
type: execute
wave: 6
depends_on:
  - 08-08
  - 08-09
files_modified:
  - convex/schema.ts
  - src/app/device/page.tsx
  - src/components/device/device-approval.tsx
  - src/app/(auth)/login/page.tsx
  - CLAUDE.md
autonomous: false
requirements:
  - ADM-01
  - ADM-02
  - ADM-03
  - ADM-04
  - ADM-05

must_haves:
  truths:
    - "convex/schema.ts userProfiles table has no creditsRemaining field"
    - "npm run build exits 0 after final schema cleanup"
    - "Device approval page and login ?next= redirect path are verified working"
    - "CLAUDE.md is updated to remove GitHub App section and reflect current architecture"
    - "Full test suite passes (npx vitest run) with no new failures"
  artifacts:
    - path: "convex/schema.ts"
      provides: "Final schema: userProfiles without creditsRemaining; plan union trial|plate|free"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "/device?code=XXXX"
      via: "?next= query param redirect"
      pattern: "next="
---

<objective>
Final schema cleanup (Deploy 2 of 2), device approval path verification (D-15), and documentation housekeeping. This plan completes the two-deploy credits teardown sequence by removing the now-unused creditsRemaining field from the schema.

Also includes a human checkpoint to verify the device approval path end-to-end (ADM-01, D-15) since this cannot be automated without a running CLI + browser session.

Purpose: Close out the Convex schema migration safely (all code references stripped in Plans 08-04/08-06), update project docs to reflect the trimmed state, and confirm the device flow path works.

Output: Clean schema with no creditsRemaining field; verified device flow; updated CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final Convex schema cleanup — remove creditsRemaining</name>
  <files>convex/schema.ts</files>

  <read_first>
    - convex/schema.ts — read the current state of the file; confirm creditsRemaining is still present as v.optional(v.number()) (from Plan 08-02); confirm plan union is now trial|plate|free (from Plan 08-06); confirm trialEnd is present
    - Run grep to confirm zero code references remain before schema change: `grep -rn "creditsRemaining" src/ convex/ --include="*.ts" --include="*.tsx" | grep -v schema.ts | grep -v "__tests__"` — must return empty before proceeding
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-7 (two-deploy sequence: this is deploy 2 — safe only after code references are stripped)
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-7 Pitfall 1 (warning: do not proceed if any code reference remains)
  </read_first>

  <action>
    PRECONDITION CHECK (run first, before making any change):
    ```
    grep -rn "creditsRemaining" src/ convex/ --include="*.ts" --include="*.tsx" | grep -v "schema.ts" | grep -v "__tests__"
    ```
    If this returns ANY matches: STOP. Do not proceed. The code reference must be removed in Plan 08-04 before this task can run. Report the remaining reference to the user.

    If precondition passes (no matches):

    In convex/schema.ts, find the `userProfiles` table definition and remove the `creditsRemaining: v.optional(v.number())` line entirely.

    Do NOT touch any other field. The final userProfiles schema should have:
    - userId: v.string()
    - email: v.optional(v.string())
    - plan: v.union(v.literal("trial"), v.literal("plate"), v.literal("free"))
    - trialEnd: v.optional(v.number())
    - All other existing optional fields (lastDraftsVisitAt, etc. — keep anything that was there before Phase 8)
    - NO creditsRemaining field

    After removing the field, run `npm run build` to confirm Convex codegen regenerates cleanly.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "creditsRemaining" convex/schema.ts` returns no matches
    - `grep -n "trialEnd" convex/schema.ts` matches the optional number field
    - `grep -n "plan:" convex/schema.ts` shows only "trial", "plate", "free" literals (no starter/pro/scale/toast/buffet)
    - `npm run build` exits 0
    - Convex codegen does not error on the updated schema
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify device approval path and update CLAUDE.md</name>
  <files>
    src/app/device/page.tsx,
    src/components/device/device-approval.tsx,
    src/app/(auth)/login/page.tsx,
    CLAUDE.md
  </files>

  <read_first>
    - src/app/device/page.tsx — read; confirm the page exists and renders correctly; check that it reads the `?code=` query param and shows the approval UI
    - src/components/device/device-approval.tsx — read; confirm the approval button and device-flow handshake logic are intact (not accidentally deleted in Phase 8)
    - src/app/(auth)/login/page.tsx — read lines 10-25; confirm `?next=` param handling is present (per RESEARCH.md ADM-01 verified path)
    - CLAUDE.md — read the full file; find: GitHub App section (to update/remove after webhook deletion), any references to Kitchen/cook authoring, any references to NEXT_PUBLIC_LAUNCH_MODE, any credits/multi-tier references
    - .planning/phases/08-admin-trim/08-CONTEXT.md §canonical_refs §Device approval (D-15 — verify URL-param path only)
  </read_first>

  <action>
    Step 1 — Verify device approval path (read-only verification, no code change expected):
    Read src/app/device/page.tsx and src/components/device/device-approval.tsx. Confirm:
    - device/page.tsx reads `?code=` from searchParams
    - It redirects to `/login?next=/device?code=XXXX` when user is not logged in
    - device-approval.tsx has an "Approve CLI Access" button that completes the device-flow handshake
    - login/page.tsx handles the `?next=` param and redirects after login

    If any of these are missing or broken (e.g., something was accidentally deleted in Phase 8), fix it. If the path is intact, note "Device approval path verified" in the task output.

    Step 2 — Update CLAUDE.md to reflect Phase 8 changes:
    In CLAUDE.md:
    - Find the "GitHub App" section — update it to reflect that the GitHub App integration is removed. Replace the section body with a one-line note: "GitHub App integration removed in Phase 8 (ADR-0001 — shelved automation)."
    - Find any references to Kitchen, cook authoring UI, or the kitchen server route — remove or update them
    - Find any reference to NEXT_PUBLIC_LAUNCH_MODE or launch-mode.ts — remove
    - Find the "Convex" tables list — update to remove tables/sections that described deleted surfaces (githubInstallations/RepoConfigs from the table if listed; milestoneHits/triggerEvents/goals/briefings entries if present)
    - Find the "API Routes" section — remove the GitHub App routes (webhooks, callback, installations, repos, configs) from the listed routes; update to add `/api/v1/sous-chef/integrations` as the Buffer connect route

    Do NOT rewrite CLAUDE.md extensively — make minimal targeted edits to reflect the Phase 8 deletions. The document structure and most content stays.
  </action>

  <verify>
    <automated>npx vitest run 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `ls src/app/device/page.tsx` confirms the file exists
    - `grep -n "next=\|code=" src/app/(auth)/login/page.tsx` confirms ?next= handling is present
    - `grep -n "Approve\|approve" src/components/device/device-approval.tsx` confirms the approval button exists
    - `grep -n "GitHub App" CLAUDE.md` returns the updated minimal note (not the old multi-paragraph section)
    - `grep -n "NEXT_PUBLIC_LAUNCH_MODE\|launch-mode" CLAUDE.md` returns no matches
    - `npx vitest run` exits 0 or shows only the pre-existing GitHub callback test failures (STATE.md: two callback redirect failures are pre-existing and acceptable)
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 8 admin trim:
    - Sous-Chef, Kitchen, GitHub App, Briefing, Goals surfaces deleted
    - Credits system removed; single ~$29/mo plan with 14-day trial via trialEnd
    - 402 subscription gate on schedule/drafts backend routes
    - Gallery is read-only (SocialCopySection removed)
    - /admin/integrations page with Buffer connect
    - Workspace Default/Custom template toggle
    - Admin sidebar slimmed (6 links removed, Integrations added)
    - Device approval path preserved and verified in code
  </what-built>

  <how-to-verify>
    1. Run `npm run build` — must exit 0 with no errors
    2. Run `npx vitest run` — all tests pass except the pre-existing two GitHub callback redirect failures (documented in STATE.md)
    3. Start the dev server: `npm run dev` + `npx convex dev`
    4. Visit /admin — confirm: no Kitchen link, no Sous-Chef link, no Goals link, no Briefing link. Confirm Integrations link is present.
    5. Visit /admin/integrations — confirm Buffer connect form loads
    6. Visit /admin/history — confirm gallery shows thumbnails + status badges + download; NO copy editing text area or submit button
    7. Visit /admin/account — confirm trial status display (N days left in trial OR subscribe CTA); no credit bar
    8. Visit /admin/account/upgrade — confirm single "Full Plate — $29/mo" plan (no Toast/Plate/Buffet grid)
    9. Open Workspace, go to template picker — confirm Default/Custom toggle is visible; Default tab shows 5 built-ins
    10. Confirm device approval page at /device still loads (not accidentally deleted)
  </how-to-verify>

  <resume-signal>
    Type "approved" if all checks pass, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → /device?code= → admin | Device approval flow completes handshake; code is single-use and time-limited |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21 | Repudiation | creditsRemaining removal — data loss | accept | creditsRemaining was already optional (Plan 08-02); existing documents have the field at whatever value they had. Removing the schema field means Convex will stop returning the field on queries. No operational data is lost — credits are no longer meaningful. |
| T-08-22 | Elevation of Privilege | Device code endpoint survival | mitigate | src/app/api/v1/device/code/route.ts and token/route.ts were NOT in the deletion blast radius; they are Phase 2 endpoints. Verify they still exist after Phase 8. The device/page.tsx also should still exist. |
| T-08-23 | Spoofing | Login ?next= open redirect | accept | login page.tsx ?next= redirect is scoped to same-origin paths only (existing pattern from Phase 2; CONTEXT.md §canonical_refs confirms it redirects to /device?code=). No new redirect surface introduced. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Full phase gate:
```
npx vitest run && npm run build
```

Both must pass (vitest: pre-existing callback failures acceptable; build: exit 0).

Final grep sanity checks:
```
grep -rn "creditsRemaining" src/ convex/ --include="*.ts" --include="*.tsx" | grep -v "__tests__"
grep -rn "launch-mode\|getLaunchMode" src/ --include="*.ts" --include="*.tsx"
grep -rn "kitchen\|sous-chef\|briefing\|triggerEvents" src/app/ --include="*.ts" --include="*.tsx" -l
```

All three must return no output.
</verification>

<success_criteria>
- creditsRemaining field removed from convex/schema.ts entirely
- Device approval path verified in code (device/page.tsx + device-approval.tsx + login ?next= intact)
- CLAUDE.md updated to remove GitHub App section and stale references
- Full test suite passes (pre-existing failures only)
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-10-SUMMARY.md` when done
</output>
