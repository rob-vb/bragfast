---
phase: 08-admin-trim
plan: 06
type: execute
wave: 4
depends_on:
  - 08-04
files_modified:
  - convex/stripe.ts
  - convex/schema.ts
  - src/app/(admin)/admin/account/page.tsx
  - src/app/(admin)/admin/account/upgrade/page.tsx
  - src/app/(admin)/admin/account/upgrade/actions.ts
  - src/components/admin/upsell-modal.tsx
  - convex/userProfiles.ts
  - convex/__tests__/stripe.test.ts
autonomous: true
requirements:
  - ADM-05

must_haves:
  truths:
    - "convex/stripe.ts uses a single STRIPE_PLATE_PRICE_ID; multi-tier price map is deleted"
    - "plan union in schema.ts is collapsed to trial | plate | free with a migration mutation for old plan values"
    - "account/page.tsx shows subscription status (subscribed / trial N days left / trial expired) with no credit bar"
    - "upgrade/page.tsx shows single-plan checkout (not multi-tier grid)"
    - "stripe.test.ts passes with updated single-plan behavior"
  artifacts:
    - path: "convex/stripe.ts"
      provides: "createCheckoutSession uses single STRIPE_PLATE_PRICE_ID; handleSubscriptionChange sets plan to plate; no credits"
    - path: "src/app/(admin)/admin/account/page.tsx"
      provides: "Trial countdown banner when plan=trial; subscribe CTA when expired/free; no CreditBar"
    - path: "convex/migrations.ts"
      provides: "Plan migration mutation mapping old plan strings to plate/free"
  key_links:
    - from: "convex/stripe.ts createCheckoutSession"
      to: "process.env.STRIPE_PLATE_PRICE_ID"
      via: "single price ID lookup"
      pattern: "STRIPE_PLATE_PRICE_ID"
    - from: "src/app/(admin)/admin/account/page.tsx"
      to: "convex/userProfiles.ts getStats"
      via: "trialEnd field in stats return"
      pattern: "trialEnd"
---

<objective>
Billing reshape — single Stripe price, plan union collapse with migration, account page showing trial/subscribed status. This is the last schema-touching plan before Plan 08-10's final field removal.

Purpose: Deliver D-09 (single plan), D-10 (trialEnd-based trial display), and complete the plan union cleanup with a data migration (per RESEARCH.md RQ-7 Pitfall 5). After this plan, the billing surface is clean: one price, two meaningful states (trialing vs subscribed), no credits.

Output: stripe.ts collapsed; plan union migrated; account page shows trial days remaining; upgrade page is single-plan; stripe tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-PATTERNS.md
@.planning/phases/08-admin-trim/08-UI-SPEC.md
</context>

<interfaces>
From convex/stripe.ts (per PATTERNS.md §stripe.ts collapse):

createCheckoutSession target:
- Single: `const priceId = process.env.STRIPE_PLATE_PRICE_ID; if (!priceId) throw new Error("STRIPE_PLATE_PRICE_ID not configured");`

handleSubscriptionChange target:
- `await ctx.db.patch(profile._id, { plan: "plate" });` (no creditsRemaining)

handleSubscriptionDeleted target:
- `await ctx.db.patch(profile._id, { plan: "free" });`

From convex/userProfiles.ts getStats — must add trialEnd to returned object.

From convex/schema.ts — plan union target after migration: `v.union(v.literal("trial"), v.literal("plate"), v.literal("free"))`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse convex/stripe.ts to single price + plan union migration + schema final literals</name>
  <files>
    convex/stripe.ts,
    convex/schema.ts,
    convex/migrations.ts,
    convex/userProfiles.ts,
    convex/__tests__/stripe.test.ts
  </files>

  <read_first>
    - convex/stripe.ts — read the full file; understand the current multi-plan createCheckoutSession, handleSubscriptionChange, handleSubscriptionDeleted, handleInvoicePaid, priceToPlan, priceToTier functions; note PLAN_CREDITS and TIER_CONFIG imports
    - convex/schema.ts — read the current plan union (has starter/pro/scale/toast/buffet/plate/free/trial literals); must be updated AFTER migration mutation is written
    - convex/migrations.ts — read if it exists; check for any existing migration patterns to model the plan migration on
    - convex/userProfiles.ts — find `getStats` query; add `trialEnd` to its return; this is the second modification to this file (first was Plan 08-02's create mutation)
    - convex/__tests__/stripe.test.ts — read; understand existing test structure; update to test single-plan behavior
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-5 and §RQ-7 (Pitfall 5 — plan migration before literal cleanup)
    - .planning/phases/08-admin-trim/08-PATTERNS.md §stripe.ts collapse (exact target shapes)
    - convex/_generated/ai/guidelines.md — Convex internalMutation/internalAction patterns
  </read_first>

  <behavior>
    - stripe.test.ts: handleSubscriptionChange with STRIPE_PLATE_PRICE_ID → patches profile to plan: "plate" with no creditsRemaining
    - stripe.test.ts: handleSubscriptionDeleted → patches profile to plan: "free"
    - stripe.test.ts: createCheckoutSession with planId "plate" → creates Stripe session (no multi-plan lookup)
  </behavior>

  <action>
    Step 1 — Write plan migration mutation in convex/migrations.ts (or a new convex/planMigration.ts if migrations.ts structure doesn't fit):
    Create an `internalMutation` named `migratePlanLiterals` that:
    - Queries all userProfiles via `ctx.db.query("userProfiles").collect()`
    - For each profile: if `plan` is one of "starter", "pro", "scale", "toast", "buffet", "plate" → patch to "plate"; if "trial" → keep "trial"; if "free" → keep "free"
    - Returns a count of migrated rows
    This mutation must be runnable before the schema literal cleanup below.

    Step 2 — Collapse convex/stripe.ts:
    - Replace `priceEnvMap` multi-plan lookup in `createCheckoutSession` with single `process.env.STRIPE_PLATE_PRICE_ID`
    - Delete `priceToPlan`, `priceToTier`, `PLAN_CREDITS` map, and `TIER_CONFIG` import
    - Collapse `handleSubscriptionChange`: remove multi-plan branching; patch `plan: "plate"` only (no creditsRemaining)
    - Collapse `handleSubscriptionDeleted`: patch `plan: "free"` only (no creditsRemaining)
    - Delete or no-op `handleInvoicePaid` (no credits to reset; can be a no-op handler or removed if stripe webhook doesn't call it)

    Step 3 — Update convex/userProfiles.ts getStats: add `trialEnd: profile.trialEnd` to the returned stats object so the account page can display trial days remaining.

    Step 4 — Update convex/schema.ts plan union ONLY AFTER the migration mutation above is ready:
    Collapse to: `v.union(v.literal("trial"), v.literal("plate"), v.literal("free"))`
    Remove old literals: "starter", "pro", "scale", "toast", "buffet".
    NOTE: The migration mutation must be deployed and run before this schema change deploys in production. In local dev, write both in the same plan — the migration can be invoked via `npx convex run` before the schema change takes effect.

    Step 5 — Update convex/__tests__/stripe.test.ts: update test cases to reflect single-plan behavior (handleSubscriptionChange → sets plan to "plate", no credits; handleSubscriptionDeleted → sets plan to "free"). Remove tests for priceToPlan/priceToTier.
  </action>

  <verify>
    <automated>npx vitest run convex/__tests__/stripe.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "priceEnvMap\|priceToPlan\|priceToTier\|PLAN_CREDITS\|TIER_CONFIG" convex/stripe.ts` returns no matches
    - `grep -n "STRIPE_PLATE_PRICE_ID" convex/stripe.ts` matches the single price lookup
    - convex/schema.ts plan union: `grep -A5 "plan:" convex/schema.ts | grep -E "starter|pro|scale|toast|buffet"` returns no matches
    - plan union contains exactly: "trial", "plate", "free"
    - `npx vitest run convex/__tests__/stripe.test.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Account page and upgrade page billing reshape</name>
  <files>
    src/app/(admin)/admin/account/page.tsx,
    src/app/(admin)/admin/account/upgrade/page.tsx,
    src/app/(admin)/admin/account/upgrade/actions.ts,
    src/components/admin/upsell-modal.tsx
  </files>

  <read_first>
    - src/app/(admin)/admin/account/page.tsx — read the full file; find CreditBar component usage, credit stats section, plan display, ManageBillingButton, upgrade link; understand how it fetches user stats
    - src/app/(admin)/admin/account/upgrade/page.tsx — read the full file; understand the multi-tier grid structure (Toast/Plate/Buffet)
    - src/app/(admin)/admin/account/upgrade/actions.ts — read; find createCheckout action; understand how planId is passed
    - src/components/admin/upsell-modal.tsx — read; find TIER_CONFIG usage and multi-tier content (per RESEARCH.md open question 3 — refactor for single-plan subscribe prompt)
    - .planning/phases/08-admin-trim/08-UI-SPEC.md §Account / Billing (remove credit bar, show subscription status, trial countdown)
    - .planning/phases/08-admin-trim/08-UI-SPEC.md §Copywriting (trial active: "{N} days left in your trial"; expired: "Trial ended")
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-5 Account page reshape
  </read_first>

  <action>
    Step 1 — account/page.tsx reshape:
    - Remove the `CreditBar` component import and usage entirely
    - Remove credit stats section (credits remaining display, credits used this month)
    - Add trial status display: read `trialEnd` from the stats query result
      - If plan === "trial" and trialEnd > Date.now(): show "{N} days left in your trial" where N = Math.ceil((trialEnd - Date.now()) / (24 * 60 * 60 * 1000))
      - If plan === "trial" and trialEnd <= Date.now(): show "Trial ended" + Subscribe Now CTA
      - If plan === "free": show Subscribe Now CTA
      - If plan === "plate": show "Subscribed" with ManageBillingButton
    - Keep: ManageBillingButton, profile info, API key links, any other account settings
    - Style: follow UI-SPEC — trial indicator uses Geist Sans 14px; "Subscribe Now" CTA uses PixelButton primary variant (gold background)

    Step 2 — upgrade/page.tsx collapse to single price:
    - Replace multi-tier grid (Toast/Plate/Buffet) with a single plan card: "Full Plate — $29/mo"
    - Include a brief description and a single "Subscribe Now" button that calls the createCheckout action with planId: "plate"
    - Style: PixelCard + PixelButton pattern; Press Start 2P for plan name heading

    Step 3 — upgrade/actions.ts:
    - Simplify createCheckout: it should accept `planId = "plate"` (or hardcode it) and call the Convex createCheckoutSession action. Remove any multi-plan validation.

    Step 4 — upsell-modal.tsx refactor:
    - Remove TIER_CONFIG import and multi-tier content (Toast/Plate/Buffet tier comparison)
    - Refactor to a simple single-plan subscribe prompt: heading "Trial ended", body "Scheduling and sync require a subscription. Local render keeps working.", CTA "Subscribe Now" button linking to /admin/account/upgrade
    - Keep the component name (upsell-modal) and export name — it will be reused in Plan 08-07 for the Workspace 402 prompt as needed

    Run `npm run build` after all changes.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "CreditBar\|creditsRemaining\|TIER_CONFIG\|pricing-data" src/app/(admin)/admin/account/page.tsx` returns no matches
    - account/page.tsx shows trialEnd-based status rendering (grep for "trialEnd" matches)
    - upgrade/page.tsx no longer renders a multi-tier grid: `grep -n "Toast\|Buffet\|TIER" src/app/(admin)/admin/account/upgrade/page.tsx` returns no matches
    - upsell-modal.tsx no longer imports TIER_CONFIG: `grep -n "TIER_CONFIG\|plan-tiers" src/components/admin/upsell-modal.tsx` returns no matches
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Stripe checkout action | createCheckoutSession Convex action accepts planId from client; must not allow arbitrary price IDs |
| admin UI → Convex stats | trialEnd returned to client UI — no sensitive data, just a timestamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | createCheckoutSession — price ID spoofing | mitigate | After collapse, action uses single `process.env.STRIPE_PLATE_PRICE_ID` hardcoded from env; caller passes planId "plate" which must match `=== "plate"` check; no arbitrary price ID passthrough |
| T-08-10 | Information Disclosure | trialEnd timestamp returned to client | accept | trialEnd is a Unix ms timestamp with no sensitive data; client uses it only to compute days remaining for display |
| T-08-11 | Elevation of Privilege | plan migration — mapping active paid plans to "plate" | accept | Migration maps paid plans to plate (same or better entitlement); no privilege escalation; "free" users stay "free" |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
```
npx vitest run convex/__tests__/stripe.test.ts
npm run build
```

stripe tests pass; build exits 0.

Also verify: `grep -rn "STRIPE_STARTER_PRICE_ID\|STRIPE_PRO_PRICE_ID\|STRIPE_SCALE_PRICE_ID\|STRIPE_TOAST_PRICE_ID\|STRIPE_BUFFET_PRICE_ID" src/ convex/ --include="*.ts" --include="*.tsx"` returns no matches (old price IDs no longer referenced in code; env vars can stay set for Stripe webhook compatibility).
</verification>

<success_criteria>
- stripe.ts uses single STRIPE_PLATE_PRICE_ID, no multi-plan branching
- plan union collapsed to trial | plate | free with migration mutation written
- account page shows trial countdown / subscribe CTA based on trialEnd
- upgrade page is single-plan
- stripe tests pass; npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-06-SUMMARY.md` when done
</output>
