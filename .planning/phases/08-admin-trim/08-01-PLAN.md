---
phase: 08-admin-trim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth/__tests__/subscription-gate.test.ts
  - convex/__tests__/userProfiles.test.ts
  - .planning/phases/08-admin-trim/08-VALIDATION.md
autonomous: true
requirements:
  - ADM-05

must_haves:
  truths:
    - "A failing test exists for the 402 subscription gate: expired trial returns 402, active trial returns null, plate plan returns null, free plan returns 402"
    - "A test assertion exists for userProfiles.create setting trialEnd ~14 days in the future"
  artifacts:
    - path: "src/lib/auth/__tests__/subscription-gate.test.ts"
      provides: "Test scaffold for checkSubscriptionGate() — all four cases"
    - path: "convex/__tests__/userProfiles.test.ts"
      provides: "Updated test asserting create mutation sets trialEnd"
  key_links:
    - from: "src/lib/auth/__tests__/subscription-gate.test.ts"
      to: "src/lib/auth/subscription-gate.ts"
      via: "import checkSubscriptionGate"
      pattern: "checkSubscriptionGate"
---

<objective>
Wave 0 test scaffolds for the two new behaviors that require behavior assertions (per VALIDATION.md Wave 0 requirements). Both tests will initially fail (the implementation files do not exist yet) — that is expected and correct. The tests define the contract; implementation comes in later plans.

Purpose: Establish Nyquist-compliant test coverage for ADM-05 before implementation begins. VALIDATION.md wave 0 gaps explicitly lists both files as missing (❌ W0).

Output: Two test files — subscription-gate test covering all four plan/trial combinations, and an updated userProfiles test asserting trialEnd is set on create.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Subscription gate test scaffold</name>
  <files>src/lib/auth/__tests__/subscription-gate.test.ts</files>

  <read_first>
    - src/lib/auth/authenticate.ts — peer function; the gate will sit beside it
    - src/lib/auth/__tests__/ — check if directory exists; check any existing auth test for import/mock patterns
    - convex/__tests__/userProfiles.test.ts — Convex mock pattern used in existing tests
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-5 (gate logic: plan === "free" OR (plan === "trial" AND trialEnd less than Date.now()) → 402)
    - .planning/phases/08-admin-trim/08-PATTERNS.md §subscription-gate.ts (error shape, fetchQuery pattern)
  </read_first>

  <behavior>
    - Test 1: active trial (plan: "trial", trialEnd = Date.now() + 7 days) → checkSubscriptionGate returns null (no gate failure)
    - Test 2: expired trial (plan: "trial", trialEnd = Date.now() - 1 day) → checkSubscriptionGate returns Response with status 402, body { error: "subscription_required" }
    - Test 3: free plan (plan: "free", no trialEnd) → checkSubscriptionGate returns Response with status 402
    - Test 4: subscribed plate plan (plan: "plate", no trialEnd) → checkSubscriptionGate returns null
  </behavior>

  <action>
    Create src/lib/auth/__tests__/subscription-gate.test.ts. The test imports `checkSubscriptionGate` from `@/lib/auth/subscription-gate` (which does not exist yet — the import will fail at test-run time, which is expected in Wave 0).

    Mock `fetchQuery` from `convex/nextjs` and `api` from `@convex/_generated/api` to return controlled userProfile shapes. The mock userProfile shape needs only: `{ plan: string, trialEnd?: number }`.

    For the 402 cases: assert `result` is a `Response` instance, `result.status === 402`, and `await result.json()` contains `{ error: "subscription_required" }`.

    For the null cases: assert `result === null`.

    Use vitest `vi.mock`, `vi.fn()`, `describe`, `it`, `expect` — same pattern as other auth tests in the repo. Do NOT use `beforeEach` reset if a simple per-test mock suffices.

    The file must NOT import anything that does not exist yet (except `subscription-gate.ts` which is the file under test — that import is expected to fail until Plan 08-08 creates it).
  </action>

  <verify>
    <automated>npx vitest run src/lib/auth/__tests__/subscription-gate.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - File exists at src/lib/auth/__tests__/subscription-gate.test.ts
    - Four test cases present covering: active-trial→null, expired-trial→402, free→402, plate→null
    - Running the test file produces a FAIL with "Cannot find module" or "Failed to resolve" for subscription-gate.ts (not a syntax error in the test itself)
    - No TypeScript syntax errors in the test file itself: `npx tsc --noEmit --skipLibCheck 2>&1 | grep subscription-gate` returns nothing about syntax errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: userProfiles trialEnd test assertion</name>
  <files>convex/__tests__/userProfiles.test.ts</files>

  <read_first>
    - convex/__tests__/userProfiles.test.ts — read the entire existing file to understand current test structure, mock patterns, and what `create` mutation tests already exist
    - convex/userProfiles.ts — lines 45-68 (create mutation, per PATTERNS.md) — understand current creditsRemaining insertion
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-3 Layer 2 (create mutation: strip creditsRemaining, set trialEnd: Date.now() + 14 * 24 * 60 * 60 * 1000)
  </read_first>

  <behavior>
    - Test: calling the `create` mutation handler for a new user sets `trialEnd` to approximately `Date.now() + 14 * 24 * 60 * 60 * 1000` (within ±5 seconds tolerance)
    - Test: the created userProfile document does NOT contain a `creditsRemaining` field (or it is undefined/null)
  </behavior>

  <action>
    Open convex/__tests__/userProfiles.test.ts. Add two assertions to the existing `create` mutation test block (or add a new `describe("create — trialEnd")` block if the existing test does not cover create).

    Assertion 1 — trialEnd set correctly: after calling the create handler in the test, assert that the inserted document's `trialEnd` field is within ±5000ms of `Date.now() + 14 * 24 * 60 * 60 * 1000`. Use `Math.abs(insertedDoc.trialEnd - expectedTrialEnd) < 5000`.

    Assertion 2 — creditsRemaining absent: assert `insertedDoc.creditsRemaining === undefined`.

    These assertions will FAIL until Plan 08-02 modifies the create mutation. That is expected and correct.

    Do NOT modify any existing passing tests — only ADD new assertions or a new describe block.
  </action>

  <verify>
    <automated>npx vitest run convex/__tests__/userProfiles.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - File convex/__tests__/userProfiles.test.ts is modified (not replaced)
    - Two new assertions present: trialEnd within tolerance, creditsRemaining absent
    - Running the test either (a) fails on the new trialEnd/creditsRemaining assertions (expected — implementation not yet changed), or (b) passes if existing create behavior already matches. Either outcome is acceptable for Wave 0.
    - No existing tests that were previously passing are now failing (only the new assertions may fail)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mark Wave 0 complete in VALIDATION.md frontmatter</name>
  <files>.planning/phases/08-admin-trim/08-VALIDATION.md</files>

  <read_first>
    - .planning/phases/08-admin-trim/08-VALIDATION.md — read the frontmatter block (top of file); confirm `wave_0_complete: false` currently
  </read_first>

  <action>
    After both Wave 0 test files are confirmed created (Tasks 1 and 2), update the frontmatter of `.planning/phases/08-admin-trim/08-VALIDATION.md`: change `wave_0_complete: false` to `wave_0_complete: true`. Leave `nyquist_compliant: false` unchanged — that flag flips in Plan 08-10 after the full suite passes at phase end.
  </action>

  <verify>
    <automated>grep -n "wave_0_complete:" .planning/phases/08-admin-trim/08-VALIDATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "wave_0_complete: true" .planning/phases/08-admin-trim/08-VALIDATION.md` returns a match
    - `grep "nyquist_compliant: false" .planning/phases/08-admin-trim/08-VALIDATION.md` still returns a match (unchanged)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → module-under-test | Test file imports subscription-gate.ts which does not exist yet — expected failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; vitest already installed |

This plan creates test scaffolds only. No production code, no trust boundaries introduced. Security review deferred to Plan 08-08 (implementation of the gate).
</threat_model>

<verification>
`npx vitest run src/lib/auth/__tests__/subscription-gate.test.ts convex/__tests__/userProfiles.test.ts 2>&1 | tail -30`

Expected: subscription-gate test fails with "Cannot find module" (correct); userProfiles test fails only on the new trialEnd assertions (all pre-existing tests still pass).
</verification>

<success_criteria>
- Both test files exist
- subscription-gate.test.ts: four cases, fails on module-not-found (not syntax error)
- userProfiles.test.ts: two new assertions added, pre-existing assertions unchanged
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-01-SUMMARY.md` when done
</output>
