---
phase: 08-admin-trim
plan: 08
type: execute
wave: 5
depends_on:
  - 08-06
  - 08-07
files_modified:
  - src/lib/auth/subscription-gate.ts
  - src/app/api/v1/schedule/route.ts
  - src/app/api/v1/schedule/upload-url/route.ts
  - src/app/api/v1/drafts/route.ts
  - src/app/api/v1/drafts/[id]/route.ts
autonomous: true
requirements:
  - ADM-05

must_haves:
  truths:
    - "checkSubscriptionGate() returns 402 Response for expired trial or free plan; returns null for active trial or plate subscription (D-11)"
    - "POST /api/v1/schedule is gated; unauthenticated requests still get 401 (not 402)"
    - "POST /api/v1/schedule/upload-url is gated"
    - "POST /api/v1/drafts is gated"
    - "PATCH /api/v1/drafts/[id] is gated"
    - "GET routes and local render are NOT gated"
    - "subscription-gate test from Plan 08-01 passes"
  artifacts:
    - path: "src/lib/auth/subscription-gate.ts"
      provides: "checkSubscriptionGate(userId): Promise<Response | null>"
    - path: "src/app/api/v1/schedule/route.ts"
      provides: "POST handler calls checkSubscriptionGate after authenticate()"
    - path: "src/app/api/v1/drafts/route.ts"
      provides: "POST handler calls checkSubscriptionGate after auth"
  key_links:
    - from: "src/app/api/v1/schedule/route.ts POST"
      to: "src/lib/auth/subscription-gate.ts"
      via: "checkSubscriptionGate(auth.userId)"
      pattern: "checkSubscriptionGate"
    - from: "src/lib/auth/subscription-gate.ts"
      to: "convex/userProfiles.ts getByUserId"
      via: "fetchQuery(api.userProfiles.getByUserId, { userId })"
      pattern: "getByUserId"
---

<objective>
402 subscription gate — creates the `checkSubscriptionGate()` helper and wires it into the four backend routes that the CLI calls through the proxy. This is the enforcement mechanism for D-11 (expired trial or free plan → 402 on backend API; local render keeps working offline).

Purpose: After this plan, the CLI's backend-dependent flows (scheduling, draft save) are gated behind an active trial or plate subscription. The gate runs AFTER authenticate() so unauthenticated requests still get 401, not 402 (RESEARCH.md security finding).

Output: subscription-gate.ts implemented; 4 routes gated; Plan 08-01's test file passes.
</objective>

<decisions>
Implements CONTEXT.md decisions:
- D-11: Expired/unsubscribed trial gates backend API access with 402 — the thin backend rejects the CLI's authenticated calls (schedule, presigned upload, draft save); local render still works offline.
</decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-PATTERNS.md
</context>

<interfaces>
From src/lib/auth/authenticate.ts (PATTERNS.md §subscription-gate — peer function analog):
```typescript
import { fetchQuery } from "convex/nextjs";
import { api } from "@convex/_generated/api";

export async function authenticate(request: Request): Promise<{ userId: string } | null>
```

Gate call site pattern (PATTERNS.md §402 gate enforcement):
```typescript
// After authenticate():
const gate = await checkSubscriptionGate(auth.userId);
if (gate) return gate;
```

Gate logic (RESEARCH.md RQ-5):
- plan === "free" → 402
- plan === "trial" AND trialEnd < Date.now() → 402
- plan === "trial" AND trialEnd >= Date.now() → null (active trial)
- plan === "plate" → null (subscribed)

Error response shape (PATTERNS.md §subscription-gate):
```typescript
return Response.json(
  { error: "subscription_required", message: "Trial expired. Subscribe to continue." },
  { status: 402 }
);
```

Routes to gate (RESEARCH.md RQ-5):
- POST /api/v1/schedule (src/app/api/v1/schedule/route.ts)
- POST /api/v1/schedule/upload-url (src/app/api/v1/schedule/upload-url/route.ts)
- POST /api/v1/drafts (src/app/api/v1/drafts/route.ts)
- PATCH /api/v1/drafts/[id] (src/app/api/v1/drafts/[id]/route.ts)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement subscription-gate.ts</name>
  <files>src/lib/auth/subscription-gate.ts</files>

  <read_first>
    - src/lib/auth/authenticate.ts — read the full file; understand fetchQuery usage, api import, and single-responsibility helper pattern to match
    - src/lib/auth/__tests__/subscription-gate.test.ts — read the test file from Plan 08-01; understand the exact function signature expected, the mock pattern for fetchQuery/api, and the four test cases (active-trial→null, expired-trial→402, free→402, plate→null)
    - convex/userProfiles.ts — find the `getByUserId` query export name and its argument shape ({ userId: string })
    - convex/_generated/ai/guidelines.md — verify fetchQuery usage from Next.js context
    - .planning/phases/08-admin-trim/08-PATTERNS.md §subscription-gate.ts (complete pattern including error shape and query pattern)
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-5 (gate logic: the exact plan/trialEnd condition)
  </read_first>

  <behavior>
    - Active trial (plan: "trial", trialEnd = now + 7d) → returns null
    - Expired trial (plan: "trial", trialEnd = now - 1d) → returns Response with status 402, body { error: "subscription_required" }
    - Free plan (plan: "free") → returns Response with status 402
    - Plate subscription (plan: "plate") → returns null
  </behavior>

  <action>
    Create src/lib/auth/subscription-gate.ts. This is the implementation that makes the Plan 08-01 test pass.

    Export a single async function: `checkSubscriptionGate(userId: string): Promise<Response | null>`

    Implementation:
    1. Import `fetchQuery` from `"convex/nextjs"` and `api` from `"@convex/_generated/api"`
    2. Fetch the user profile: `const profile = await fetchQuery(api.userProfiles.getByUserId, { userId })`
    3. If profile not found: return 402 (treat missing profile as no subscription)
    4. Gate condition: `if (profile.plan === "free") return subscriptionRequired()` and `if (profile.plan === "trial" && profile.trialEnd !== undefined && profile.trialEnd < Date.now()) return subscriptionRequired()`
    5. Otherwise: return null (allow through)

    Helper (inline in the file): `function subscriptionRequired(): Response { return Response.json({ error: "subscription_required", message: "Trial expired. Subscribe to continue." }, { status: 402 }); }`

    The function must NOT touch the request object — it only takes userId (string). The caller already verified auth before calling this.

    Do NOT import Request or NextRequest — the helper is pure business logic over a userId string.
  </action>

  <verify>
    <automated>npx vitest run src/lib/auth/__tests__/subscription-gate.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/lib/auth/__tests__/subscription-gate.test.ts` exits 0 with all 4 cases passing
    - File exports `checkSubscriptionGate` function
    - `grep -n "subscription_required" src/lib/auth/subscription-gate.ts` matches the error shape
    - `npx tsc --noEmit 2>&1 | grep "subscription-gate"` returns no type errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire checkSubscriptionGate into 4 gated routes</name>
  <files>
    src/app/api/v1/schedule/route.ts,
    src/app/api/v1/schedule/upload-url/route.ts,
    src/app/api/v1/drafts/route.ts,
    src/app/api/v1/drafts/[id]/route.ts
  </files>

  <read_first>
    - src/app/api/v1/schedule/route.ts — read the POST handler (lines 260-314 per PATTERNS.md); find the `authenticate(request)` call; this is the insertion point
    - src/app/api/v1/schedule/upload-url/route.ts — read; find the POST handler auth pattern
    - src/app/api/v1/drafts/route.ts — read; find the POST handler auth call (lines 20-26 per PATTERNS.md; uses `resolveAuth` or `authenticate`)
    - src/app/api/v1/drafts/[id]/route.ts — read; find the PATCH handler auth pattern; confirm GET handler is NOT gated
    - .planning/phases/08-admin-trim/08-PATTERNS.md §402 gate enforcement (exact insertion points and import pattern)
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-5 (routes to gate, routes NOT to gate)
  </read_first>

  <action>
    For each of the 4 routes, add exactly two lines after the auth check:

    ```
    const gate = await checkSubscriptionGate(auth.userId);
    if (gate) return gate;
    ```

    And add the import at the top of each file:
    `import { checkSubscriptionGate } from "@/lib/auth/subscription-gate";`

    Detailed insertion points for each route:

    1. src/app/api/v1/schedule/route.ts — POST handler, after `if (!auth) return Response.json({ error: "Unauthorized" }, { status: 401 });` (approximately line 265)

    2. src/app/api/v1/schedule/upload-url/route.ts — POST handler, after the auth check and 401 return

    3. src/app/api/v1/drafts/route.ts — POST handler ONLY. After `if (!auth) return ...` (approximately line 24). Do NOT add gate to the GET handler.

    4. src/app/api/v1/drafts/[id]/route.ts — PATCH handler ONLY. After the auth check. Do NOT add gate to the GET handler.

    CRITICAL: Confirm that the auth check (which returns 401) runs BEFORE the gate check (which returns 402). The gate must always be the second check, never the first.

    Do NOT gate:
    - GET /api/v1/drafts
    - GET /api/v1/drafts/[id]
    - Any GET routes
    - POST /api/v1/upload (local render media upload must work offline)
    - Any cook API routes (dormant stubs)

    After wiring, run `npm run build` to confirm no TS errors.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "checkSubscriptionGate" src/app/api/v1/schedule/route.ts` matches at least one line
    - `grep -n "checkSubscriptionGate" src/app/api/v1/schedule/upload-url/route.ts` matches
    - `grep -n "checkSubscriptionGate" src/app/api/v1/drafts/route.ts` matches
    - `grep -n "checkSubscriptionGate" "src/app/api/v1/drafts/[id]/route.ts"` matches
    - In drafts/route.ts: gate call appears AFTER the 401 auth check and BEFORE any rate limit or payload parsing
    - `npm run build` exits 0
    - `npx vitest run src/lib/auth/__tests__/subscription-gate.test.ts` still exits 0 (no regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → backend API routes | 4 gated routes; unauthenticated requests must 401 before 402 is reachable |
| gate helper → Convex | fetchQuery called server-side in Next.js route handler; userId came from authenticate() — already validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Elevation of Privilege | 402 gate bypass — unauthenticated request getting 402 | mitigate | Gate is called AFTER `authenticate()` which returns 401 for unauth requests. Unauthenticated request → 401 before gate ever runs. Test case: `authenticate()` returns null → 401 returned before checkSubscriptionGate() is called. |
| T-08-16 | Elevation of Privilege | 402 gate bypass — missing trialEnd field | mitigate | Gate treats `profile.trialEnd === undefined` as free plan condition: if plan==="trial" and trialEnd is undefined, the `profile.trialEnd !== undefined && profile.trialEnd < Date.now()` check will NOT gate (existing trial users before Plan 08-02 deploy have no trialEnd). Recommendation: treat undefined trialEnd on trial plan as blocked (expired) to be safe — add `|| profile.trialEnd === undefined` to the expired condition. |
| T-08-17 | Elevation of Privilege | 402 gate missing from upload-url | mitigate | POST /api/v1/schedule/upload-url explicitly included in gate list; verified in acceptance criteria |
| T-08-18 | Availability | Local render broken by gate | mitigate | POST /api/v1/upload is NOT gated (local render media upload); POST /api/local/render is NOT a v1 API route and is not gated; local render works offline as intended |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
```
npx vitest run src/lib/auth/__tests__/subscription-gate.test.ts
npm run build
```

Both must pass. All 4 test cases green; build exits 0.

Security verification: in each gated route file, confirm the auth 401 check appears on a lower line number than the gate call:
```
grep -n "checkSubscriptionGate\|Unauthorized.*401\|status: 401" src/app/api/v1/schedule/route.ts | head -5
```
The 401 line must appear before the checkSubscriptionGate line.
</verification>

<success_criteria>
- subscription-gate.ts implemented and tested (all 4 cases pass)
- 4 routes gated: POST schedule, POST upload-url, POST drafts, PATCH drafts/[id]
- Authentication (401) always runs before subscription check (402)
- GET routes and local render routes are NOT gated
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-08-SUMMARY.md` when done
</output>
