---
phase: 08-admin-trim
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/app/(admin)/admin/kitchen/
  - src/app/(admin)/admin/briefing/
  - src/app/(admin)/admin/sous-chef/
  - src/app/(admin)/admin/report/
  - src/app/(admin)/admin/drafts/page.tsx
  - src/components/kitchen/
  - src/components/admin/briefing-client.tsx
  - src/components/admin/sous-chef-client.tsx
  - src/components/admin/sous-chef-goals-client.tsx
  - src/components/admin/sous-chef-history-feed.tsx
  - src/components/admin/sous-chef-wizard.tsx
  - src/components/admin/sous-chef-goals-section.tsx
  - src/components/admin/goal-create-modal.tsx
  - src/components/admin/goal-hero-card.tsx
  - src/components/admin/goals-section.tsx
  - src/components/admin/pixel-event-card.tsx
  - src/components/admin/github-repo-list.tsx
  - src/components/admin/github-repo-card.tsx
  - src/components/admin/github-section.tsx
  - src/components/admin/drafts-client.tsx
  - src/components/admin/report-client.tsx
  - src/components/admin/routing-defaults-client.tsx
  - src/components/admin/retro-draft-hero.tsx
  - src/components/admin/destination-picker-modal.tsx
  - src/app/api/github/webhooks/route.ts
  - src/app/api/github/configs/route.ts
  - src/app/api/github/repos/
  - src/app/api/github/installations/
  - src/lib/github/pr-merge.ts
  - src/lib/github/retro-pr.ts
  - convex/drafts.ts
  - convex/triggerEvents.ts
  - convex/goals.ts
  - convex/briefings.ts
  - convex/briefingsActions.ts
  - convex/goalEmails.ts
  - convex/triggerDrafting.ts
  - convex/sousChef.ts
  - convex/integrations/githubStars.ts
  - convex/crons.ts
  - src/components/admin/posthog-identifier.tsx
  - src/components/admin/approve-draft-modal.tsx
  - src/app/api/v1/goals/
  - src/app/api/v1/drafts/[id]/approve/
  - src/app/api/v1/drafts/[id]/rewrite-copy/
  - src/lib/drafts/
  - src/lib/safety/
autonomous: true
requirements:
  - ADM-01
  - ADM-03

must_haves:
  truths:
    - "All Kitchen, Sous-Chef, GitHub App, and Briefing pages and components are deleted"
    - "convex/drafts.ts no longer imports triggerEvents; core draft CRUD functions are intact"
    - "convex/triggerEvents.ts, goals.ts, briefings.ts, briefingsActions.ts, goalEmails.ts, triggerDrafting.ts, sousChef.ts are deleted"
    - "GitHub webhook route is deleted; no dangling import in any surviving file"
    - "npm run build exits 0 with no dangling imports after all deletions"
  artifacts:
    - path: "convex/drafts.ts"
      provides: "Core draft CRUD intact; insertTriggerEvent import stripped; PR-merge functions removed"
  key_links:
    - from: "convex/drafts.ts"
      to: "convex/triggerEvents.ts"
      via: "import stripped before deletion"
      pattern: "insertTriggerEvent"
---

<objective>
Mass deletion wave — removes all Sous-Chef automation surfaces, Kitchen cook/release authoring UI, GitHub App UI, PR-merge webhook, and exclusively-owned backend modules. This is a non-additive plan: every task deletes files and verifies the build stays clean.

CRITICAL ordering (RESEARCH.md Pitfall 2): convex/drafts.ts must have its PR-merge-specific functions and triggerEvent import stripped BEFORE convex/triggerEvents.ts is deleted. Task 1 handles this; Task 2 and 3 proceed with deletions after Task 1 succeeds.

Purpose: Clear the ~40-file blast radius (D-01, D-03, D-08) so subsequent plans work in a clean codebase. Satisfies ADM-01 (admin is now login/gallery/brand/keys/billing only — no automation cruft) and ADM-03 (gallery path cleaned of Kitchen dependencies).

Output: Deleted directories/files; clean `npm run build` confirming no dangling imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip PR-merge functions from convex/drafts.ts (must run before triggerEvents deletion)</name>
  <files>convex/drafts.ts</files>

  <read_first>
    - convex/drafts.ts — read the FULL file; identify: import of `insertTriggerEvent` from `./triggerEvents` (line ~10); the PR-merge-specific functions: `insertDraftIfNewAction`, `countRecentPrMergesByRepo`, `findRecentPrMergeForRepo`, `appendPrMergeRollup`, `getRecentApprovedEdits` (lines ~303-499 per RESEARCH.md RQ-1)
    - convex/triggerEvents.ts — glance at exports to confirm which ones drafts.ts imports
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-1 (triggerEvents deletion verdict + drafts.ts dependency detail)
  </read_first>

  <action>
    In convex/drafts.ts:

    1. Delete the import line for `insertTriggerEvent` from `./triggerEvents` (approximately line 10).

    2. Delete the following PR-merge-specific exported functions entirely (they are only called from the webhook route which is being deleted in Task 2):
       - `insertDraftIfNewAction` — the function that calls insertTriggerEvent
       - `countRecentPrMergesByRepo`
       - `findRecentPrMergeForRepo`
       - `appendPrMergeRollup`
       - `getRecentApprovedEdits`

    3. Do NOT touch any other functions in drafts.ts. The core CRUD functions (`listByUser`, `createDraft`, `patchDraft`, `getDraft`, `removeDraft`, and any others) must be preserved intact — the Workspace depends on them.

    After editing, verify the file compiles: run `npx tsc --noEmit` and confirm no new errors are introduced in drafts.ts.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "drafts" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "insertTriggerEvent" convex/drafts.ts` returns no matches
    - `grep -n "triggerEvents" convex/drafts.ts` returns no matches
    - `grep -n "insertDraftIfNewAction" convex/drafts.ts` returns no matches
    - convex/drafts.ts still contains `listByUser`, `createDraft`, `patchDraft`, `getDraft` functions (core CRUD intact)
    - `npx tsc --noEmit 2>&1 | grep "drafts.ts"` shows no new type errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete Convex automation backend (triggerEvents, goals, briefings, sousChef, githubStars, crons update)</name>
  <files>
    convex/triggerEvents.ts,
    convex/goals.ts,
    convex/briefings.ts,
    convex/briefingsActions.ts,
    convex/goalEmails.ts,
    convex/triggerDrafting.ts,
    convex/sousChef.ts,
    convex/integrations/githubStars.ts,
    convex/crons.ts
  </files>

  <read_first>
    - convex/crons.ts — read the full file; identify any cron entries that reference githubStars, triggerDrafting, goalEmails, briefings, or other modules being deleted; those cron entries must be removed
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-1 (exclusivity verdicts for each Convex module)
    - convex/integrations/ — ls to confirm githubStars.ts location; check if any other file in convex/integrations/ is in the deletion scope
  </read_first>

  <action>
    Delete these Convex modules (they are exclusively owned by the automation surface):

    ```
    rm convex/triggerEvents.ts
    rm convex/goals.ts
    rm convex/briefings.ts
    rm convex/briefingsActions.ts
    rm convex/goalEmails.ts
    rm convex/triggerDrafting.ts
    rm convex/sousChef.ts
    rm convex/integrations/githubStars.ts
    ```

    Then open convex/crons.ts and remove any cron job entries that reference the deleted modules (githubStars scanner cron, any briefing/goal/trigger scheduling crons). Keep any crons that reference surviving modules (schedulePush, releases, etc.).

    Also check convex/http.ts — if it registers any HTTP routes for the deleted modules, remove those route registrations. The webhook handler routes for GitHub (pr-merge path) may be wired here.

    After deletions and cron cleanup, run `npm run build` to confirm Convex codegen completes without missing module errors.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - None of the deleted files exist: `ls convex/triggerEvents.ts convex/goals.ts convex/briefings.ts 2>&1` shows "No such file"
    - convex/crons.ts no longer references any deleted module
    - `npm run build` exits 0 — Convex codegen regenerates without errors
    - `npx tsc --noEmit` exits 0 or shows only pre-existing errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete UI pages, components, GitHub API routes, and lib modules</name>
  <files>
    src/app/(admin)/admin/kitchen/,
    src/app/(admin)/admin/briefing/,
    src/app/(admin)/admin/sous-chef/,
    src/app/(admin)/admin/report/,
    src/app/(admin)/admin/drafts/page.tsx,
    src/components/kitchen/,
    src/components/admin/briefing-client.tsx,
    src/components/admin/sous-chef-client.tsx,
    src/components/admin/drafts-client.tsx,
    src/components/admin/report-client.tsx,
    src/components/admin/routing-defaults-client.tsx,
    src/components/admin/pixel-event-card.tsx,
    src/components/admin/github-repo-list.tsx,
    src/components/admin/github-repo-card.tsx,
    src/components/admin/github-section.tsx,
    src/components/admin/retro-draft-hero.tsx,
    src/components/admin/approve-draft-modal.tsx,
    src/components/admin/posthog-identifier.tsx,
    src/app/api/github/webhooks/route.ts,
    src/app/api/github/configs/route.ts,
    src/app/api/github/repos/,
    src/app/api/github/installations/,
    src/lib/github/pr-merge.ts,
    src/lib/github/retro-pr.ts,
    src/app/api/v1/goals/,
    src/app/api/v1/drafts/[id]/approve/,
    src/app/api/v1/drafts/[id]/rewrite-copy/,
    src/lib/drafts/,
    src/lib/safety/
  </files>

  <read_first>
    - src/components/admin/posthog-identifier.tsx — read the file; find the `api.githubInstallations.listByUserId` query (line ~22 per RESEARCH.md RQ-1); this query must be removed before deleting the file, OR remove the file entirely if it serves no other purpose
    - src/app/(admin)/admin/ — ls to identify any additional page under kitchen/, sous-chef/, briefing/ to confirm complete removal
    - src/components/admin/admin-sidebar.tsx — skim for any imports of the components being deleted (sidebar cleanup is Plan 08-05, but must not have dead imports from deleted files after this plan; if sidebar imports deleted components directly, those imports must be removed here to unblock the build)
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-1 (exclusivity verdicts — each file's safe-to-delete confirmation)
    - src/app/(admin)/admin/page.tsx (admin dashboard) — skim for imports of dashboard-sources-widget.tsx or pixel-event-card or any of the deleted components; remove those imports (dashboard full slim happens in Plan 08-05, but dangling imports must not block build)
  </read_first>

  <action>
    Delete all directories and files listed in the files field. Use `rm -rf` for directories.

    Specific ordering to avoid leaving dangling imports:

    1. Before deleting `posthog-identifier.tsx`: check if it is imported by any surviving file (e.g., admin layout). If it is, either remove the import from the layout first, or gut posthog-identifier.tsx to remove the `githubInstallations` query and keep it as a simpler component (remove only the github-installations portion; keep any remaining PostHog identity-setting logic). Check `src/app/(admin)/layout.tsx` or similar for the import.

    2. Before deleting `approve-draft-modal.tsx`: confirm no surviving page imports it (report-client.tsx and cook-page.tsx are both being deleted; confirm nothing else imports it).

    3. Delete all UI pages: the entire `kitchen/`, `briefing/`, `sous-chef/` (with all sub-pages: goals, history, page, routing), and `report/` directories under `src/app/(admin)/admin/`. Also delete `src/app/(admin)/admin/drafts/page.tsx` (but NOT the drafts directory if it contains only page.tsx — if so, also delete the directory).

    4. Delete all listed component files and directories.

    5. Delete GitHub API routes (`webhooks/route.ts`, `configs/route.ts`, `repos/` dir, `installations/` dir).

    6. Delete lib modules: `src/lib/github/pr-merge.ts`, `src/lib/github/retro-pr.ts`, `src/lib/drafts/` (entire dir — compose-copy.ts, pick-template.ts, etc.), `src/lib/safety/` (entire dir — content-filter.ts, etc.), `src/app/api/v1/goals/` (entire dir), `src/app/api/v1/drafts/[id]/approve/route.ts`, `src/app/api/v1/drafts/[id]/rewrite-copy/route.ts`.

    7. After all deletions, also check for and remove any test files for deleted modules: `src/lib/__tests__/credits.test.ts`, `src/lib/__tests__/cook-credits.test.ts`, `src/lib/__tests__/plan-tiers.test.ts`, `src/lib/__tests__/cook-api.test.ts`, `src/lib/__tests__/cook-state.test.ts` if they exist.

    8. Run `npm run build` after all deletions. Fix any remaining dangling imports (TS errors showing "Cannot find module" for deleted files) before declaring done.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `ls src/app/(admin)/admin/kitchen/ 2>&1` returns "No such file or directory"
    - `ls src/app/(admin)/admin/sous-chef/ 2>&1` returns "No such file or directory"
    - `ls src/components/kitchen/ 2>&1` returns "No such file or directory"
    - `ls src/app/api/github/webhooks/ 2>&1` returns "No such file or directory"
    - `grep -r "from.*kitchen" src/ --include="*.ts" --include="*.tsx" -l` returns no files
    - `grep -r "from.*pr-merge" src/ --include="*.ts" --include="*.tsx" -l` returns no files
    - `npm run build` exits 0 with no "Cannot find module" or "Module not found" errors
    - `npx vitest run 2>&1 | grep -i "fail" | head -10` shows no new test failures (pre-existing callback test failures are acceptable per STATE.md)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → webhook endpoint | Webhook route deleted — GitHub will receive 404 for future webhook calls, which is safe |
| Admin UI → deleted Convex fns | Deleted pages no longer accessible; no orphaned authenticated routes remain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Elevation of Privilege | Deleted webhook route | mitigate | Route deletion removes the endpoint entirely; GitHub webhook will 404 — no unauthenticated access path remains for the PR-merge → Haiku draft gen flow |
| T-08-04 | Elevation of Privilege | Orphaned API routes after deletion | mitigate | `npm run build` gate catches dangling imports; TypeScript confirms no surviving file references deleted modules |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all three tasks complete:

```
npm run build
```

Must exit 0 with zero "Module not found" errors. This is the standing regression gate for delete-heavy plans (VALIDATION.md).

Additional spot checks:
```
grep -r "from.*kitchen" src/ --include="*.tsx" -l
grep -r "triggerEvents" convex/ --include="*.ts" -l
grep -r "insertTriggerEvent" convex/ --include="*.ts" -l
```

All must return no files.
</verification>

<success_criteria>
- All Kitchen, Sous-Chef, GitHub App, Briefing, and Goals surfaces deleted from both UI and exclusively-owned backend
- convex/drafts.ts is clean: no triggerEvents import, core CRUD intact
- GitHub webhook route deleted
- npm run build exits 0 — no dangling imports
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-03-SUMMARY.md` when done
</output>
