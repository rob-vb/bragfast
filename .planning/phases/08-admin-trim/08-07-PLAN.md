---
phase: 08-admin-trim
plan: 07
type: execute
wave: 4
depends_on:
  - 08-05
files_modified:
  - src/components/admin/history-table.tsx
  - src/app/(admin)/admin/integrations/page.tsx
  - src/components/admin/integrations-client.tsx
  - src/app/api/v1/sous-chef/integrations/route.ts
autonomous: true
requirements:
  - ADM-02
  - ADM-03

must_haves:
  truths:
    - "history-table.tsx no longer contains SocialCopySection component or its code (lines 79-203)"
    - "Gallery shows thumbnails, status badge (PixelBadge), download button — no copy editing affordance"
    - "New /admin/integrations page exists with Buffer connect/disconnect UI"
    - "integrations route strips githubInstallations fetch"
    - "npm run build exits 0"
  artifacts:
    - path: "src/components/admin/history-table.tsx"
      provides: "Read-only gallery row: thumbnail + PixelBadge status + download; SocialCopySection deleted"
    - path: "src/app/(admin)/admin/integrations/page.tsx"
      provides: "New admin page at /admin/integrations; auth guard + IntegrationsClient"
    - path: "src/components/admin/integrations-client.tsx"
      provides: "Buffer connect/disconnect UI moved from sous-chef-client.tsx"
  key_links:
    - from: "src/components/admin/admin-sidebar.tsx"
      to: "/admin/integrations"
      via: "nav link added in Plan 08-05"
      pattern: "integrations"
    - from: "src/components/admin/integrations-client.tsx"
      to: "/api/v1/sous-chef/integrations"
      via: "fetch GET/POST/DELETE"
      pattern: "sous-chef/integrations"
---

<objective>
Gallery read-only strip (D-13) and Buffer/Provider integration page extraction (D-02). These two tasks share the same wave since they have no file conflicts and both are medium-weight UI modifications.

Purpose: Satisfy ADM-03 (read-only gallery — no SocialCopySection inline editor) and ADM-02 (Buffer connection survives at /admin/integrations after sous-chef-client.tsx deletion). Also strips the githubInstallations lookup from the integrations API route.

Output: history-table.tsx without SocialCopySection; new /admin/integrations page with Buffer connect; integrations route cleaned of dead GitHub lookup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-PATTERNS.md
@.planning/phases/08-admin-trim/08-UI-SPEC.md
</context>

<interfaces>
From src/components/admin/history-table.tsx (CONTEXT.md canonical_refs §Gallery):
- Lines 1-78: read-only gallery row rendering (thumbnail, PixelBadge, download) — KEEP
- Lines 79-203: SocialCopySection component + its edit affordance — DELETE
- Lines 8-22: Release type with status field including "scheduled"

From src/components/admin/sous-chef-client.tsx PostingProviderBlock (PATTERNS.md §integrations-client — analog pattern):
- PixelCard + connect/disconnect structure
- disconnect fetch: DELETE /api/v1/sous-chef/integrations?provider=...
- ConnectDialog from ./integration-forms (survives deletion)

From src/app/(admin)/admin/keys/page.tsx (PATTERNS.md §integrations page.tsx — analog):
- getSessionUser() + redirect("/login") pattern
- PixelCard + heading structure
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Strip SocialCopySection from history-table.tsx (D-13)</name>
  <files>src/components/admin/history-table.tsx</files>

  <read_first>
    - src/components/admin/history-table.tsx — read the FULL file; understand the full component structure; identify SocialCopySection component definition (lines 79-203 per CONTEXT.md); identify any other components or state in the file that depend on SocialCopySection; identify the table row structure that currently conditionally renders SocialCopySection
    - src/components/admin/history-client.tsx — skim; understand how it uses history-table.tsx; confirm history-client.tsx doesn't have edit dependencies
    - .planning/phases/08-admin-trim/08-UI-SPEC.md §Gallery / History (keep: thumbnail, PixelBadge, download button, JSON viewer; remove: SocialCopySection)
    - .planning/phases/08-admin-trim/08-CONTEXT.md §Gallery D-13/D-14 (exact lines to delete)
  </read_first>

  <action>
    In src/components/admin/history-table.tsx:

    1. Delete the `SocialCopySection` component definition entirely (lines 79-203 per CONTEXT.md). This includes the component function itself and any state, types, or helper functions that are exclusively used by SocialCopySection.

    2. Remove any usage of SocialCopySection from the table row/rendering code — wherever it was rendered conditionally or unconditionally in the table rows.

    3. Remove any imports only needed by SocialCopySection (e.g., PATCH fetch calls to `/api/v1/cook/[id]/copy`). Check for the `PATCH /api/v1/cook/[id]/copy` edit affordance specifically — this is the inline social copy editor endpoint to remove.

    4. Keep intact: thumbnail display, PixelBadge status badge (rendered/scheduled/pending/failed — per D-14, no "published" badge), download button, JSON viewer expand/collapse. These are the surviving gallery row affordances.

    5. Also check: the `credits_used` column in the table header/rows (RESEARCH.md §RQ-3 Layer 5 — remove this column from history-table.tsx). Remove the credits_used table column if present.

    After editing, the history-table.tsx must still compile and render a valid read-only gallery row.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "history-table" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "SocialCopySection" src/components/admin/history-table.tsx` returns no matches
    - `grep -n "PATCH.*copy\|api/v1/cook.*copy" src/components/admin/history-table.tsx` returns no matches
    - `grep -n "credits_used" src/components/admin/history-table.tsx` returns no matches
    - The file still contains thumbnail rendering, PixelBadge, and download button code
    - `npx tsc --noEmit 2>&1 | grep "history-table"` shows no errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: New /admin/integrations page + extract Buffer connect UI (D-02) + clean integrations route</name>
  <files>
    src/app/(admin)/admin/integrations/page.tsx,
    src/components/admin/integrations-client.tsx,
    src/app/api/v1/sous-chef/integrations/route.ts
  </files>

  <read_first>
    - src/app/(admin)/admin/keys/page.tsx — read the full file; this is the exact analog for the new integrations/page.tsx (PATTERNS.md §integrations page.tsx)
    - src/components/admin/sous-chef-client.tsx — read lines 161-274 (PostingProviderBlock — the analog for integrations-client.tsx, per PATTERNS.md); also read lines 12-16 for ConnectDialog import pattern; NOTE: this file is deleted in Plan 08-03 but you need to read it NOW if it still exists, or read the excerpt preserved in PATTERNS.md if it's already gone
    - src/components/admin/integration-forms.tsx — skim to understand ConnectDialog export; this file is NOT deleted and contains the connect form the new client will reuse
    - src/app/api/v1/sous-chef/integrations/route.ts — read the full file; find the githubInstallations.listByUserId call (line ~27 per RESEARCH.md RQ-1); find GET/POST/DELETE handlers for integrationSecrets
    - .planning/phases/08-admin-trim/08-PATTERNS.md §integrations-client.tsx (PostingProviderBlock excerpt, disconnect fetch pattern, ConnectDialog usage)
    - .planning/phases/08-admin-trim/08-UI-SPEC.md §/admin/integrations (NES card layout, Buffer not connected copy, disconnect confirmation)
    - .planning/phases/08-admin-trim/08-UI-SPEC.md §Copywriting (Buffer not connected heading/body, Disconnect Buffer confirmation copy)
  </read_first>

  <action>
    Step 1 — Create src/app/(admin)/admin/integrations/page.tsx:
    Model exactly on src/app/(admin)/admin/keys/page.tsx:
    - `import { getSessionUser } from "@/lib/auth/get-session-user"`
    - `import { redirect } from "next/navigation"`
    - `import { IntegrationsClient } from "@/components/admin/integrations-client"`
    - Auth guard: `const user = await getSessionUser(); if (!user) redirect("/login");`
    - Render: heading "Integrations" in Press Start 2P (`font-[family-name:var(--font-press-start)] text-lg text-brand`), then `<IntegrationsClient />`
    - No PixelCard wrapper at page level — IntegrationsClient owns its cards

    Step 2 — Create src/components/admin/integrations-client.tsx:
    This is a `"use client"` component. It renders the Buffer connection section, moved from the deleted sous-chef-client.tsx PostingProviderBlock.

    Structure:
    - Fetch integration status on mount: `GET /api/v1/sous-chef/integrations` → determine if Buffer is connected
    - If connected: show provider name + "Connected" badge + channel list + "Disconnect Buffer" AlertDialog
    - If not connected: show "Buffer not connected" heading, body copy "Connect your Buffer account to schedule posts directly from the Workspace.", ConnectDialog (import from `./integration-forms`)
    - Disconnect: `DELETE /api/v1/sous-chef/integrations?provider=buffer` → reload
    - AlertDialog for disconnect confirmation: "Disconnect Buffer?" / "Scheduled posts already sent won't be affected." / Confirm: "Disconnect" (danger variant) / Cancel: "Stay Connected"
    - Loading state: pixel-skeleton or loading indicator while fetching
    - Error state: "Something went wrong. Try again or reload the page." (UI-SPEC copy)

    Follow the PixelCard + PixelButton + Press Start 2P heading pattern from PATTERNS.md §shared patterns.

    Step 3 — Clean src/app/api/v1/sous-chef/integrations/route.ts:
    - Read the route file; find the `api.githubInstallations.listByUserId` call (line ~27)
    - Remove that query call and any related GitHub installation data from the route response
    - The route must still handle GET/POST/DELETE for integrationSecrets correctly
    - The GET response should include integration secrets without the github installation context
    - Remove any import of `githubInstallations` from the route

    Run `npm run build` after all three changes.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `ls src/app/(admin)/admin/integrations/page.tsx` confirms file exists
    - `ls src/components/admin/integrations-client.tsx` confirms file exists
    - `grep -n "githubInstallations" src/app/api/v1/sous-chef/integrations/route.ts` returns no matches
    - integrations-client.tsx contains: `fetch("/api/v1/sous-chef/integrations")` and `AlertDialog`
    - integrations/page.tsx contains: `getSessionUser`, `redirect("/login")`, `IntegrationsClient`
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → integrations route | GET/POST/DELETE /api/v1/sous-chef/integrations; must require session auth |
| integrations-client → API | Buffer API key stored server-side via sealed credentials; client never sees raw key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Information Disclosure | Buffer API key exposure | mitigate | integrations route stores/retrieves via `convex/integrationSecrets.ts` + `src/lib/crypto/secret-box.ts` (sealed, server-side); new integrations-client.tsx calls the same route — key never returned to browser, only "connected/not-connected" status |
| T-08-13 | Elevation of Privilege | Integrations page auth | mitigate | integrations/page.tsx uses `getSessionUser() → redirect("/login")` guard matching all other admin pages; route also validates session |
| T-08-14 | Tampering | SocialCopySection removal from gallery | accept | Gallery was already read-only from history-client.tsx perspective; removing the edit affordance closes the PATCH /cook/[id]/copy path in the UI (route itself stays as a stub) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
```
npm run build
```

Must exit 0.

Manual check (post-deploy): Visit /admin/history — confirm no copy editing affordance is present. Visit /admin/integrations — confirm Buffer connect form loads.
</verification>

<success_criteria>
- SocialCopySection removed from history-table.tsx; gallery is display-only
- /admin/integrations page exists with Buffer connect/disconnect UI
- integrations API route has no githubInstallations dependency
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-07-SUMMARY.md` when done
</output>
