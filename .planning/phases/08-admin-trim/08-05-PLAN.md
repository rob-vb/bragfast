---
phase: 08-admin-trim
plan: 05
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - src/lib/launch-mode.ts
  - src/app/page.tsx
  - src/app/welcome/pick-repo/pick-repo-client.tsx
  - src/app/welcome/install-warning/page.tsx
  - src/components/admin/dashboard-client.tsx
  - src/app/api/github/__tests__/callback.test.ts
  - docs/conventions.md
  - src/components/admin/admin-sidebar.tsx
  - src/app/(admin)/admin/page.tsx
autonomous: true
requirements:
  - ADM-01
  - ADM-03

must_haves:
  truths:
    - "src/lib/launch-mode.ts is deleted and no file in src/ imports from it (D-07)"
    - "All 5 consumers of launch-mode are collapsed to their repositioned branch with no legacy dead code"
    - "Admin sidebar has Kitchen/Drafts/Briefing/Goals/Activity/Settings links removed; Integrations link added"
    - "Admin dashboard page strips the Sous-Chef activity widget; stats + recent Creations remain (D-06)"
    - "npm run build exits 0"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "data-launch-mode attribute removed; repositioned homepage content kept"
    - path: "src/components/admin/admin-sidebar.tsx"
      provides: "Nav trimmed: 6 links removed, 1 added (Integrations → /admin/integrations)"
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Dashboard slim: activity widget stripped, stats + recent Creations kept"
  key_links:
    - from: "src/components/admin/admin-sidebar.tsx"
      to: "/admin/integrations"
      via: "new nav link"
      pattern: "integrations"
---

<objective>
Launch-mode collapse (D-07) and admin nav/dashboard trim (D-03, D-06). Two independent concerns in one plan since they share no file conflicts and both are pure modification/deletion tasks with no new functionality.

Purpose: Remove the NEXT_PUBLIC_LAUNCH_MODE runtime flag and collapse all 5 consumers to their repositioned branch. Simultaneously slim the admin sidebar (remove 6 stale links, add Integrations) and strip the Sous-Chef activity widget from the dashboard. Satisfies ADM-01 (clean admin navigation) and ADM-03 (no creation/editing UI in admin).

Output: launch-mode.ts deleted; 5 consumers cleaned; sidebar updated; dashboard slimmed; build green.
</objective>

<decisions>
Implements CONTEXT.md decisions:
- D-06: `/admin` becomes a slim dashboard — strip the Sous-Chef activity widget; keep stats + recent Creations.
- D-07: Delete + drop legacy mode entirely — hard-delete legacy files, remove `NEXT_PUBLIC_LAUNCH_MODE` + `src/lib/launch-mode.ts`, collapse all 5 consumers to the repositioned branch.
</decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-PATTERNS.md
@.planning/phases/08-admin-trim/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete launch-mode.ts and collapse all 5 consumers to repositioned branch</name>
  <files>
    src/lib/launch-mode.ts,
    src/app/page.tsx,
    src/app/welcome/pick-repo/pick-repo-client.tsx,
    src/app/welcome/install-warning/page.tsx,
    src/components/admin/dashboard-client.tsx,
    src/app/api/github/__tests__/callback.test.ts,
    docs/conventions.md
  </files>

  <read_first>
    - src/lib/launch-mode.ts — read to understand getLaunchMode() and isLaunchModeRepositioned() exports
    - src/app/page.tsx — read; find getLaunchMode() call and data-launch-mode attribute (lines 11, 45 per RESEARCH.md RQ-4); keep the repositioned content
    - src/app/welcome/pick-repo/pick-repo-client.tsx — read; find isLaunchModeRepositioned() at lines 5, 84; repositioned branch keeps `next = "/welcome/brand"`
    - src/app/welcome/install-warning/page.tsx — read; find isLaunchModeRepositioned() at lines 3, 16; repositioned branch keeps `skipPath = "/welcome/brand"`
    - src/components/admin/dashboard-client.tsx — read; find isLaunchModeRepositioned() at lines 13, 38; repositioned branch is the new dashboard; drop legacy branch
    - src/app/api/github/__tests__/callback.test.ts — read; find vi.mock("@/lib/launch-mode", ...) at lines 7, 8, 26, 30; remove mock entirely, keep repositioned-only test behavior
    - docs/conventions.md — skim for "Launch mode flag" section to remove/update
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-4 (exact table: file → usage → repositioned branch to keep)
  </read_first>

  <action>
    For each of the 5 consumers (RESEARCH.md RQ-4 table):

    1. src/app/page.tsx (lines 11, 45):
       - Remove the `getLaunchMode()` import from `@/lib/launch-mode`
       - Remove the `data-launch-mode={...}` attribute from the root element
       - Keep all other repositioned homepage content intact
       - Do NOT remove any visible page content

    2. src/app/welcome/pick-repo/pick-repo-client.tsx (lines 5, 84):
       - Remove the `isLaunchModeRepositioned()` import
       - Replace the conditional `next = isLaunchModeRepositioned() ? "/welcome/brand" : "/other"` with the hardcoded repositioned value: `next = "/welcome/brand"`
       - Delete the else branch / legacy path entirely

    3. src/app/welcome/install-warning/page.tsx (lines 3, 16):
       - Remove the `isLaunchModeRepositioned()` import
       - Replace conditional `skipPath = isLaunchModeRepositioned() ? "/welcome/brand" : "/other"` with: `skipPath = "/welcome/brand"`
       - Delete the legacy branch

    4. src/components/admin/dashboard-client.tsx (lines 13, 38):
       - Remove `isLaunchModeRepositioned()` import
       - Replace the conditional render with only the repositioned dashboard arm
       - Delete the legacy dashboard branch entirely (the Sous-Chef activity branch)

    5. src/app/api/github/__tests__/callback.test.ts (lines 7, 8, 26, 30):
       - Remove the `vi.mock("@/lib/launch-mode", ...)` call and any related mock setup
       - Keep the repositioned-only test behavior; update any assertions that previously tested legacy redirect paths (the STATE.md notes existing failures for this file — do not make the failure worse, but do remove the launch-mode mock)

    Then delete src/lib/launch-mode.ts.

    Finally, open docs/conventions.md, find the "Launch mode flag" section, and remove that section entirely (or replace with a one-line note: "Launch mode flag removed in Phase 8 — the product is the repositioned experience.").

    Verify no remaining imports: `grep -r "launch-mode" src/ --include="*.ts" --include="*.tsx" -l` must return nothing.
  </action>

  <verify>
    <automated>grep -r "launch-mode\|getLaunchMode\|isLaunchModeRepositioned\|NEXT_PUBLIC_LAUNCH_MODE" src/ --include="*.ts" --include="*.tsx" -l 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `ls src/lib/launch-mode.ts 2>&1` returns "No such file or directory"
    - `grep -r "launch-mode\|getLaunchMode\|isLaunchModeRepositioned" src/ --include="*.ts" --include="*.tsx"` returns no matches
    - `grep -r "NEXT_PUBLIC_LAUNCH_MODE" src/ --include="*.ts" --include="*.tsx"` returns no matches
    - `npx tsc --noEmit 2>&1 | grep "launch-mode"` returns no errors
    - Each consumer file no longer has a launch-mode import and the legacy branch is gone
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Admin sidebar nav trim + dashboard slim (D-06)</name>
  <files>
    src/components/admin/admin-sidebar.tsx,
    src/app/(admin)/admin/page.tsx
  </files>

  <read_first>
    - src/components/admin/admin-sidebar.tsx — read the full file; identify lines 61-82 (nav groups) and lines 312-339 (footer); find the nav items for: Kitchen, Drafts, Briefing, Goals, Activity, Settings (Sous-Chef) — these are removed. Find the pattern for nav item rendering to add Integrations.
    - src/app/(admin)/admin/page.tsx — read the full file; identify the dashboard component structure; find the Sous-Chef activity widget import and usage (likely dashboard-sources-widget.tsx or similar); find stats row and recent Creations list
    - src/components/admin/dashboard-client.tsx — skim; this is the client component for the dashboard; understand what it renders after Task 1 already removed the legacy branch
    - .planning/phases/08-admin-trim/08-UI-SPEC.md §Admin Sidebar and §/admin Dashboard (nav changes, dashboard focal point: stats row full-width top, recent Creations below)
    - .planning/phases/08-admin-trim/08-CONTEXT.md §canonical_refs §Device approval + nav (sidebar lines cited)
  </read_first>

  <action>
    Step 1 — admin-sidebar.tsx nav trim:
    Remove the nav link entries for these pages (find by their label or href patterns):
    - Kitchen (href contains "kitchen")
    - Drafts (href contains "drafts")
    - Briefing (href contains "briefing")
    - Goals (href contains "goals" or "sous-chef/goals")
    - Activity / History feed (any activity log link; not the "History" gallery link — keep History/gallery)
    - Settings / Sous-Chef (the sous-chef main settings page link)

    Add a new nav link: label "Integrations", href "/admin/integrations". Match the exact same nav link component/pattern used by other links in the sidebar (same className, same icon pattern — use an appropriate lucide-react icon, e.g., `Plug` or `Link2`).

    Keep these nav links: Templates, Brands, History (gallery), API Keys, Account.

    Step 2 — admin dashboard page.tsx slim:
    Read the current dashboard page. Identify and remove the Sous-Chef activity widget component (likely `DashboardSourcesWidget` or `PixelEventCard`-based activity feed) from the render. Remove its import.

    Keep: the stats row (PixelCard stat blocks), the recent Creations list section.

    Per UI-SPEC: stats row is the primary focal point — full-width at top. Recent Creations below. Two-column at lg breakpoint. Exact number of stat cards is at implementer discretion.

    After trimming, the dashboard must compile and render without the deleted widget. If dashboard-sources-widget.tsx was already deleted in Plan 08-03 (it may reference deleted components), verify the import is gone. If it still exists and only references surviving modules, it may stay but should not be imported in the dashboard.

    Run `npm run build` after both changes.
  </action>

  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "kitchen\|drafts\|briefing\|goals\|sous-chef\|activity" src/components/admin/admin-sidebar.tsx | grep -i "href"` returns no matches for the removed routes
    - `grep -n "integrations" src/components/admin/admin-sidebar.tsx` returns the new nav link entry
    - src/app/(admin)/admin/page.tsx no longer imports any deleted widget (no "Cannot find module" in build)
    - `npm run build` exits 0
    - `npx vitest run 2>&1 | grep -E "FAIL|failed" | head -5` shows no new failures beyond pre-existing ones
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin nav | Navigation links are purely client-side routing; removed links simply don't appear — no server-side access control change needed since the deleted pages are already gone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | NEXT_PUBLIC_LAUNCH_MODE env var removal | accept | Env var removal is a deployment step; removing from code first is safe. Old deployments with the var will simply ignore it once code no longer reads it. |
| T-08-08 | Elevation of Privilege | Nav links to deleted pages | accept | Deleted pages (Kitchen, Sous-Chef, etc.) were removed in Plan 08-03. Removing their nav links is cosmetic. Even if a user navigated directly to the URL, Next.js would return 404. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
```
grep -r "launch-mode\|getLaunchMode\|isLaunchModeRepositioned" src/ --include="*.ts" --include="*.tsx"
npm run build
```

First command returns nothing. Second exits 0.
</verification>

<success_criteria>
- launch-mode.ts deleted; all 5 consumers collapsed to repositioned branch
- admin-sidebar: 6 links removed, Integrations link added
- dashboard: activity widget removed, stats + recent Creations kept
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-05-SUMMARY.md` when done
</output>
