---
phase: 08-admin-trim
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - convex/schema.ts
  - convex/userProfiles.ts
autonomous: true
requirements:
  - ADM-05

must_haves:
  truths:
    - "The userProfiles table has a trialEnd optional number field in the schema"
    - "The creditsRemaining field is optional (not required) in the schema — safe for deploy before code removal"
    - "userProfiles.create mutation sets trialEnd to Date.now() + 14 * 24 * 60 * 60 * 1000 and no longer sets creditsRemaining (D-10)"
  artifacts:
    - path: "convex/schema.ts"
      provides: "userProfiles table with trialEnd: v.optional(v.number()) added and creditsRemaining changed to v.optional(v.number())"
    - path: "convex/userProfiles.ts"
      provides: "create mutation sets trialEnd, strips creditsRemaining"
  key_links:
    - from: "convex/userProfiles.ts create mutation"
      to: "convex/schema.ts userProfiles.trialEnd"
      via: "ctx.db.insert with trialEnd field"
      pattern: "trialEnd"
---

<objective>
Convex deploy step 1 of 2 for the credits → trial migration. This plan makes two schema changes that are safe to deploy together: (a) makes `creditsRemaining` optional so existing code still works during the transition window, and (b) adds `trialEnd` as an optional number field. It also updates the `userProfiles.create` mutation to set `trialEnd` and stop setting `creditsRemaining`.

CRITICAL: This is Deploy 1 of the 2-deploy credits teardown sequence (RESEARCH.md RQ-7, Pitfall 1). Deploy 2 (removing `creditsRemaining` from schema entirely) happens in Plan 08-10 after all code references are stripped.

Purpose: Unblocks Plan 08-04 (credits teardown) by making creditsRemaining optional first. Sets trialEnd so new signups get a trial immediately after this deploy.

Output: Modified convex/schema.ts and convex/userProfiles.ts; npx vitest run convex/__tests__/userProfiles.test.ts passes the new trialEnd assertions.
</objective>

<decisions>
Implements CONTEXT.md decisions:
- D-10: Card-less app-tracked 14-day trial — `userProfiles.create` sets `trialEnd = now + 14d`, no Stripe `trial_period_days`, no card up front.
</decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-trim/08-CONTEXT.md
@.planning/phases/08-admin-trim/08-RESEARCH.md
@.planning/phases/08-admin-trim/08-PATTERNS.md
</context>

<interfaces>
From convex/schema.ts — current userProfiles table (per PATTERNS.md lines 88-107):
```
userProfiles: defineTable({
  userId: v.string(),
  email: v.optional(v.string()),
  creditsRemaining: v.number(),  // must become v.optional(v.number())
  plan: v.union(
    v.literal("trial"), v.literal("starter"), v.literal("pro"),
    v.literal("scale"), v.literal("free"), v.literal("toast"),
    v.literal("plate"), v.literal("buffet")
  ),
  lastDraftsVisitAt: v.optional(v.number()),
  lastBriefingVisitAt: v.optional(v.number()),
  // ... other optional fields
})
```

From convex/userProfiles.ts — create mutation insertion (PATTERNS.md lines 147-157):
```typescript
return ctx.db.insert("userProfiles", {
  userId,
  email,
  creditsRemaining: previous ? 0 : 30,  // strip this line
  plan: "trial",
  // add: trialEnd: Date.now() + 14 * 24 * 60 * 60 * 1000
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Schema — make creditsRemaining optional + add trialEnd</name>
  <files>convex/schema.ts</files>

  <read_first>
    - convex/schema.ts — read the full file; locate userProfiles table definition; find creditsRemaining field and its exact current form (v.number())
    - convex/_generated/ai/guidelines.md — Convex schema validator syntax to confirm v.optional(v.number()) is correct
    - .planning/phases/08-admin-trim/08-PATTERNS.md §schema (the two-deploy sequence and current schema excerpt)
  </read_first>

  <action>
    In convex/schema.ts, find the `userProfiles` defineTable block. Make exactly two changes:

    1. Change `creditsRemaining: v.number()` to `creditsRemaining: v.optional(v.number())`. Do NOT remove the field yet — only make it optional.

    2. Add `trialEnd: v.optional(v.number())` as a new field in the userProfiles table. Place it directly after the `plan` field union for readability.

    Do NOT change the plan union literals yet — keep all existing literals ("trial", "starter", "pro", "scale", "free", "toast", "plate", "buffet"). Plan union cleanup happens in Plan 08-10.

    Do NOT modify any other table in schema.ts.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - convex/schema.ts userProfiles table contains `creditsRemaining: v.optional(v.number())`
    - convex/schema.ts userProfiles table contains `trialEnd: v.optional(v.number())`
    - All existing plan union literals remain present (no schema regression)
    - `npx tsc --noEmit` exits 0 (or any errors are pre-existing, not caused by this change)
    - `grep -n "creditsRemaining" convex/schema.ts` shows `v.optional(v.number())` not `v.number()`
    - `grep -n "trialEnd" convex/schema.ts` shows the new field
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: userProfiles.create — set trialEnd, strip creditsRemaining</name>
  <files>convex/userProfiles.ts</files>

  <read_first>
    - convex/userProfiles.ts — read the full file; locate the `create` mutation (lines 45-68 per PATTERNS.md); also locate `getBalance` (line 70), `reserve` (line 83), `refund` (line 207), and `getStats` (line ~130) — these are noted but NOT removed here (removed in Plan 08-04 credits teardown)
    - convex/__tests__/userProfiles.test.ts — the test added in Plan 08-01 asserting trialEnd ~14 days and creditsRemaining absent
    - .planning/phases/08-admin-trim/08-PATTERNS.md §userProfiles create mutation (exact line numbers and target shape)
    - .planning/phases/08-admin-trim/08-RESEARCH.md §RQ-3 Layer 2 (create mutation changes only)
  </read_first>

  <behavior>
    - After modification: calling create with { userId, email } inserts a document with trialEnd = Date.now() + 14 * 24 * 60 * 60 * 1000 (within test tolerance)
    - After modification: the inserted document does NOT contain creditsRemaining
  </behavior>

  <action>
    In convex/userProfiles.ts, find the `create` mutation's `ctx.db.insert("userProfiles", {...})` call. Make exactly two changes to the object passed to insert:

    1. Remove the line `creditsRemaining: previous ? 0 : 30` entirely.
    2. Add `trialEnd: Date.now() + 14 * 24 * 60 * 60 * 1000` as a new field in the insert object.

    Do NOT touch any other function in userProfiles.ts. `getBalance`, `reserve`, `refund`, and `getStats` are removed in Plan 08-04. This plan touches only the `create` mutation insert.

    After the change, verify the test from Plan 08-01 passes: the new trialEnd assertions should now be green.
  </action>

  <verify>
    <automated>npx vitest run convex/__tests__/userProfiles.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run convex/__tests__/userProfiles.test.ts` exits 0 with the trialEnd assertion passing
    - creditsRemaining assertion (undefined) also passes
    - No other previously passing tests in this file now fail
    - `grep -n "creditsRemaining" convex/userProfiles.ts` does NOT match any line inside the `create` mutation's insert call
    - `grep -n "trialEnd" convex/userProfiles.ts` matches the new insert field
    - `npm run build` exits 0 (Convex codegen + Next.js build — confirms no TS errors introduced)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Convex mutation | userProfiles.create is called by Better Auth signup flow; trialEnd is server-set, not client-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | userProfiles.create — trialEnd field | mitigate | trialEnd is computed as `Date.now() + 14 * 24 * 60 * 60 * 1000` server-side in the Convex mutation; the create mutation args are `{ userId: v.string(), email: v.string() }` — no trialEnd in args, client cannot supply it |
| T-08-02 | Information Disclosure | plan field during migration window | accept | creditsRemaining is now optional; reads return undefined for new rows — no sensitive data exposed |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
```
npm run build
```
Must exit 0. Convex codegen will regenerate `_generated/` reflecting the new schema fields. Next.js build must pass with no TS errors.

Also run: `npx vitest run convex/__tests__/userProfiles.test.ts` — must exit 0 with trialEnd + creditsRemaining assertions green.
</verification>

<success_criteria>
- convex/schema.ts: creditsRemaining is optional, trialEnd is present as optional number
- convex/userProfiles.ts: create mutation sets trialEnd, does not set creditsRemaining
- npm run build exits 0
- userProfiles tests pass including new trialEnd assertions
</success_criteria>

<output>
Create `.planning/phases/08-admin-trim/08-02-SUMMARY.md` when done
</output>
