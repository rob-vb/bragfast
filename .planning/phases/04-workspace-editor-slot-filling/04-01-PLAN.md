---
phase: 04-workspace-editor-slot-filling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/workspace/package.json
  - packages/workspace/vite.config.ts
  - packages/workspace/src/index.css
  - packages/workspace/vitest.config.ts
  - packages/render-core/package.json
  - packages/render-core/tsup.config.ts
  - packages/render-core/src/browser.ts
  - packages/render-core/src/canvas-defaults.ts
  - packages/render-core/src/index.ts
  - src/lib/templates/canvas-defaults.ts
  - src/lib/templates/default-configs.ts
  - src/components/admin/draft-preview.tsx
  - src/lib/__tests__/carousel-pipeline.test.ts
  - src/lib/__tests__/preview-sample.test.ts
  - src/lib/drafts/types.ts
  - src/lib/drafts/validate.ts
  - src/lib/drafts/__tests__/validate.test.ts
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-05
  - WORK-06
must_haves:
  truths:
    - "D-11 covered: DraftConfig includes optional caption and validate.ts accepts caption strings for create and patch while rejecting non-string caption"
    - "D-15 covered: the 5 built-in templates live in packages/render-core/src/canvas-defaults.ts and Next.js imports are repointed to the shared source"
    - "D-02 and D-03 enabled: packages/workspace can import browser-safe CanvasRenderer and canvas types without pulling node-only fonts.ts into Vite"
    - "UI-SPEC covered: packages/workspace uses Tailwind v4 tokens for forest/off-white/sage/charcoal/lime and does not add shadcn"
    - "Research A1/A2 checkpoint covered: verify @types/multer and @tailwindcss/vite peer compatibility before dependency install"
  artifacts:
    - path: "packages/render-core/src/browser.ts"
      provides: "Browser-safe render-core barrel"
      contains: "CanvasRenderer"
    - path: "packages/render-core/src/canvas-defaults.ts"
      provides: "Shared CANVAS_DEFAULTS source for the 5 built-ins"
      contains: "CANVAS_DEFAULTS"
    - path: "src/lib/drafts/types.ts"
      provides: "DraftConfig with caption field"
      contains: "caption?: string"
    - path: "packages/workspace/vitest.config.ts"
      provides: "Workspace jsdom test config"
      contains: "environment"
user_setup:
  - "Before installing dependencies, run `npm view @tailwindcss/vite peerDependencies` and `npm view @types/multer version`; if Tailwind's Vite peer range excludes Vite 8, record the result and use the PostCSS fallback instead of forcing a broken plugin install."
---

<objective>
Prepare the shared contracts and test infrastructure that the Workspace editor plans depend on: Tailwind v4 workspace styling, workspace vitest, browser-safe render-core exports, shared built-in templates, and backend draft caption validation.

This plan intentionally owns the dependency checkpoint. It may run `npm install tailwindcss @tailwindcss/vite --workspace=packages/workspace`, `npm install multer --workspace=packages/cli`, and `npm install --save-dev @types/multer --workspace=packages/cli` only after the package checks in `user_setup` are recorded in the summary.
</objective>

<context>
@.planning/phases/04-workspace-editor-slot-filling/04-CONTEXT.md
@.planning/phases/04-workspace-editor-slot-filling/04-RESEARCH.md
@.planning/phases/04-workspace-editor-slot-filling/04-UI-SPEC.md
@.planning/phases/04-workspace-editor-slot-filling/04-PATTERNS.md
@.planning/phases/04-workspace-editor-slot-filling/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace Tailwind and vitest infrastructure</name>
  <files>
    packages/workspace/package.json,
    packages/workspace/vite.config.ts,
    packages/workspace/src/index.css,
    packages/workspace/src/main.tsx,
    packages/workspace/vitest.config.ts
  </files>
  <read_first>
    - packages/workspace/package.json — current dependencies/scripts
    - packages/workspace/vite.config.ts — current Vite config
    - packages/workspace/src/main.tsx — CSS import location
    - .planning/phases/04-workspace-editor-slot-filling/04-UI-SPEC.md — Design System, Typography, Color, Spacing Scale
    - .planning/phases/04-workspace-editor-slot-filling/04-RESEARCH.md — Pattern 8 and Open Question A2
  </read_first>
  <action>
    Verify `@tailwindcss/vite` peer dependencies first. If compatible with Vite 8, install `tailwindcss` and `@tailwindcss/vite` in `packages/workspace`; otherwise use the documented PostCSS fallback and record the choice.

    Update `packages/workspace/vite.config.ts` to include Tailwind and keep existing `base: "/"` and `build.outDir: "dist"`.

    Add `packages/workspace/src/index.css` containing `@import "tailwindcss";` plus CSS custom properties for the UI-SPEC values: `--workspace-bg: #F7F6F2`, `--workspace-surface: #FFFFFF`, `--workspace-forest: #1F3D3A`, `--workspace-lime: #87FF5C`, `--workspace-sage: #6F8F88`, `--workspace-ink: #1A1A1A`, `--workspace-border: rgba(26, 26, 26, 0.12)`, and `--workspace-muted: rgba(26, 26, 26, 0.62)`. Import this CSS from `main.tsx`.

    Add `packages/workspace/vitest.config.ts` with jsdom environment and React plugin so workspace hooks/components can be tested without watch mode.
  </action>
  <verify>
    <automated>npm run build --workspace=packages/workspace</automated>
  </verify>
  <acceptance_criteria>
    - packages/workspace/vite.config.ts imports Tailwind integration or documented PostCSS fallback and still sets base "/" and build.outDir "dist"
    - packages/workspace/src/index.css contains `@import "tailwindcss"` and all UI-SPEC color token values listed above
    - packages/workspace/src/main.tsx imports "./index.css"
    - packages/workspace/vitest.config.ts sets test.environment to "jsdom"
    - packages/workspace/package.json does not add shadcn, radix-ui, or workspace component registry dependencies
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create browser-safe render-core entry and move built-in templates</name>
  <files>
    packages/render-core/package.json,
    packages/render-core/tsup.config.ts,
    packages/render-core/src/browser.ts,
    packages/render-core/src/canvas-defaults.ts,
    packages/render-core/src/index.ts,
    src/lib/templates/canvas-defaults.ts,
    src/lib/templates/default-configs.ts,
    src/components/admin/draft-preview.tsx,
    src/lib/__tests__/carousel-pipeline.test.ts,
    src/lib/__tests__/preview-sample.test.ts
  </files>
  <read_first>
    - src/lib/templates/canvas-defaults.ts — pure built-in template data to move
    - packages/render-core/src/index.ts — current node-capable exports
    - packages/render-core/src/canvas-renderer.tsx — browser-safe exports
    - packages/render-core/src/fonts.ts — node-only imports that browser entry must omit
    - packages/render-core/tsup.config.ts — current single entry config
    - .planning/phases/04-workspace-editor-slot-filling/04-RESEARCH.md — Pattern 2 and Pattern 7
  </read_first>
  <action>
    Move the built-in template data into `packages/render-core/src/canvas-defaults.ts`. Export `CANVAS_DEFAULTS` from a new `packages/render-core/src/browser.ts` along with `CanvasRenderer`, `renderObject`, `FORMAT_DIMENSIONS`, `migrateConfig`, and the relevant canvas/object types.

    Add `src/browser.ts` as a second tsup entry and add a package export for browser consumers without removing the existing default node entry. The browser export must not export `loadFonts`, `renderImage`, `renderVideo`, or anything from `fonts.ts`.

    Repoint existing Next.js imports from `@/lib/templates/canvas-defaults` to the shared source, or keep `src/lib/templates/canvas-defaults.ts` as a thin re-export that imports from `@bragfast/render-core`. Preserve all current tests.
  </action>
  <verify>
    <automated>npm run build --workspace=packages/render-core</automated>
    <automated>npx vitest run src/lib/__tests__/carousel-pipeline.test.ts src/lib/__tests__/preview-sample.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/render-core/src/browser.ts exists and does not contain "fonts", "renderImage", or "renderVideo"
    - packages/render-core/src/canvas-defaults.ts exports the 5 built-in templates
    - packages/render-core/tsup.config.ts builds both src/index.ts and src/browser.ts
    - packages/render-core/package.json exposes the browser entry without breaking the existing main/types fields
    - Existing app tests that depend on built-ins still pass
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add DraftConfig caption support and tests</name>
  <files>
    src/lib/drafts/types.ts,
    src/lib/drafts/validate.ts,
    src/lib/drafts/__tests__/validate.test.ts,
    packages/workspace/src/types.ts
  </files>
  <read_first>
    - src/lib/drafts/types.ts — DraftConfig and DraftObjectContent definitions
    - src/lib/drafts/validate.ts — allowedTop sets and validation flow
    - src/lib/drafts/__tests__/validate.test.ts — existing validator coverage
    - packages/workspace/src/types.ts — workspace-local mirror types
    - .planning/phases/04-workspace-editor-slot-filling/04-RESEARCH.md — Pattern 6 and Pitfall 6
  </read_first>
  <behavior>
    - validateDraftPayload accepts a payload whose config has `caption: "Launch copy"`
    - validateDraftPatchPayload accepts `caption: "Updated copy"`
    - both validators reject `caption: 42` with an error containing "caption must be string"
    - unknown keys other than caption still reject
  </behavior>
  <action>
    Add `caption?: string` to the shared `DraftConfig` interface and the workspace-local DraftConfig mirror.

    Add `"caption"` to create and patch `allowedTop` sets in `src/lib/drafts/validate.ts`. Add a branch that copies caption when it is a string and fails with `caption must be string` when present with a non-string value.
  </action>
  <verify>
    <automated>npx vitest run src/lib/drafts/__tests__/validate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/drafts/types.ts contains `caption?: string`
    - packages/workspace/src/types.ts contains a matching `caption?: string`
    - validateDraftPayload and validateDraftPatchPayload allow `caption`
    - validator tests prove string captions pass and non-string captions fail
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure / DoS | render-core browser import | medium | mitigate | Browser entry omits node-only font/render exports so Vite does not bundle `fs`, `os`, or `path` into Workspace. |
| T-04-04 | Tampering | draft config validation | medium | mitigate | Caption is explicitly validated as a string; existing unknown-key rejection remains in place. |
| T-04-SC | Supply Chain | new npm dependencies | high | mitigate | Human checkpoint verifies `@tailwindcss/vite`, `multer`, and `@types/multer` metadata before install. |
</threat_model>

<verification>
- `npm run build --workspace=packages/render-core`
- `npm run build --workspace=packages/workspace`
- `npx vitest run src/lib/drafts/__tests__/validate.test.ts src/lib/__tests__/carousel-pipeline.test.ts src/lib/__tests__/preview-sample.test.ts`
</verification>

<success_criteria>
- Workspace has styling/test infrastructure without shadcn.
- Browser-safe render-core exports and shared template defaults unblock Workspace preview.
- Caption is accepted by draft create/patch validation and mirrored in workspace types.
</success_criteria>
