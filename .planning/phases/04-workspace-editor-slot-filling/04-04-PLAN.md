---
phase: 04-workspace-editor-slot-filling
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/workspace/src/App.tsx
  - packages/workspace/src/pages/Home.tsx
  - packages/workspace/src/components/TemplatePreview.tsx
  - packages/workspace/src/components/FormatSwitcher.tsx
  - packages/workspace/src/components/BrandPicker.tsx
  - packages/workspace/src/lib/buildDraftObjectData.ts
  - packages/workspace/src/lib/clientFonts.ts
  - packages/workspace/src/__tests__/Home.test.tsx
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-07
  - WORK-08
must_haves:
  truths:
    - "D-01 partially covered: home can enter a single-screen editor path rather than the legacy wizard"
    - "D-02 covered: workspace TemplatePreview ports the kitchen preview scale/font pattern"
    - "D-03 covered: FormatSwitcher exposes stable Landscape, Square, Portrait controls"
    - "D-10 and D-12 covered: Workspace home shows recent drafts and a thumbnail grid of the 5 built-ins"
    - "D-13 and D-14 covered: template selection applies first/default brand colors and logo when present"
    - "UI-SPEC covered: Workspace home/editor chrome uses premium SaaS styling, not NES-retro classes"
  artifacts:
    - path: "packages/workspace/src/pages/Home.tsx"
      provides: "Recent drafts and template grid"
      contains: "Start from template"
    - path: "packages/workspace/src/components/TemplatePreview.tsx"
      provides: "Scaled browser canvas preview"
      contains: "ResizeObserver"
    - path: "packages/workspace/src/components/FormatSwitcher.tsx"
      provides: "Landscape/Square/Portrait control"
      contains: "Landscape"
---

<objective>
Replace the Phase 3 Workspace shell with the real entry experience: recent drafts, five built-in template thumbnails, browser-safe live previews, format switcher, and brand/logo application on template select.
</objective>

<context>
@.planning/phases/04-workspace-editor-slot-filling/04-CONTEXT.md
@.planning/phases/04-workspace-editor-slot-filling/04-RESEARCH.md
@.planning/phases/04-workspace-editor-slot-filling/04-UI-SPEC.md
@.planning/phases/04-workspace-editor-slot-filling/04-PATTERNS.md
@.planning/phases/04-workspace-editor-slot-filling/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port browser preview utilities</name>
  <files>
    packages/workspace/src/components/TemplatePreview.tsx,
    packages/workspace/src/lib/buildDraftObjectData.ts,
    packages/workspace/src/lib/clientFonts.ts
  </files>
  <read_first>
    - src/components/kitchen/template-preview.tsx — ResizeObserver scale and CanvasRenderer usage
    - src/lib/preview-sample.ts — buildDraftObjectData mapping
    - src/lib/client-fonts.ts — browser font injection
    - packages/render-core/src/browser.ts — browser-safe imports from Plan 04-01
    - packages/render-core/src/canvas-renderer.tsx — VideoComponent prop contract
  </read_first>
  <action>
    Port the preview component into the Workspace with no Next.js imports and no node-only render-core imports. Use `ResizeObserver` to scale the canvas inside a stable aspect-ratio wrapper. Port or adapt `buildDraftObjectData` so stored `image_url` becomes `imageBase64` for browser `<img src>` usage.

    Add a native `BrowserVideoComponent` that renders `<video autoPlay muted loop playsInline>` and pass it to `CanvasRenderer`.
  </action>
  <verify>
    <automated>npm run build --workspace=packages/workspace</automated>
  </verify>
  <acceptance_criteria>
    - TemplatePreview imports from render-core browser-safe entry only
    - TemplatePreview uses ResizeObserver and stable aspect-ratio sizing
    - TemplatePreview passes a VideoComponent that renders a muted looping video
    - buildDraftObjectData maps `image_url` and `video_url` into CanvasRenderer object data
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Workspace home with recent drafts and template grid</name>
  <files>
    packages/workspace/src/App.tsx,
    packages/workspace/src/pages/Home.tsx,
    packages/workspace/src/__tests__/Home.test.tsx
  </files>
  <read_first>
    - packages/workspace/src/App.tsx — current repo-context stub to replace
    - packages/workspace/src/api.ts — fetchDrafts helper
    - packages/render-core/src/canvas-defaults.ts — five built-in templates
    - src/components/kitchen/template-picker-dialog.tsx — existing picker pattern
    - .planning/phases/04-workspace-editor-slot-filling/04-UI-SPEC.md — Workspace home layout and copywriting contract
  </read_first>
  <behavior>
    - Home renders the exact empty state heading `No drafts yet`
    - Home renders `Start from template`
    - Home renders exactly 5 template tiles from the built-ins
    - clicking a recent draft calls the reopen handler with the draft id
    - clicking a template calls the new-template handler without creating a draft
  </behavior>
  <action>
    Replace the repo-context table shell with app state that shows `Home` by default. Home must lay out recent drafts and template grid per UI-SPEC, using copy from the contract. Fetch recent drafts through `fetchDrafts()` and tolerate load failure with the exact error copy from UI-SPEC.

    Template browsing must not call `createDraft`. It only selects a template and transitions app state into the editor route/state that Plan 04-05 will complete.
  </action>
  <verify>
    <automated>npx vitest run --config packages/workspace/vitest.config.ts packages/workspace/src/__tests__/Home.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Workspace home shows recent drafts area and 5 template tiles
    - Home uses `No drafts yet`, `Pick a template to start your first local draft.`, `Start from template`, and `Use template`
    - Template click does not call createDraft/postDraft
    - Mobile layout stacks recent drafts above templates
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add FormatSwitcher and BrandPicker</name>
  <files>
    packages/workspace/src/components/FormatSwitcher.tsx,
    packages/workspace/src/components/BrandPicker.tsx
  </files>
  <read_first>
    - src/components/shared/brand-color-picker.tsx — behavior to reimplement without shadcn
    - packages/workspace/src/hooks/useBrand.ts — brand state helper from Plan 04-03
    - packages/render-core/src/canvas-types.ts — FormatKey and FORMAT_DIMENSIONS
    - .planning/phases/04-workspace-editor-slot-filling/04-UI-SPEC.md — component contracts, colors, interaction states
  </read_first>
  <action>
    Create a stable segmented `FormatSwitcher` with `Landscape`, `Square`, and `Portrait`. The active tab uses forest text and lime underline or quiet filled state; tab widths must not change across selection.

    Create a workspace-native `BrandPicker` using native `select` and color inputs/swatches. It must not import shadcn or root app UI primitives. It exposes brand selection, brand colors, and manual color overrides.
  </action>
  <verify>
    <automated>npm run build --workspace=packages/workspace</automated>
  </verify>
  <acceptance_criteria>
    - FormatSwitcher has stable dimensions and labels Landscape, Square, Portrait
    - BrandPicker uses native controls and no imports from `@/components/ui`
    - BrandPicker supports zero brands without throwing
    - Workspace chrome colors use UI-SPEC tokens rather than selected brand colors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Denial of Service | Workspace browser bundle | medium | mitigate | Browser preview imports only the browser-safe render-core entry and is verified by workspace build. |
</threat_model>

<verification>
- `npx vitest run --config packages/workspace/vitest.config.ts packages/workspace/src/__tests__/Home.test.tsx`
- `npm run build --workspace=packages/workspace`
</verification>

<success_criteria>
- The Workspace home is the first screen and lists templates plus recent drafts.
- Template thumbnails render live browser previews.
- Format and brand controls exist for the editor to consume.
</success_criteria>
