---
phase: 04-workspace-editor-slot-filling
plan: 06
type: execute
wave: 4
depends_on:
  - 04-05
files_modified:
  - packages/workspace/src/App.tsx
  - packages/workspace/src/pages/Home.tsx
  - packages/workspace/src/pages/Editor.tsx
  - packages/workspace/src/components/TemplatePreview.tsx
  - packages/workspace/src/components/SlotPanel.tsx
  - packages/workspace/src/components/VisualField.tsx
  - packages/workspace/src/components/BrandPicker.tsx
  - packages/workspace/src/components/SavedIndicator.tsx
  - packages/workspace/src/index.css
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - WORK-07
  - WORK-08
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-04
  - MEDIA-05
must_haves:
  truths:
    - "All Phase 4 success criteria are exercised through build, focused tests, and manual local Workspace verification"
    - "UI-SPEC covered: premium SaaS/product-tool chrome, no NES-retro typography/borders/shadows in Workspace editor"
    - "UI-SPEC covered: stable dimensions prevent layout shifts in preview wrappers, format tabs, template tiles, and upload zones"
    - "Security covered: browser source contains no hardcoded API key, no bearer token, and no hardcoded localhost URLs except returned media URLs from the CLI at runtime"
    - "Deferred-scope guard covered: no image render, video render, scheduling, carousel, template authoring, or AI slot fill is added"
  artifacts:
    - path: "packages/workspace/src/App.tsx"
      provides: "Complete Workspace flow"
      contains: "Editor"
    - path: "packages/workspace/src/index.css"
      provides: "Workspace responsive and accessibility polish"
      contains: "--workspace-forest"
---

<objective>
Run the final integration and polish pass for Phase 4. This plan validates the full Workspace editor experience, closes UI/accessibility gaps, and proves the phase with build, tests, and manual local-server checks.
</objective>

<context>
@.planning/phases/04-workspace-editor-slot-filling/04-CONTEXT.md
@.planning/phases/04-workspace-editor-slot-filling/04-RESEARCH.md
@.planning/phases/04-workspace-editor-slot-filling/04-UI-SPEC.md
@.planning/phases/04-workspace-editor-slot-filling/04-PATTERNS.md
@.planning/phases/04-workspace-editor-slot-filling/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run focused regression suite and close source-level gaps</name>
  <files>
    packages/workspace/src/App.tsx,
    packages/workspace/src/pages/Home.tsx,
    packages/workspace/src/pages/Editor.tsx,
    packages/workspace/src/components/TemplatePreview.tsx,
    packages/workspace/src/components/SlotPanel.tsx,
    packages/workspace/src/components/VisualField.tsx,
    packages/workspace/src/components/BrandPicker.tsx,
    packages/workspace/src/components/SavedIndicator.tsx
  </files>
  <read_first>
    - all files listed above
    - .planning/phases/04-workspace-editor-slot-filling/04-UI-SPEC.md — component contracts and interaction states
    - .planning/phases/04-workspace-editor-slot-filling/04-VALIDATION.md — validation sign-off checklist
  </read_first>
  <action>
    Run the focused tests from Plans 04-01 through 04-05. Fix only gaps that block Phase 4 success criteria: missing exact UI copy, missing labels, non-stable layout dimensions, unsupported file errors, incomplete save states, or build/type errors.

    Do not add Phase 5/6 render controls, schedule controls, AI fill, carousel support, or template authoring.
  </action>
  <verify>
    <automated>npx vitest run packages/cli/src/__tests__/server.test.ts src/lib/drafts/__tests__/validate.test.ts</automated>
    <automated>npx vitest run --config packages/workspace/vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - focused CLI, draft validation, and workspace tests pass
    - grep for "Press Start 2P" in packages/workspace/src returns zero matches
    - grep for "@/components/ui" in packages/workspace/src returns zero matches
    - grep for "/api/v1/upload" in packages/workspace/src returns zero matches
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Build and manually verify local Workspace flow</name>
  <files>
    packages/workspace/src/App.tsx,
    packages/workspace/src/pages/Home.tsx,
    packages/workspace/src/pages/Editor.tsx,
    packages/cli/src/server.ts
  </files>
  <read_first>
    - packages/cli/src/index.ts — how the local server starts
    - packages/cli/src/server.ts — Workspace URL output and media route
    - packages/workspace/src/App.tsx — final screen flow
    - .planning/phases/04-workspace-editor-slot-filling/04-CONTEXT.md — Phase success criteria
  </read_first>
  <action>
    Run `npm run cli:build`, then start the local CLI server using the existing brag command path for this repo. In the browser, verify:
    1. Workspace home loads with recent drafts and 5 template thumbnails.
    2. Selecting any template opens the editor and shows a scaled live preview.
    3. Typing/pasting in each text slot updates preview in real time.
    4. Landscape, Square, and Portrait switch the rendered layout.
    5. Dragging and browsing image/video files fills visual slots and previews them.
    6. Clearing and replacing media updates the preview.
    7. Caption field is visible and saved separately from rendered slots.
    8. Save indicator reaches `Saved`.
    9. Reopening the recent draft restores text, media refs, caption, active format, colors, and brand/logo state.

    If browser automation is available, capture desktop and mobile screenshots and check no horizontal overflow. If automation is not available, record manual viewport results in the plan summary.
  </action>
  <verify>
    <manual>`npm run cli:build` exits 0; local Workspace smoke flow above passes on desktop and mobile widths</manual>
  </verify>
  <acceptance_criteria>
    - all 6 ROADMAP success criteria for Phase 4 are manually verified
    - desktop editor keeps preview and slot panel visible together
    - mobile editor stacks preview above slot panel with no horizontal overflow
    - no UI text overlaps inside buttons, tabs, fields, tiles, or upload zones
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run full phase gate</name>
  <files>
    package.json,
    packages/workspace/package.json,
    packages/cli/package.json,
    packages/render-core/package.json
  </files>
  <read_first>
    - package.json — root build/test commands
    - .planning/phases/04-workspace-editor-slot-filling/04-VALIDATION.md — phase gate commands
  </read_first>
  <action>
    Run the final automated gate: `npm run build` and `npx vitest run`. Fix any failures caused by Phase 4 changes. If a failure is unrelated or environment-only, document it with exact command output in the summary instead of masking it.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - npx vitest run exits 0, or unrelated failures are documented with exact failing test names
    - no Phase 4 deferred-scope features appear in packages/workspace UI
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-ALL | Multiple | Phase 4 integration | high | mitigate | Final full build/test/manual gate validates upload, preview, save, reopen, and browser-only security boundaries together. |
</threat_model>

<verification>
- `npx vitest run packages/cli/src/__tests__/server.test.ts src/lib/drafts/__tests__/validate.test.ts`
- `npx vitest run --config packages/workspace/vitest.config.ts`
- `npm run build`
- `npx vitest run`
- Manual local Workspace smoke flow
</verification>

<success_criteria>
- A developer can pick a built-in template, fill text/media slots, switch formats, enter a caption, auto-save, and reopen the draft.
- Brand logo auto-populates logo slots when available.
- Phase 4 remains scoped to editor/slot filling only.
</success_criteria>
